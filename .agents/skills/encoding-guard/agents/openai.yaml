interface:
  display_name: "encoding-guard"
  short_description: "Detect mojibake and suspicious text-loss regressions"
  default_prompt: "Run encoding-guard before and after edits. Block completion if mojibake or suspicious question-mark text loss is detected."
