interface:
  display_name: "智能 Emoji Conventional Commit"
  short_description: "分析代码变更，以合适的粒度创建符合 Conventional Commits 规范且带有 emoji 的提交"
  default_prompt: "当任务涉及 git commit 时，分析代码变更，判断是否需要拆分 commit，并生成 emoji + Conventional Commit 格式的提交信息，默认优先使用中文 summary。"
