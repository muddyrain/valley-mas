# 刮出传说（Scratch Legend）- 开发任务清单

---

# 🎯 当前阶段

👉 当前已完成：**阶段 7 Prestige 永久成长**

👉 下一步可继续：**阶段 8 体验与数值调优**

当前状态：

* 阶段 0：✅ 已实现
* 阶段 1：✅ 已实现
* 阶段 2：✅ 已实现
* 阶段 2.5：✅ 已实现
* 阶段 2.6：✅ 已实现
* 阶段 3：✅ 已实现
* 阶段 4：✅ 已实现
* 阶段 5：✅ 已实现
* 阶段 6A：✅ 已实现
* 阶段 6B：✅ 已实现
* 阶段 6C：✅ 已实现
* 阶段 7：✅ 已实现

---

# 🧠 核心开发原则

1. 不允许跨阶段开发。
2. 所有玩法必须符合 `scratch-legend-design.md`。
3. 数值、概率、解锁条件和节奏必须以设计文档为准。
4. 新功能必须先进入设计文档，再进入本任务文档，最后才能实现。
5. 当前目标是“形成刮卡循环”，不是堆功能。

---

# 🔒 设计一致性闸门

任何功能变更、玩法插入、数值调整或阶段改动前，必须先做一致性检查。

## 必查问题

1. 这个功能是否已经在 `scratch-legend-design.md` 中定义？
2. 它在核心循环中的位置是什么？
3. 它是否会改变当前阶段的目标？
4. 它是否引入了新的成本、奖励、概率、惩罚或解锁条件？
5. 它是否会破坏“洗盘子是门票，刮刮卡是主菜”的主循环？

## 处理规则

* 如果设计文档已定义，任务文档只允许拆解执行步骤。
* 如果设计文档未定义，必须先更新设计文档，再更新任务文档。
* 如果设计文档与任务文档冲突，以设计文档为准，并先修正任务文档。
* 如果只是实现细节变化，但不改变玩法和数值，可以直接写入当前阶段任务。
* 如果会新增玩法、改变节奏或改变奖励池，不允许直接实现。

## 玩法插入规则

中途想加入新玩法时，必须先在设计文档中说明：

* 它插入核心循环的哪个位置。
* 它替代、补充还是延后现有阶段。
* 它的成本、奖励、风险和解锁条件。
* 它对前 10 分钟节奏的影响。
* 它是否属于当前阶段。

只有完成上述设计对齐后，才能把它加入本任务清单。

---

# 🎨 当前蓝图设计

当前蓝图设计图：

```text
apps/scratch-legend/docs/design-assets/v1-gameplay-overview.png
```

开发使用规则：

* 该图是当前蓝图设计，不是逐像素 UI 还原稿。
* 阶段 0 / 阶段 1 只参考图中的 `1.1 刮盘子界面`、`1.2 结算界面`、`1.3 工作面板`。
* 图中的中期、后期、经济系统、游戏目标、视觉风格模块作为后续阶段蓝图，不允许提前实现。
* 图中的“荣耀奖金 / 成为传说”按 `scratch-legend-design.md` 中的 Prestige 永久成长方向理解。
* 图中的展示数值不覆盖设计文档，真实数值以 `scratch-legend-design.md` 为准。

---

# 🧩 阶段 0：项目初始化与基础骨架（已完成）

## 🎯 目标

建立一个可承载完整单页 MVP 的基础骨架。

## ✅ 任务

* [x] 创建或补齐 Scratch Legend 的 Next.js App Router 单页入口。
* [x] 建立基础页面布局：左侧状态 / 工作区、中央木桌操作区、右侧信息区。
* [x] 参考 `v1-gameplay-overview.png` 的像素风、深色面板、木桌、盘子和电话元素搭建基础视觉。
* [x] 建立玩家状态模型：金币、累计赚到的金币、累计洗盘子次数、累计刮卡次数、连败次数。
* [x] 建立游戏状态机：洗盘子、可买卡、刮卡中、结算中、升级中。
* [x] 预留刮刮卡 Canvas 区域，但不实现刮卡玩法。
* [x] 预留升级 / 解锁内容展示区域，但保持禁用或未解锁状态。
* [x] 页面保持单屏可视，MVP 阶段不做多页面。

## ✅ 验收标准

* 单页可运行。
* 页面能展示基础金币和状态。
* 代码结构能承接后续阶段。
* 未实现洗盘子收益、刮卡、升级、自动化和 Prestige。

## 🚫 禁止

* 不允许提前实现洗盘子收益。
* 不允许提前实现刮刮卡购买或开奖。
* 不允许引入后端、登录、数据库或排行榜。

---

# 🧩 阶段 1：洗盘子启动金（已完成）

✅ 已完成，当前可进入阶段 2。

## 🎯 目标

让玩家通过洗盘子获得第一张低级刮刮卡的启动金，同时避免游戏变成点击器。

## ✅ 核心机制

### 1. 动作锁

```ts
actionDuration = 400~800ms
```

### 2. 盘子成本

```ts
workPlateCost = 1
initialGold = 1
```

### 3. 收益与碎盘

```ts
if (workLevel < 1) {
  reward = +2
} else if (random < 0.9) {
  reward = +configuredLevelReward
} else if (gold - configuredLevelReward >= 1) {
  reward = -configuredLevelReward
  plateBroken = true
}
```

## ✅ 任务

* [x] 左侧展示“日常工作”卡片。
* [x] 日常工作卡片展示盘子费用 `$1`、`等级 0` 和等级进度条。
* [x] 日常工作最高等级为 10 级；每级升级所需盘子数由 `apps/scratch-legend/lib/game-config.ts` 中的 `work.level.platesRequiredByLevel` 统一配置，页面只显示进度条，不显示“等级进度”文字。
* [x] 参考 `v1-gameplay-overview.png` 的 `1.3 工作面板`，展示中奖率、基础收益和升级按钮；升级按钮当前可以展示但不实现升级效果。
* [x] 点击“日常工作”后，在桌面生成一个脏盘子小物件。
* [x] 短点击桌面脏盘子后，稳定进入放大清洁视图。
* [x] 参考 `v1-gameplay-overview.png` 的 `1.1 刮盘子界面`，放大清洁视图展示大盘子、污渍层、海绵和奖励金额，不展示清洁进度文案。
* [x] 玩家必须拖动海绵擦除污渍。
* [x] 根据污渍遮罩擦除比例计算清洁进度。
* [x] 清洁完成前禁止领取奖励。
* [x] 清洁完成前不提前展示本次盘子的实际结算金额；达到可领取状态后才显示真实金额。
* [x] 参考 `v1-gameplay-overview.png` 的 `1.2 结算界面`，清洁完成后显示可领取金额。
* [x] 领取后结算金币，并回到桌面状态。
* [x] 支持连续点击日常工作生成多个脏盘子，每个盘子独立携带奖励。
* [x] 桌面已有未处理刮刮卡时，只要金币足够，仍允许继续生成新的脏盘子。
* [x] 连续点击工作卡或盘子只增强视觉反馈，不增加收益。
* [x] 接近第二段熟练度 `10` 时只提示可解锁刮刮卡，不自动进入阶段二玩法。
* [x] 强化“我在赚第一张卡门票”的反馈。
* [x] 支持桌面脏盘子长按后进入可拖动态，移动后开始拖动；拖动时显示拿起阴影，悬浮时轻微晃动，并保持跟手。
* [x] 新脏盘子从桌子顶部飞入桌面，落点在桌面可操作区域内随机生成。
* [x] 清洁视图使用海绵 cursor，按住拖动时才擦除污渍。
* [x] 每个桌面脏盘子独立保存已擦除点位；清洁一半关闭后，再次打开同一只盘子会恢复上次清洁状态。
* [x] 游戏开局金币为 `$1`。
* [x] 点击日常工作固定花费 `$1` 生成一个脏盘子，等级不改变盘子价格。
* [x] 日常工作等级 `0` 时固定奖励 `$2`，概率为 `100%`。
* [x] 日常工作等级 `1` 起按 `90%` 正收益、`10%` 碎盘扣除当前等级正收益等额金币，并在金币不足继续买盘子时禁用碎盘结果。
* [x] 日常工作等级收益由配置表约束：`等级 1=$2`、`等级 2=$3`、`等级 3=$4`，后续等级继续走配置。
* [x] 辅助道具默认显示“未解锁”占位，不在桌面展示垃圾桶。
* [x] 垃圾桶在熟练度达到 `3` 后解锁购买资格，并播放解锁特效。
* [x] 解锁后需要在辅助道具页花费 `$2` 购买垃圾桶，购买后才显示在桌面右下角。
* [x] 已购买垃圾桶后，脏盘子拖入时打开，松手后删除该脏盘子并关闭。
* [x] 日常工作达到 `等级 1` 后清洁说明开始展示 `10%` 碎盘风险。
* [x] 玩家第一次真实遇到碎盘并完成结算后，电话响动并出现碎盘风险消息。
* [x] 碎盘风险消息参考清洁说明浮层，展示 `90%` 正收益和 `10%` 等额扣款。
* [x] 累计赚到 `$10` 时电话提示刮刮乐模式已解锁，并播放解锁特效；阶段二再实现刮刮乐玩法。
* [x] 电话消息支持点击“我知道了”关闭。
* [x] 阶段一已实现的玩法数值、金币规则、等级成长、奖励表、电话提示和辅助道具解锁统一收敛到 `apps/scratch-legend/lib/game-config.ts`，作为后续静态规则入口。
* [x] 熟练度解锁改为配置驱动：当前里程碑包含 `10` 解锁垃圾桶、`50` 解锁刮刮卡，以及 `300`、`500`、`1000`、`3000`、`5000`、`10000` 的后续分段目标。
* [x] 阶段一当前进度统一抽为 `apps/scratch-legend/lib/game-save.ts`，显式保存当前金币、累计赚到的金币、当前解锁、电话消息状态和桌面工作区状态，作为后续持久化入口。
* [x] 阶段一前端运行态改为 `zustand + localStorage`，由 `apps/scratch-legend/lib/game-store.ts` 统一承接状态管理与本地持久化。
* [x] 当前版本移除右侧具体信息模块，但保留右侧预留区域，供后续玩法接入，并将桌面木桌样式调整为更接近蓝图图 1 的像素切角厚桌沿、自然木纹和轻微露出的短桌腿结构。

## ✅ 验收标准

* 点击“日常工作”不会直接获得金币。
* 点击桌面脏盘子不会直接获得金币。
* 玩家可以在桌面上堆叠多个待清洁脏盘子。
* 新生成脏盘子不会固定出现在同一位置。
* 玩家必须进入放大视图并拖动海绵清洁。
* 清洁达到完成阈值后才能领取奖励，但清洁说明浮层不展示清洁进度文字。
* 金币只在领取奖励时结算一次。
* 开局能用 `$1` 生成第一只脏盘子。
* 盘子生成成本始终是 `$1`。
* 等级 `0` 时不会触发碎盘。
* 等级 `1` 后清洁说明展示碎盘风险；第一次真实碎盘结算后，玩家能看到并关闭碎盘风险电话消息。
* 垃圾桶解锁后可以通过拖拽删除桌面脏盘子。
* 累计赚到 `$10` 后玩家能收到刮刮乐模式解锁电话和解锁特效。
* 30~60 秒可以达到刮刮乐模式解锁提示。
* 玩家能理解洗盘子只是启动金来源。

---

# 🧩 阶段 2：第一张低级安全卡 MVP

✅ 已完成，本阶段不再继续追加功能。

## 🎯 目标

实现主玩法入口：购买第一张低风险教学卡、拖动刮开、渐进揭露、自动结算。

## ✅ 核心机制

```ts
proficiencyMilestones = [10, 50, 300, 500, 1000, 3000, 5000, 10000]
basicSafeCardName = '成双入对'
basicSafeCardPrice = 10
```

## ✅ 任务

* [x] 定义第一张低级安全卡的本地价格与购买入口。
* [x] 左上角进度改为当前段熟练度展示，并按后期路线展示 `0/10`、`0/50`、`0/300`、`0/500`、`0/1000`、`0/3000`、`0/5000`、`0/10000`。
* [x] 第一段熟练度刷满 `10` 后解锁垃圾桶，触发垃圾桶解锁动画。
* [x] 第二段熟练度刷满 `50` 后触发三句电话提示，电话结束后播放“刮刮卡！”解锁动画。
* [x] 刮刮卡购买入口默认不展示，完成刮刮卡电话解锁流程后才出现在左侧。
* [x] 购买时预生成卡片结果。
* [x] 点击“成双入对”购买后，刮刮卡像盘子一样飞入桌面；点击桌面卡片后进入放大刮卡视图。
* [x] 桌面刮刮卡图标缩小到接近盘子 `3/4` 大小，并改为带迷你标题、图片窗口和刮层条的卡面。
* [x] 未打开的桌面刮刮卡支持长按拖动，并可拖入已购买垃圾桶丢弃。
* [x] 打开的刮刮卡支持点击卡片以外区域关闭回桌面。
* [x] 每张桌面刮刮卡独立保存已刮开点位；刮到一半关闭后，再次打开同一张卡会恢复上次刮开状态。
* [x] 使用 Canvas 遮罩实现拖动擦除。
* [x] 刮层笔刷改为不规则碎边和颗粒擦除，视觉上更接近真实刮刮卡。
* [x] 禁止点击直接开奖。
* [x] 实现三格结果区的低门槛教学规则，采用“必须刮出任意一对才给钱”。
* [x] 实现阶段二本地 MVP 奖池，只包含未成对 / 火焰成对 / 纸钞成对 / 钱袋成对，不引入重惩罚。
* [x] 结算后更新金币、刮卡次数和连败次数。
* [x] 保留阶段三要用到的惩罚符号、弃卡和风险卡扩展位，但本阶段不启用。
* [x] 修复刮刮卡结果区遮罩露底问题，刮开前不能提前看到答案。
* [x] 修复已刮到可结算状态的卡刷新后重新显示未刮遮罩的问题。
* [x] 加入破产兜底贷款：金币为 `$0` 且桌面无盘子、无待刮卡时触发电话；有未偿还贷款时再次破产会追加下一笔。
* [x] 贷款电话支持指纹签字，签字后到账 `$5`，贷款利率展示为 `6000%`，偿还金额为 `$300`。
* [x] 右上角显示贷款按钮，打开后展示贷款列表并支持偿还勾销。
* [x] 偿还贷款时展示扣款飘字、已偿清盖章、贷款条目滑出和惩罚解除提示；最后一笔贷款偿还后账本自动收起。

## ✅ 阶段二本地奖池原则

* 未成对结果结算 `$0`，用于明确“没刮出一对不给钱”。
* 火焰成对是基础回收结果。
* 纸钞成对与钱袋成对负责提供翻盘期待。
* 本阶段奖池是 Scratch Legend 的本地 MVP 数值，不宣称等于《刮个爽》原始概率或原始金额。

## ✅ 阶段二本地 MVP 奖池

| 类型 | 真实概率 | 展示概率 | 结算金额 |
| --- | --- | --- | --- |
| 未成对 | 72% | 不展示 | `$0` |
| 火焰成对 | 20% | 50% | `$10` |
| 纸钞成对 | 6% | 40% | `$25` |
| 钱袋成对 | 2% | 10% | `$50` |

说明：

* 低级安全卡名称为“成双入对”，价格为 `$10`。
* 真实概率用于购买时预生成结果，展示概率只用于票面说明。
* 未成对只表示没有刮出一对，不引入阶段三的惩罚符号。
* 阶段二数据结构预留惩罚符号、弃卡和风险卡扩展位，但 UI 与玩法不启用。

## ✅ 阶段二卡片等级补充

本补充属于阶段二“第一张低级安全卡”的成长反馈，不进入阶段三风险卡。阶段 2 已建立卡片等级能力；阶段 2.5 需要按最新设计统一为所有刮刮卡默认 `等级 0` 起算。

### ✅ 任务

* [x] 为“成双入对”增加独立等级与等级进度。
* [x] 每结算 1 张“成双入对”，该卡等级进度增加 1；丢弃未结算卡片不增加。
* [x] 基础等级使用基础奖池：火焰 `$10`、纸钞 `$25`、钱袋 `$50`。
* [x] 累计结算 3 张后进入下一等级，并进入下一段等级进度。
* [x] 下一等级中奖金额按 `1.3` 倍向下取整：火焰 `$13`、纸钞 `$32`、钱袋 `$65`。
* [x] 左侧刮刮卡目录入口展示卡片等级和当前段进度条。
* [x] 放大刮卡右侧信息面板展示当前等级和当前等级奖池，不在组件内散写金额。
* [x] 购买时按当时等级预生成结果，卡片已购买后不会因为后续升级改写本张卡 payout。

### ✅ 验收标准

* 左侧“成双入对”能看到 `Lv.` 与等级进度条。
* 刮完并结算 3 张后，等级进入下一等级，进度重新从下一段开始。
* 下一等级的奖池显示为火焰 `$13`、纸钞 `$32`、钱袋 `$65`。
* 下一等级购买的新卡结算使用对应升级后 payout。
* 未结算或丢弃的刮刮卡不会增加卡片等级进度。

## ✅ 阶段二体验修正补充

### ✅ 任务

* [x] 刮刮卡刮层笔刷大小改为配置驱动，并缩小默认刮除范围。
* [x] 火焰、纸钞、钱袋、未中结果在刮刮卡结果格内改为图标化展示，不只显示纯文字。
* [x] 真实抽奖概率与票面展示概率拆开配置；票面仍展示火焰 `50%`、纸钞 `40%`、钱袋 `10%`，真实概率按阶段二本地 MVP 奖池执行。
* [x] 支持玩家有钱时连续购买多张“成双入对”，桌面保留多张独立刮刮卡。
* [x] 多张桌面刮刮卡都能独立拖动、打开、关闭、结算或拖入垃圾桶。
* [x] 同一中奖 / 未中奖档位的结果槽位会在购卡时随机排列，避免连续卡片固定显示同一图案位置。
* [x] 电话提醒改为只响铃，玩家点击电话后才展示电话消息。
* [x] 贷款按钮增加左右内边距，避免内容贴边。

### ✅ 验收标准

* 刮刮卡不再几下就刮完整张，默认刮刷半径来自配置。
* 刮开后看到的是符号图标，文字只作为辅助说明或可访问标签。
* 多次点击“成双入对”会在桌面出现多张小卡，不会覆盖旧卡。
* 连续购买多张同档位卡片时，刮开后的符号位置不应总是相同。
* 电话响起时不会自动弹出消息；点击电话后才出现消息。
* 贷款按钮左右有明显留白。

## ✅ 验收标准

* 玩家必须拖动刮开。
* 刮开过程有遮罩渐进消失。
* 结果在购买时确定，不在最后一刻随机。
* 刮刮卡在电话解锁完成前不出现在左侧。
* 点击购买后卡片先飞入桌面，再点击桌面卡片进入放大刮卡。
* 未打开刮刮卡可以在桌面长按拖动，也可以拖进已购买垃圾桶丢弃。
* 打开刮刮卡后点击卡片外区域可以关闭回桌面。
* 没有任意一对时结算 `$0`。
* 第一张卡完成的是低风险教学职责，而不是重惩罚职责。
* 买卡、刮卡、结算、继续买卡的循环成立。
* 玩家能感知“洗盘子只是门票，刮卡才是主玩法入口”。
* 刮刮卡结果必须被遮罩完整盖住，拖动刮开前不能露出答案。
* 刮开到可结算状态前，结算按钮和说明区域不能提前展示本张卡的实际 payout。
* 刮开到可结算状态后底层结果必须清晰可读，刷新页面后不能重新盖回未刮遮罩。
* 购买类金额展示为白色，不使用红色。
* 破产且桌面没有可操作对象时，电话会引导签贷款，签字后玩家重新拥有 `$5` 启动金。
* 有未偿还贷款时，右上角能打开贷款列表，列表显示贷款标题、描述、偿还金额和“偿付”按钮。
* 偿还成功后，玩家能看到“已偿清”盖章、扣款飘字和对应惩罚解除提示。

---

# 🧩 阶段 2.5：熟练度 300 / 500 解锁补充

✅ 阶段 2.5 已完成。阶段 6C 后，升级工具迁移到熟练度 `300` 段，“三连胜出”迁移到熟练度 `500` 段。

## 🎯 目标

把阶段二后的中期目标接上，让玩家在反复洗盘子和刮“成双入对”后，先看到升级工具，再解锁第二张规则不同的刮刮卡“三连胜出”。

## ✅ 核心机制

```ts
proficiencyMilestones = [10, 50, 300, 500, 1000, 3000, 5000, 10000]
upgradeToolsUnlockSegment = 300
tripleMatchCardUnlockSegment = 500
tripleMatchCardName = '三连胜出'
tripleMatchCardPrice = 100
scratchCardDefaultLevel = 0
```

熟练度展示规则：

* `10` 段刷满后，左上角进入 `0/50`。
* `50` 段刷满后，左上角进入 `0/300`。
* `300` 段刷满后，左上角进入 `0/500`。
* `500` 段刷满后，左上角进入 `0/1000`。
* `1000` 段刷满后，左上角进入 `0/3000`。
* `3000` 段刷满后，左上角进入 `0/5000`。
* `5000` 段刷满后，左上角进入 `0/10000`。
* 切换目标不重置熟练度、金币、贷款、桌面物件、工作等级或刮刮卡等级。
* 当前目标被一次收益跨过时，溢出的熟练度继续带入下一段，例如 `2/3` 获得 `$2` 后展示 `1/10`。
* 熟练度达到当前目标后应立刻进入下一段进度；电话只作为待领取的解锁提示，玩家不点击电话也可以继续玩当前玩法并积攒后续熟练度。
* 最后一个已配置熟练度段刷满后，不再显示“等待最终挑战”这类暗示未实现玩法的文案；左上角应使用中性的已完成状态。

## ✅ 任务：熟练度 300 解锁升级工具

* [x] 将熟练度分段目标更新为 `0/10`、`0/50`、`0/300`、`0/500`、`0/1000`、`0/3000`、`0/5000`、`0/10000`。
* [x] 最后一个已配置熟练度段刷满后，使用中性的已完成状态，不暗示未实现的最终挑战。
* [x] 第三段熟练度达到 `300` 后，电话开始响铃，但不自动弹出消息。
* [x] 玩家点击电话后，按设计文档播放升级工具电话消息。
* [x] 电话结束后播放“已解锁 升级工具”发光解锁提示。
* [x] 右侧预留区域展示升级工具面板。
* [x] 配置并展示基础工具：`刮卡运气 $200`、`刮除范围 $100`、`铜币 $500`。
* [x] 工具价格、等级、上限和效果全部从 `game-config.ts` 读取。
* [x] “刮除范围”可以影响刮层笔刷半径，但不能让刮卡变成点击开奖或瞬间完成。
* [x] “刮除范围”同步影响洗盘子清洁半径；盘子基础清洁半径独立配置，并保持明显大于刮刮卡基础刮除范围。
* [x] “刮卡运气”实装为真实奖池概率调整；“铜币”先按配置预留入口，不提前实现自动刮卡。

## ✅ 任务：通用刮刮卡规则统一

* [x] 所有已解锁刮刮卡在金币足够时都允许连续购买多张。
* [x] 每次购买都独立扣费、独立预生成结果，并生成新的桌面小卡，不覆盖旧卡。
* [x] 所有未打开的桌面刮刮卡都支持自由拖动。
* [x] 所有桌面刮刮卡都支持打开、点击外部关闭、结算，以及拖入已购买垃圾桶丢弃。
* [x] 所有刮刮卡都拥有独立等级和等级进度。
* [x] 所有刮刮卡默认从 `等级 0` 开始，禁止不同卡片使用不同起始等级命名。
* [x] 所有可见 UI 只展示当前等级和等级进度条，不显示等级进度数字、分母或 `MAX` 文案。
* [x] “成双入对”从 `等级 0` 使用基础奖池，结算 3 张后升到 `等级 1` 并使用 `1.3` 倍 payout。
* [x] 新增“三连胜出”时同步接入通用等级结构，默认 `等级 0`。

## ✅ 验收标准：通用刮刮卡规则

* 玩家有足够金币时，连续点击同一卡片入口会生成多张独立桌面卡。
* 不同类型刮刮卡可以同时存在于桌面，并能分别拖动和处理。
* 任意未打开刮刮卡都可以在桌面自由拖动。
* 左侧目录中每张已解锁刮刮卡都从 `等级 0` 开始展示。
* 左侧目录、放大刮卡信息面板和升级工具面板不显示等级进度数字。
* 结算某张卡只推进该卡自己的等级进度，不影响其他卡。

## ✅ 验收标准：升级工具

* `300` 段刷满后，左上角显示切到 `0/500`。
* 电话只响铃，必须点击电话才出现升级工具消息。
* 电话消息结束后才出现升级工具解锁提示与右侧升级面板。
* 右侧面板能看到三项基础工具及价格。
* 工具配置不散写在组件里。

## ✅ 任务：熟练度 500 解锁三连胜出

* [x] 第四段熟练度达到 `500` 后，电话开始响铃，但不自动弹出消息。
* [x] 玩家点击电话后，按设计文档播放新刮刮卡电话消息。
* [x] 电话结束后播放“已解锁 三连胜出”发光解锁提示。
* [x] 左侧刮刮卡目录新增“三连胜出”，价格 `$100`。
* [x] “三连胜出”购买前不展示为可买入口；未解锁槽位继续显示锁定状态。
* [x] 点击“三连胜出”后，如果金币足够，扣 `$100` 并预生成结果；金币足够时可连续购买多张。
* [x] “三连胜出”像盘子和“成双入对”一样飞入桌面，支持自由拖动、打开、关闭、结算和丢弃。
* [x] 放大视图使用蓝色横向卡面，左侧 5 个圆形结果位，右侧独立刮层视觉块，真实可刮遮罩只覆盖左侧结果位，卡面外右侧展示中奖说明。
* [x] 实现 5 格结果区：必须出现 3 个相同图标才中奖，否则结算 `$0`。
* [x] 票面展示概率为 `30% / 30% / 30% / 10%`，真实概率按设计文档单独配置。
* [x] 三连胜出真实奖池、票面奖池、价格、耐刮度和刮刷配置全部来自 `game-config.ts`。
* [x] “三连胜出”接入独立等级与等级进度结构，默认显示 `等级 0`，本阶段可先不启用等级数值成长。

## ✅ 验收标准：三连胜出

* `500` 段刷满后，左上角显示切到 `0/1000`。
* 电话消息完成前，左侧不会出现可购买的“三连胜出”。
* 电话消息完成后，左侧出现“三连胜出”入口，价格 `$100`。
* 买卡时立即扣钱并预生成结果，不在结算时再抽奖。
* 金币足够时可以连续购买多张“三连胜出”，桌面旧卡不会被覆盖。
* 未打开的“三连胜出”可以在桌面自由拖动。
* 三连胜出必须拖动刮开，不能点击开奖。
* 没有 3 个相同图标时结算 `$0`。
* 三连胜出目录等级默认从 `等级 0` 开始。
* 右侧票面概率和真实概率分离，组件内不散写概率。

---

# 🧩 阶段 2.6：金币与刮卡揭露特效

✅ 已完成。该阶段只做体验反馈，不新增卡片规则、概率、奖励或惩罚。

## 🎯 目标

让玩家在金币增加、金币减少和刮出每个图案时获得清晰、短促、不卡顿的即时反馈。

## ✅ 任务

* [x] 新增统一特效层，用于承接金币浮字、金币栏闪烁和少量粒子。
* [x] 金币增加时播放金色反馈：金币栏短暂放大 / 闪光，并显示 `+$X` 浮字。
* [x] 金币增加时金币栏数字从旧值短暂滚动到新值，避免数值瞬跳。
* [x] 金币减少时播放扣款反馈：金币栏轻微下沉 / 抖动，并显示 `-$X` 浮字。
* [x] 区分正常消费、碎盘扣钱、还贷和中奖到账的视觉强度。
* [x] 洗盘子领取奖励、刮刮卡中奖、贷款到账必须触发金币增加特效。
* [x] 生成脏盘子、购买刮刮卡、购买升级、偿还贷款、碎盘扣钱必须触发金币减少特效。
* [x] 刮刮卡每个结果格第一次被刮出时播放一次图案闪烁。
* [x] 单个结果格必须刮开到至少 `95%` 才触发揭露闪烁，刚蹭到角不算。
* [x] 已触发过闪烁的结果格继续刮时不能重复闪烁。
* [x] 未中奖 / 空白图案闪烁必须弱于中奖图案。
* [x] 刮完整张卡后，中奖组合需要额外同步闪烁；“三连胜出”只强调三个相同中奖图案。
* [x] 特效必须限制同屏事件和粒子数量，避免影响刮盘子与刮卡性能。
* [x] 支持 `prefers-reduced-motion`，减少动画用户保留静态或弱动效反馈。

## ✅ 验收标准

* 金币增加时能明确看到 `+$X` 和金色金币栏反馈。
* 金币减少时能明确看到 `-$X` 和扣款反馈，且不会误认为奖励。
* 连续洗盘子、连续买卡、快速刮盘子和快速刮卡时不出现明显卡顿。
* 每个刮刮卡结果格第一次露出时只闪烁一次。
* 中奖组合在刮完整张卡后有额外强调。
* “三连胜出”的 5 个结果位揭露闪烁与遮罩覆盖区域对齐。

---

# 🧩 阶段 3：风险卡与 Peek / 轻惩罚

✅ 已完成，本阶段不再继续追加功能。

## 🎯 目标

引入《刮个爽》式风险决策，让玩家开始面对“继续刮还是止损”的选择。

## ✅ 任务

* [x] 实现第一张局部排雷型风险卡，至少包含 `4~6` 个可刮区。
* [x] 风险卡必须在票面上预埋真实危险符号，不能只在结算时临时随机惩罚。
* [x] 惩罚符号应与揭露行为绑定，默认在完全揭露危险位后触发。
* [x] 支持局部 Peek 或等价的半揭露判断能力。
* [x] 支持在完全揭露前放弃当前卡，并扣除固定处理费或价格的 `20%~40%`。
* [x] 实现轻惩罚结果，例如收益归零、少量扣金或额外弃卡成本。
* [x] 风险卡默认仍可手动处理，但不应成为基础自动化的默认目标。
* [x] 惩罚不得导致玩家前中期直接崩盘。

## ✅ 验收标准

* 玩家能明显感到“继续刮可能爆奖，也可能踩惩罚”。
* 玩家能通过局部揭露做出继续或止损判断。
* 放弃有成本，但不会让前期无法继续。
* 风险判断来源于揭露过程，而不是最终纯开奖。

---

# 🧩 阶段 4：升级与更多卡类

✅ 阶段 4 已完成。阶段 3 已完成，中期升级项、更多卡类骨架与自动刮刮机购买入口已收口。

## 🎯 目标

让玩家从“反复刮第一张卡”进入“升级能力、解锁不同规则卡”的中期循环。

## ✅ 任务

* [x] 先把卡册 / 目录结构补出来，每个卡册至少包含稳定票、风险票、高赔率票和终局票的职责位。
* [x] 实现至少两个升级项，优先从幸运、刮擦效率中选择。
* [x] 升级消耗金币。
* [x] 升级效果必须明确影响刮卡体验、真实概率、结算倍率或自动化行为中的一种。
* [x] 阶段 4 先在内部建立第一册内的稳定票、风险票、高赔率票和终局票职责位；玩家 UI 不直接展示“第一本卡册 / 下一本卡册 / 设计中”等开发结构文案。
* [x] 后续新卡必须有不同成本、不同规则或不同风险，不允许只是数值放大。
* [x] 未解锁内容不提前展示，达到条件后再通过电话、解锁特效或新增入口自然出现。
* [x] 自动刮刮机通过 `1000` 熟练度分段里程碑获得购买资格，价格 `$1000`，并在可购买时显示差额提示。
* [x] 自动刮刮机不再要求第一本卡册“街角好运”结算 `10` 张卡，和前期解锁统一走熟练度。
* [x] 自动刮刮机后续升级线先保留为配置，不在玩家 UI 中提前展示锁定位。
* [x] 自动刮刮机入口说明其职责为“自动处理稳定票”，并明确风险票和终局票仍需手动。
* [x] 未解锁票不在玩家 UI 中提前展示；终局票规则未开放前不显示入口。
* [x] 达成熟练度里程碑后，辅助道具页提供购买入口；金币不足时显示差额并禁用购买按钮，金币足够后可购买并扣除 `$1000`。
* [x] 左上角进度始终保持纯数字分段熟练度，不切换为“自动机 N/10”，电话提示不阻塞熟练度继续累计。

## ✅ 阶段 4 自动刮刮机边界

* 阶段 4 只在熟练度达标后展示自动刮刮机购买入口；不实现自动购买、自动刮开或自动结算。
* 阶段 4 可以展示 `还差 $X` 和购买价格，但不展示下一目标、本册进度或锁定升级项。
* 自动刮刮机真实运行逻辑归入阶段 5。

## ✅ 当前升级规则

* [x] “刮除范围”初始升级价格调整为 `$100`。
* [x] “刮除范围”“刮卡运气”“铜币”支持按当前等级递增价格，默认倍率为 `1 / 1.5 / 2.25 / 3.4 / 5.1 / 7.6 / 11.4 / 17 / 25.5 / 38`。
* [x] 购买升级时按当前等级动态扣费，升级面板也展示当前等级的下一次购买价格。
* [x] “刮卡运气”每级从未中奖真实概率中转移 `3%` 到中奖档位，未中奖总概率最低保留 `45%`。
* [x] “刮卡运气”当前影响“成双入对”和“三连胜出”，不影响“险中求财”的危险符号与止损规则。

## ✅ 验收标准

* 玩家有明确升级目标。
* 同一目录内至少能看见“稳定票 / 风险票 / 爆点票 / 终局票”的分工。
* 升级面板中“刮除范围”等级 0 价格为 `$100`，后续至少能看到 `$150`、`$225` 的递增。
* 升级面板中“刮卡运气”等级 0 价格为 `$200`，等级 1 价格为 `$300`。
* 购买“刮卡运气”后，新购买的“成双入对”和“三连胜出”使用调整后的真实奖池；已在桌面上的旧卡不被 retroactively 改写。
* “险中求财”的危险位、放弃成本和轻惩罚不受“刮卡运气”影响。
* 新卡不是第一张安全卡的纯数值放大。
* 玩家刷满自动刮刮机段 `1000` 熟练度后，会看到解锁提示，并能在辅助道具页看到自动刮刮机购买入口和 `$1000` 价格。
* 左上角只显示纯数字分段熟练度，不出现“自动机 N/10”或下一目标文案。
* 玩家不会提前看到容量、力量、速度三条自动刮刮机升级线的锁定目标。
* 达成条件后，玩家能购买自动刮刮机并看到已解锁状态，但机器仍不会自动处理卡片。
* 未解锁卡片和终局票不会提前显示；玩家只看到已经解锁的票种。
* 5~10 分钟内能形成“买卡 - 刮卡 - 升级 - 追下一册”的循环。

---

# 🧩 阶段 5：自动刮卡

✅ 阶段 5 已完成。自动刮刮机第一版队列、自动补“成双入对”、按时间处理、自动结算、暂停 / 继续、自动购买开关、最低保留金币、单张票手动接管、允许票种过滤 UI 和更高阶票自动化边界已通过验收。

## 🎯 目标

解锁自动化，让玩家从手动刮卡进入收益放大阶段。

## ✅ 任务

* [x] 设置自动化解锁条件。
* [x] 自动化需要有队列容量、处理速度、可处理票池和风险过滤配置。
* [x] 自动购买并处理低风险卡。
* [x] 自动执行擦除与结算。
* [x] 保留动画和时间消耗。
* [x] 高风险卡默认不自动处理，或要求玩家手动开启。
* [x] 终局票 / Final Chance 票必须强制手动处理。
* [x] 玩家可以把单张票切回手动接管。
* [x] 购买自动刮刮机后进入已解锁状态，基础容量为 `2` 张，基础处理时间为 `8s / 张`。
* [x] 自动刮刮机状态至少包含：未解锁、空闲、补票中、处理中、暂停、阻塞。
* [x] 自动刮刮机购买卡片时立即扣费并预生成结果，处理完成时只揭露并结算，不允许临时抽奖。
* [x] 第一版默认只自动处理“成双入对”。
* [x] “三连胜出”需要后续机器力量升级才允许自动处理。
* [x] “险中求财”和终局票默认不可自动处理。
* [x] 支持整台自动刮刮机暂停 / 继续控制。
* [x] 支持自动购买开关，关闭后不再自动补新票，但仍可处理已有队列。
* [x] 支持最低保留金币配置，自动购买不会让金币低于保留值。
* [x] 支持允许票种过滤配置。
* [x] 金币不足或低于最低保留金币时，机器进入阻塞状态。
* [x] 队列已满或没有可处理票种时，机器进入阻塞或暂停状态。
* [x] 桌面展示机器本体、队列槽位、当前处理进度和完成反馈。
* [x] 自动机控制面板保留上次结算结果，玩家可以持续查看上一张自动刮卡奖励。
* [x] 自动结算后的金币浮字、数字滚动和下一次自动补票之间保留短暂视觉节奏，避免扣款反馈打断中奖反馈。

## ✅ 验收标准

* 自动刮卡不是瞬间批量开奖。
* 自动化不能完全替代玩家决策。
* 玩家仍然能感受到刮卡过程。
* 玩家可以把机器当前处理或队列中的单张票接管回桌面，之后按普通刮刮卡手动打开、刮开和结算。
* 基础自动化只适合安全票和低风险票。
* 机器运行时能看到队列和单张处理进度。
* 机器不会自动处理“险中求财”或终局票。
* 自动购买不会突破最低保留金币配置。
* 自动机面板能开关“成双入对”是否允许自动补票；关闭后没有可处理票种时会进入阻塞并显示原因。
* 自动机面板只对已经出现的高阶票展示自动化边界：“三连胜出”需要后续机器力量，“险中求财”仍需手动，不会真正加入自动队列。
* 队列满、金币不足、自动购买关闭、没有允许票种、低于保留金币等情况会显示具体原因，而不是只显示泛化阻塞文案。
* 自动处理完成后会更新金币、卡片等级进度和本册进度。
* 自动处理完成后，上次结算结果会留在自动机面板中，直到下一张自动票结算后更新。
* 自动处理完成后，中奖金币浮字和金币数字滚动先完成主要反馈，再进入下一次自动补票扣款。
* 已在队列或处理中的卡片不会因为后续升级被 retroactively 改写结果。

---

# 🧩 阶段 6：高风险卡与终局卡

⚠️ 当前可进入本阶段；开始实现前必须先对齐本文档与 `scratch-legend-design.md` 的高风险卡和终局票规则。

## 🎯 目标

引入后期刺激，形成高成本、高风险、高爆点的挑战。

## ✅ 任务

### 阶段 6A：架构与规则设计

* [x] 对齐 `scratch-legend-design.md`，确认阶段 6A 先做第一张高风险票，不同时实现终局票。
* [x] 定义新增票种必须收口的规则能力：职责、进度 key、自动化权限、决策模式、结算模式、刮层布局和展示文案。
* [x] 明确新增高风险票不能进入基础自动刮刮机，且刮卡运气默认不影响高风险票。
* [x] 定义第一张高风险票“步步加码”的解锁条件、价格、逐层止盈金额、爆雷手续费、预生成路径和结算边界。
* [x] 明确旧存档兼容要求：新增票种进度时必须默认补齐，不清空金币、贷款、升级、桌面物件或自动机队列。

### 阶段 6A：实现任务

* [x] 先做卡片规则 / 能力描述收口，减少 `ScratchCardType` 新增后在 `game.ts`、`ScratchLegendGame.tsx`、`ScratchCardCanvas.tsx` 中继续复制分支。
* [x] 新增“步步加码”配置：`cardType = push-luck`、价格 `$800`、4 层刮区、逐层可收手金额和爆雷手续费。
* [x] 为“步步加码”新增纯逻辑测试，覆盖路径预生成、逐层安全 / 爆雷、收手结算、手续费破产保护和等级进度推进。
* [x] 扩展存档结构和合并逻辑，为“步步加码”增加独立卡片进度，并兼容旧存档缺字段。
* [x] 新增“步步加码”购买入口，只在自动刮刮机已购买且 `1000` 熟练度段达成后展示。
* [x] 实现“步步加码”桌面卡片、放大票面、4 层刮区和逐层揭露状态。
* [x] 将“步步加码”桌面小卡和放大票面改成高风险阶梯视觉，避免长得像普通刮刮卡。
* [x] “步步加码”使用更小的默认刮层笔刷，并在单格达到 `95%` 揭露阈值后自动清空该格残留遮罩。
* [x] 实现“见好就收 / 继续加码”手动决策；未选择继续前不能刮下一层。
* [x] 爆雷后本票收益归零并扣除本层手续费；扣款必须遵守至少保留 `$1` 的保护。
* [x] 结算后更新金币、卡片等级进度、刮卡次数、连败和金币浮字反馈。
* [x] 自动刮刮机继续只处理允许自动化的票；“步步加码”只能显示手动边界，不提供入队开关。
* [x] 跑 `pnpm --filter @valley/scratch-legend typecheck`。
* [x] 跑 `pnpm --filter @valley/scratch-legend check`，如只剩既有格式化差异需明确说明。
* [x] 按项目规则提供人工验收标准，不使用 Playwright 自动验收。

### 阶段 6C：后期熟练度路线重设

目标：把 `1000` 从“步步加码”解锁点改为自动化入口，新增 `3000`、`5000`、`10000` 后期路线，让“步步加码”在完整刷满 `10000/10000` 后才解锁。

新路线：

| 当前段目标 | 累计真实阈值 | 解锁 / 作用 |
| ---: | ---: | --- |
| `10` | `10` | 垃圾桶 |
| `50` | `60` | 刮刮卡 |
| `300` | `360` | 升级工具 |
| `500` | `860` | 三连胜出 |
| `1000` | `1860` | 自动刮刮机资格 |
| `3000` | `4860` | 自动机稳定期 / 后期准备 |
| `5000` | `9860` | 高风险票预热或自动机强化 |
| `10000` | `19860` | 步步加码 |

实现任务：

* [x] 更新 `game-config.ts` 的熟练度分段与里程碑 id / label / description。
* [x] 将“步步加码”解锁条件从原 `1000` 段迁移到 `10000` 段。
* [x] 补齐 `3000` 和 `5000` 两个后期承接节点，先保证路线存在，不在玩家 UI 暴露“设计中”。
* [x] 移除或替换运行时“等待最终挑战”文案，避免暗示未实现的最终玩法。
* [x] 更新熟练度阈值、下一目标和“步步加码”解锁相关测试。
* [x] 同步更新设计文档、任务文档中旧的 `3 / 10 / 50 / 100 / 250 / 1000` 路线引用。
* [x] 跑 `pnpm --filter @valley/scratch-legend typecheck`。
* [x] 跑 `pnpm --filter @valley/scratch-legend check`。
* [x] 跑 encoding 检查。
* [x] 按项目规则提供人工验收标准，不使用 Playwright 自动验收。

### 阶段 6B：终局票 / Final Chance

* [x] 对齐 `scratch-legend-design.md`，明确第一张终局票玩家可见名称为“最后一刮”，类型标识建议为 `final-chance`。
* [x] 明确阶段 6B 只进入“本轮结算 / 等待 Prestige”状态，不实现真正 Prestige 重置、永久升级或下一轮加速。
* [x] 新增“最后一刮”配置：价格 `$5000`、5 格终局刮区、`3` 个传说符号成功、固定结果概率和荣耀点预览。
* [x] 将终局票接入卡片规则 / 能力描述：`role = finale`、`automationPolicy = never`、`decisionMode = finale-choice`、`settlementMode = prestige-trigger`。
* [x] 终局票购买时立即扣除 `$5000` 并预生成完整 5 格结果；后续刮开和结算只能消费预生成结果，不能临时抽奖。
* [x] 解锁条件为：已购买自动刮刮机、步步加码已解锁、步步加码完成 `3` 次有效结算；未解锁前不在玩家 UI 中展示入口。
* [x] 为存档补齐终局状态，至少记录：终局票是否已解锁、是否已完成、本轮终局成败、传说符号数量、荣耀点预览和本轮结算状态。
* [x] 旧存档进入游戏时自动补齐终局字段，不清空金币、贷款、升级、桌面卡片、已刮遮罩状态、自动机队列或步步加码进度。
* [x] 终局票永远不能被自动刮刮机购买、加入队列、处理或结算；本轮进入结算状态后自动刮刮机应停止继续补票。
* [x] 终局票不受“刮卡运气”影响，不能复用普通票或步步加码的概率加成。
* [x] UI 新增“最后一刮”购买入口、桌面票面、5 格刮区、本轮结算反馈和荣耀点预览；玩家界面不出现“阶段 6B”“下一目标”“设计中”等开发文案。
* [x] 处理终局票后，无论成功或失败，都让玩家明确知道本轮已经到达结算时刻，不能继续用同一轮资源无限补救。
* [x] 为终局票新增纯逻辑测试，覆盖解锁条件、购买扣费、预生成结果、成功阈值、荣耀点预览、旧存档默认值和自动化排除。
* [x] 为 UI / 状态派生新增必要测试，覆盖未解锁不展示、解锁后展示、结算后停止购买 / 自动补票，以及玩家可见文案不泄露开发阶段。
* [x] 跑 `pnpm --filter @valley/scratch-legend typecheck`。
* [x] 跑 `pnpm --filter @valley/scratch-legend check`。
* [x] 跑终局票相关测试。
* [x] 跑 encoding 检查。
* [x] 按项目规则提供人工验收标准，不使用 Playwright 自动验收。

## ✅ 验收标准

* 高风险卡让玩家产生“可能翻盘，也可能亏”的决策感。
* “步步加码”的桌面小卡和放大票面必须能一眼看出是逐层加码票，而不是普通刮刮卡换色。
* “步步加码”每层都必须先刮开再决策，不能点击按钮直接开奖。
* 单层图案刮到揭露阈值后，残留银色遮罩会自动清空；未解锁的后续层仍保持遮罩。
* 玩家安全刮开一层后，可以明确选择“见好就收”或“继续加码”。
* 玩家选择继续后才允许刮下一层，且下一层结果来自购买时预生成路径。
* 爆雷后不能继续加码，本票收益归零，并按当前层手续费扣款但保留至少 `$1`。
* “步步加码”不会被自动刮刮机购买、处理或结算。
* 旧存档升级后仍能正常进入游戏，原有金币、贷款、桌面卡片、自动机状态和升级不丢失。
* 终局票能成为本轮阶段性目标。
* 处理终局票后，玩家会明确知道这一轮该结束了。
* 不会破坏前中期经济循环。

---

# 🧩 阶段 7：Prestige 永久成长

⚠️ 当前禁止开发，先完成阶段 6。

## 🎯 目标

建立后期局外循环，让玩家重置本轮进度换取永久成长。

## ✅ 任务

* [ ] 设置 Prestige 条件，优先由终局票 / Final Chance 票触发。
* [ ] 重置当前金币、卡片解锁、本轮升级、自动化队列和本轮临时负面状态。
* [ ] 给予永久点数。
* [ ] 实现至少两个永久升级，优先从起始资金、永久幸运中选择。
* [ ] 给出至少一个“卡册起步优势”或等价的中期推进型永久升级。
* [ ] 新轮次更快回到中高阶卡片。

## ✅ 验收标准

* 第一次 Prestige 目标在 10~20 分钟左右出现。
* Prestige 后玩家明显变强。
* 永久成长不直接跳过核心刮卡过程。
* 玩家能感受到“这一轮结算值得，下轮明显更快”的反馈。

---

# 🧩 阶段 8：体验与数值调优

⚠️ 当前可进入本阶段。

## 🎯 目标

调顺前 10 分钟节奏和长期经济。

## ✅ 任务

* [ ] 校准洗盘子到第一张卡的时间。
* [ ] 校准第一张安全卡期望收益。
* [ ] 校准升级成本。
* [ ] 校准下一类卡片解锁速度。
* [ ] 校准自动化收益。
* [ ] 检查是否存在无限赚钱或完全破产。

## ✅ 验收标准

* 0~1 分钟买到第一张卡。
* 1~3 分钟完成多张低级卡。
* 3~5 分钟开始形成“再买一张看看”的循环。
* 5~10 分钟看到下一类卡片和自动化目标。

---

# 🚫 全局禁止

* 后端服务
* 数据库
* 登录系统
* 排行榜
* 多页面结构
* 未写入设计文档的玩法
* 当前阶段之外的实现

---

# ✅ 完成标准

每个阶段完成时必须：

1. 当前阶段任务全部勾选。
2. 对照设计文档确认玩法、数值、概率一致。
3. 运行适用校验。
4. 更新本任务文档状态。
5. 说明未验证项和剩余风险。

---

👉 核心认知：

**任务文档负责“什么时候做什么”，设计文档负责“为什么这么做、数值是什么、玩法是否成立”。**
