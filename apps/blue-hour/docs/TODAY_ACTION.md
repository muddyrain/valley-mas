# 今日行动选择

2026-09-12。营地点击「整装出发」后打开今日行动选择，选中任务卡后才能「确认出发」。Esc、手柄 B 或「返回营地」回到整备，浏览和取消不会写档或消耗天数。键盘左右移动卡片焦点，Enter / 手柄 A 选择；Tab 或向下到确认按钮后再出发。

## 三类地点

按用户确认，全部复用当前东岸旧街的模型、固定 Seed 20260912、既有搜索点、导航及撤离点。差异由 `data/today_actions/*.tres` 配置，不复制地图生成器，也不新增空投箱、降落事件或独立地图布局。

| 地点 | 食物倍率 | 废料倍率 | 装备地点 | 初始敌人比例 | 刷怪间隔倍率 |
| --- | ---: | ---: | --- | ---: | ---: |
| 住宅区 | 1.5 | 0.7 | 废弃轿车 | 0.5 | 1.35 |
| 商业街 | 0.8 | 1.5 | 修车铺、废弃轿车 | 0.75 | 1.0 |
| 空投回收 | 0.45 | 1.0 | 修车铺、库房、废弃轿车 | 1.0 | 0.7 |

物资按每个地点四舍五入，初始敌人数向上取整；刷怪间隔倍率越小，敌人出现越频繁。其余生命、伤害、时段、技能、搜索和撤离规则沿用当前游戏。卡片底部显示全图基础食物/废料储量、可获得装备的地点数及可搜索地点数，悬停说明它们并非保证带回量；白昼时间包含已装备被动的延时。

当前基础储量依次为：住宅区 32 食物 / 55 废料 / 1 个装备点，商业街 17 / 120 / 2，空投回收 8 / 79 / 3。它们是第一版配置，未经过长期试玩调平。

## 状态与存档

`ui/today_action_screen.gd` 只持有草稿，通过信号交给 `core/main.gd`。`Campaign.start_action(action_id)` 校验 ID 并保存可选字段 `selected_action`；版本仍为 v3，v1/v2/v3 的既有存档允许缺省此字段，未知 ID 被拒绝。

只有保存成功后才复制基础地图、应用地点配置并创建 Mission；失败时恢复整份 Campaign 数据，停留在当前卡片。重复确认在 Mission 状态被拒绝。行动中退出继续原有“回到当天营地重试”规则，保留上次确认的地点与原有 `day_rewards`，再次打开选择页恢复该选择；可以改选，奖励不重新抽取。日结算后 `_prepare_day()` 清空选择。

程序内无参数的 `start_mission()` 仍用于既有基准测试与独立外出启动检查，使用原始东岸地图。正式营地按钮统一走选择页。

## 素材与验证

10 张 PNG 保存在 `assets/ui/today_action/`，源字节与压缩包逐张一致，原名、现名、尺寸和 SHA-256 见同目录 `sources.json`。素材说明与当前实现分开记录。区域切片、原生控件和纸卡反馈见[UI 素材登记](../art/UI_ASSETS.md)。

- `tests/today_action.gd`：41 项通过，覆盖取消不写入、选择门槛、三类地点资源与敌情差异、失败回退、重复确认、重试与旧档兼容、实际启动三类行动及跨日结算。
- `tests/today_action_runtime.gd`：58 项通过，以独立 Godot 原生窗口和合成输入检查键盘/手柄/鼠标、三张卡片、返回、1024×640、1280×720、1440×900、2560×1080。默认 1600×900 也保存截图。
- 原生截图：`test-output/today-action-*.png`；结构化结果：`test-output/today-action-runtime.json`。基准验证使用独立 `user://test-runs/`，不操作玩家窗口或存档。
- `run.ps1 -Mode test/build` 包含流程检查，`capture/build` 包含原生检查；构建附加独立 EXE 的今日行动 Headless / 原生启动。

Windows 构建于 2026-09-12 13:17（+08:00）完成：`build/BlueHourHomeward.exe`，413,588,256 字节，SHA-256 `7C8C1B32B487EA9A36F2B4DE139894D1C7BE9E5A0F7B85EE77C51D672ECA6C0B`。构建内的地图、模型、玩法、五日流程、效果系统以及完整原生开局/出发/归航检查通过；把 EXE 单独复制后，主菜单、营地、行动、今日行动、展厅共 10 项 Headless / 原生启动通过。日志见 `test-output/today-action-build.log`，汇总见 [VALIDATION](VALIDATION.md)。

人工待验：三种资源缺口下的路线取舍、危险梯度和五日节奏；自动化通过不代表数值平衡定稿。没有新增第三方依赖，未提交 Git。

仓库收尾检查：定向编码、diff 空白、AGENTS 上下文与文档链接检查通过。默认 pnpm 命令调用的 WSL 缺少虚拟磁盘，改用本机 Git Bash 执行原检查脚本；Harness 检查仍报告已有的 Godot skill 未登记及根兼容链接不是软链接。这些既有问题没有在本次页面开发中改动，不能把该项记录为通过。
