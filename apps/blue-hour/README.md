# 蓝时归航 / BLUE HOUR: HOMEWARD

Godot 4.x + GDScript 的 **0.5.0 道具与技能扩充试玩**。在原有 Resource 系统上扩展为 8 个被动道具、6 个特殊技能，均有普通/升级两档和用户提供的 100×100 图标。新游戏仍选择作战、搜集或勘查专精，获得一个起始被动和一个起始技能；保留两名成员、并行搜索、小队指向射击和五日归航循环。

安全屋的「道具与技能」管理已持有内容，每个技能每日一次。持有清单、装备槽位、容量与升级状态均可保存；道具和技能只允许 Normal → Upgraded 一次。地图发现、槽位购买和正式升级费用尚未接入；验收新增内容可在安全屋按 F1，选择「给予全部道具与技能并扩容」，再选择「升级全部已持有道具与技能」。数值和实现边界见 [效果系统规格](docs/EFFECT_SYSTEM_SPEC.md)。

**0.4.1 首页更新**：接入用户提供的背景、Logo、六组菜单图标、底部入口与静态海报；开始游戏、继续存档和专精选择支持鼠标、键盘及手柄。角色图鉴、营地档案为当前数据只读页，设置可切换全屏。外部入口尚无发布地址；逐像素还原仍缺手写装饰与部分同款切图，详见 [首页素材与差异](art/UI_ASSETS.md)。

玩法基线见 [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)，已实现范围见 [docs/PLAN.md](docs/PLAN.md)，验证证据见 [docs/VALIDATION.md](docs/VALIDATION.md)。这不是完整游戏。

## 打开与验证

试玩可直接双击 `build/BlueHourHomeward.exe`，无需安装 Godot；资源内嵌在单个 Windows x64 可执行文件中。开发时使用 Godot 打开 `project.godot`。本轮使用 Godot **4.7.2.stable.official.ed1daf0bf**，Compatibility 渲染。项目独立于 pnpm/Turbo，不需要额外插件、下载美术或第三方运行依赖。

Windows 的 `run.ps1` 支持 `run`、`editor`、`art`、`import`、`test`、`smoke`、`capture`、`build`；自动查找 `GODOT_BIN`、PATH 或 Downloads 中的便携 Godot，也接受 `-GodotPath`。`build` 需要匹配引擎版本的 Windows x64 导出模板，依次执行模型与地图集成、规则、行动、派遣、并行操作、跨日规则、跨日行动、开局规则、新队伍五日流程和效果系统验证、Release 导出、独立 EXE 的 Headless 和原生启动以及内嵌模型检查，成功后更新 `build/BUILD-INFO.json`。`capture` 运行美术展示、行动、跨日界面、并行操作、开局、首页和效果系统七组原生验证。每次功能交付都构建最新 EXE。完整可复制命令集中在[仓库项目指南](../../docs/PROJECT_GUIDE.md#蓝时归航原生-godot)。

## 操作

| 操作 | 行为 |
| --- | --- |
| 新游戏 / 专精 Tab / 确认创建 | 选择起始套餐并生成两名成员；取消不替换旧进度 |
| 基地角色 / 名单 | 选择成员，查看等级、特质、武器与升级成本 |
| 升级成员 | 花食物提高本轮等级和 Trait，最高 5 级 |
| 行动特殊技能按钮 | 分别施放已装备技能，各每日一次；战术暂停可施放，持续时间随模拟暂停 |
| 安全屋「道具与技能」 | 查看已持有的道具/技能、升级效果及装备槽位；换装立即保存 |
| 安全屋武器下拉框 | 分配实际拥有的武器；选中其他成员持有的同一把时交换配装 |
| 今日备用装备 | 花费标明的 Scrap 购买一件武器；货单每天固定，购买立即保存 |
| 确认结算 | 入账、消耗当晚口粮并推进一天；连续缺粮时先选择优先供养成员 |
| 左键点击地面 / 按住左键 | 设置集合点 / 持续跟随鼠标；绕过建筑与车辆，途中自动攻击 |
| 按住 Ctrl | 掩护成员停步朝鼠标射击；可以打空，正常消耗弹药和换弹，近战只迎击近处敌人 |
| 点击感染者 / F | 集火指定敌人 / 最近敌人；射程不足的队员会接近 |
| X / 停止 | 停止掩护成员移动并取消撤离准备；保持搜索任务和自动攻击 |
| 点击建筑、入口、车辆或右侧物资点 | 自动派最近的可抵达空闲成员；可按当前人数同时派往不同地点 |
| 点击场景中正在搜索的地点 | 取消该处搜索，其他地点继续工作 |
| 点击侧栏活动地点 / “查看搜索” | 查看当前任务，保持所有任务运行 |
| “改派当前搜索” / “取消当前搜索” | 显式更换执行者 / 单独取消；进度保留，不抢其他任务成员 |
| R / 全队集合 | 结束全部搜索，所有存活成员步行前往最近设置的集合点 |
| 点击巴士 / E | 全队靠近巴士，等待 8 秒准备和关门后结算 |
| 滚轮 / “定位小队” | 缩放镜头 / 镜头回到队伍 |
| WASD / 右键或中键拖动 | 平移镜头；右键不再重复左键命令 |
| 空格 / 暂停 | 暂停、继续模拟；暂停时仍可查看和下达命令 |
| Esc | 打开、关闭暂停菜单；菜单挡住下层行动操作 |
| F1 | 行动内调试时段、战斗与携带资源；安全屋可授予全部正式道具/技能、扩容和一次升级 |

每名存活成员最多执行一个任务，允许全员分头搜索、零人掩护；旧三人队伍继续支持三处并行。移动、停止、集火与 Ctrl 只指挥掩护成员；搜索者在可见入口工作，工作时不攻击。搜索先按执行者特质计算，再叠乘工具腰包及搜刮狂潮的耗时倍率，最低 0.2 秒；换弹和上车不受影响。附近有可见敌人或受到攻击时暂停搜索并自卫，安全持续 1.25 秒后继续；召回、改派和中断保留进度。完成后自动归队，阵亡只停止对应任务，由玩家决定是否再派人。左右面板同时显示各任务、执行者与状态。

按住左键只有从地面开始才连续带队，拖过建筑不会连发派遣。Ctrl 松开后原地自动迎敌；移到 UI 上停止指向射击。打开菜单、Debug 或窗口失焦时清除持续输入，避免误操作。对照来源及未核实的原作细节见 [操作核查](docs/DEADLY_DAYS_CONTROLS_RESEARCH.md) 和 [ADR-0003](docs/adr/0003-parallel-search-and-direct-controls.md)。

掉落需要接近拾取。撤离会召回搜索者，巴士不会丢下仍存活的队员，阵亡会单独列入结算；全灭时本次物资遗失。

## 本切片内容

- 五座可搜建筑、三辆可搜车辆和一辆归航巴士；同一份地图数据生成碰撞、寻路阻挡及显示。
- 林汐「稳准」提高攻击伤害；乔安「寻迹」提高搜索速度；言秋「坚韧」降低承伤。
- 岸灯手枪、雨燕冲锋枪、门闩霰弹枪、归途撬棍。远程自动换弹，霰弹枪具有扇形多目标命中，近战只在近处生效。
- 缓行者、疾行者、夜犬；特殊敌人鸣响者周期呼叫增援。敌人复用对象池，寻路分散在不同帧，最多同时 85 名活动敌人。
- 白昼 150 秒、BLUE HOUR 18 秒；夜晚持续提高速度、伤害和增援频率。冷暖主光、环境光、城市亮窗、巴士暖灯与合成声音共同区分阶段。
- Debug 支持切三个时段、增减食物、增加废料、生成四种敌人、清敌、给任意队员四种武器、全队无敌及 1/2/4 倍时间。

安全屋新开局随机两人、6 食物；旧三人存档保留原队伍，每人每日消耗 1；足粮后休整至满血，首次缺粮使次日生命上限降至 80%，再留一天补粮。连续不足时根据实际可供养人数选择成员，后果在确认前显示；营内死亡的装备留在库存。战斗阵亡成员本轮无法恢复，随身武器遗失；只剩一人仍可搜索和撤离，全灭结束本轮。重新开局恢复角色可用性并清空上一轮物资与装备。

修车铺、库房和废弃轿车可带回差异武器；首批词条为长枪管、扩容、快装和近战加重。新装备需成功归航后使用；商店不因反复开界面或退出重进刷新。城区的普通点食物已降低，选择装备路线会消耗储备。

## 保存与退出

自动存档位于 Godot 的 `user://homeward/run.json`；当前 Windows 项目目录为 `%APPDATA%/Godot/app_userdata/蓝时归航 - BLUE HOUR- HOMEWARD/homeward/`。保存使用校验和、临时文件替换和 `.bak` 备份；无法写入时提示错误并保留原状态，损坏记录可回退到有效备份。

安全屋的购买、换装、训练和结算会保存。启动先显示主菜单，继续游戏恢复原队伍；版本 1/2 在内存迁移为版本 3，首次保存额外保留原版本 `.v1.bak` / `.v2.bak`。专精切换只是预览，创建成功才替换旧 Run。行动中退出，续玩回到当天出发前，本日技能使用随现场重试而重置，装备奖励不重抽；归航后退出会恢复待确认的结算，不能重复领取同一批收获。自动化全部使用 `user://test-runs/` 下的独立记录，不覆盖玩家存档。

## 代码与数据边界

| 路径 | 职责 |
| --- | --- |
| `core/main.tscn`、`core/main.gd` | 主菜单、开局选择、安全屋、行动与结算的场景装配 |
| `core/run_ledger.gd` | 单次行动的携带物资、装备与一次性结果 |
| `core/campaign.gd` | 本轮成员、武器个体、每日口粮、饥饿、商店、日推进与结束 |
| `core/equipment.gd`、`core/save_store.gd` | 武器差异计算；存档校验、替换与恢复 |
| `data/*_data.gd`、`data/**/*.tres` | 幸存者、Trait、武器、敌人和地图 Resource |
| `data/catalog.gd` | 内容目录、ID 引用和参数校验 |
| `time/mission_clock.gd` | 昼夜边界与夜间临时威胁规则 |
| `missions/mission.gd` | 掩护命令、目标选择、刷怪、归队、掉落和撤离编排 |
| `core/effect_modifiers.gd` | 统一倍率聚合、最终伤害、远程射速和最短搜刮耗时 |
| `missions/special_power.gd`、`missions/special_power_state.gd` | 多技能每日状态、独立持续时间和临时效果清理 |
| `missions/threat_selector.gd` | 集中火力的 Boss/Elite 优先、同级最近目标选择 |
| `ui/shelter_view.gd`、`data/shelter_layout.tres` | 复用现有模型的基地空间与成员点选 |
| `missions/search_task.gd` | 单人搜索任务、遇险暂停、改派与释放；进度仍由搜索点持有 |
| `missions/squad_input.gd` | 持续带队、Ctrl 指向、镜头、命令键和输入释放 |
| `survivors/aim_fire.gd` | 沿鼠标方向命中，复用武器射程、扇形、弹药和墙体遮挡 |
| `survivors/`、`enemies/` | 个体移动、攻击状态、弹药、生命及特殊行为 |
| `maps/city.gd` | 平面网格寻路、射线选取、碰撞与 3D 模块化城区 |
| `vfx/`、`blue_hour/`、`audio/` | 可替换占位模型、射击反馈、昼夜灯光和声音 |
| `ui/`、`debug/` | Godot Control 原生界面与调试入口 |
| `tests/` | 基础与跨日规则、行动集成、原生输入与路线诊断 |

跨日规则、价格和装备奖励池来自 `data/day_loop.tres`，词条来自 `data/affixes/`。新增内容先改资源定义与 `.tres`，联查目录校验和消费模块。模型替换只改表现层，玩法不读取具体 Mesh 子节点。网格路径使用 Godot 内置 [AStarGrid2D](https://docs.godotengine.org/en/stable/classes/class_astargrid2d.html)，占位音效使用 [AudioStreamWAV](https://docs.godotengine.org/en/stable/classes/class_audiostreamwav.html)。

## 当前局限

环境与基础枪械已接入 **BLUE HOUR STARTER ENVIRONMENT KIT V1**：52 个自主 Blender 资产，包含可重复生成的源文件、GLB 和共享材质。角色仍为 Placeholder，声音为程序生成的短音和环境低频；没有成品角色动画、配音或高级视效。数值仍是第一轮基线，路线探测不等于真实玩家体验或性能基准。仅验证当前 Windows Godot 环境；其他 Godot 4.x 小版本和平台尚未验证。截图、报告、导出物和编辑器缓存均忽略提交。

本版没有每日地点三选一、营救补员、战斗经验、跨局专精解锁、通用治疗/区域火力支援、基地经营、主线或完整 10～20 日循环；五日终点只是这一小步的试玩范围。开局与成长规格见 [NEW_RUN_SPEC](docs/NEW_RUN_SPEC.md)，跨日基础见 [DAY_LOOP_SPEC](docs/DAY_LOOP_SPEC.md)。

## 美术资产生产与检查

图片与 UI 的新增归属及文件命名见 [资源目录说明](art/RESOURCE_LAYOUT.md)：游戏对象素材归对应领域，共享控件归 `assets/ui/common/`，界面专属素材归 `main_menu/` 或 `route_selection/`。目录整理历史见 [资源迁移记录](docs/ASSET_MIGRATION_2026-09-12.md)，当前 122 个资源的旧名与新名映射见 [命名迁移记录](docs/ASSET_NAMING_2026-09-12.md)。

新增美术先读 [ART_BIBLE](art/ART_BIBLE.md)、[ASSET_PIPELINE](art/ASSET_PIPELINE.md) 与 [MODEL_CATALOG](art/MODEL_CATALOG.md)，优先复用现有模块。`art/build_assets.ps1` 按道路建筑、道具、车辆、枪械、感染者五批依次执行 Blender → GLB 校验 → Godot 导入与实例化，失败即停止；运行资产在 `assets/generated/`，可编辑源文件在 `art/blender/sources/`。

运行 `./apps/blue-hour/run.ps1 -Mode art` 或打开 `scenes/debug/art_showcase.tscn` 可查看全部资产，按类别聚焦；Tab 切换 Day / BLUE HOUR，滚轮缩放，右键拖动旋转。`test` 和 `build` 包含模型导入与地图美术回归，`capture` 包含美术展示截图；`build` 还验证独立 EXE 中全部模型的加载。完整步骤见资产管线文档。
