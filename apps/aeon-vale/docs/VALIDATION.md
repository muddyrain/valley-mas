# 纪元谷验证记录

验证日期：2026-09-06。当前版本：0.14，功能、原生画面、性能取样、录像、导出和发布启动检查完成。环境：Windows、Godot `4.7.2.stable.official.ed1daf0bf`、Compatibility / OpenGL 3.3、NVIDIA GeForce RTX 3060，驱动 591.86。

## 0.14 验证

本轮按用户的新截图修复土壤语义和地形美术，重做玩家地震，完全隐藏近景云体/云影，扩大默认窗口并修正主菜单。v11 增加裸土状态；旧档陆地仍保有原生态，水下气候不再直接决定新铺土壤的外观与植被。地震新设计取代第十三轮的纯地表痕迹方案，旧地裂和痕迹仍保留兼容。

### 逻辑与真实输入

- `headless-final14.log`：十二组共 **992 项**检查全部通过。其中 `earth_and_soil.gd` 新增 **125 项**，验证裸土颜色、先播种后施肥、十八类生态、播种保留底土、水下高度重置、自然边缘扩散、开关、存档续演、v10 字节备份、非法裸土数据、地震升降及四种笔刷边界、形状变化、撤销和重做。
- 首次新增复现测试在修改生产代码前出现预期的四项失败：新土颜色错误、肥料从无播种土上生长、无种子仍长旧生态植被、水域地震不生效。实施后转为通过。
- 本轮原生操作共 **416 项**：`runtime14.log` 175、`seasons_runtime14.log` 93、`weather_runtime14.log` 16、`interface_design14.log` 77、`landscape_runtime14.log` 34、`earth_runtime-final14.log` 21。均无失败。运行于测试窗口，通过 Godot 实际输入、真实渲染和独立 `user://test-runs/` 数据目录验收，未把测试夹具称为用户当前存档。
- `earth14-plain-soil-middle.png` 与 `earth14-seeded-soil-middle.png`：实际连续拖动铺土、再点击撒草籽；裸土不会继承湿地绿色，未播种的外围继续保留土色。土壤图标也改为土色。
- `earth14-island-far/middle/near/ground.png`：同一 384×256 世界在远中近景及隐藏植被下检查；原有宽大弯曲色纹被替换为稳定低对比像素颗粒与小幅阶梯色调。增量地表及远景更新与整图重绘一致。
- `earth14-cloud-middle.png`、`earth14-cloud-hidden-near.png`、`earth14-rain-without-cloud-near.png`：近景显示策略为零，云体与独立云影不提交绘制；雨滴仍显示，后台雨云状态保留并遵守暂停。
- `earth14-quake-before/dust/after.png`：实际点击地震，底土地形改变，枯树保留在受损陆地；地图与特效使用相同的震动偏移，逻辑镜头位置不变，结束后归位。录像另外展示升起山体的结果。
- `earth14-menu-default.png`、`earth14-menu-small.png`：默认大窗口和 1100×720、115% 界面下均无按钮与边饰重叠；上下按钮行对齐。默认大小上限 1600×1000，并受显示器可用区域约束。

### 性能

固定 384×256、种子 781936、植被丰度 0.9。未在性能采样时并行执行本任务的其他 Godot、录像或重负载检查。采样时用户的 0.13 和 WorldBox 进程仍运行，未关闭或调整它们，因此跨时段对比不是隔离环境下的严格基准。

| 场景 | 帧数 | P95 | P99 | 最高 | >33ms / >50ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| 0.13，1440×900，1x 静置 | 1786 | 12.189ms | 16.101ms | 22.941ms | 0 / 0 |
| 0.14，1440×900，1x 静置 | 1799 | 10.645ms | 13.612ms | 21.445ms | 0 / 0 |
| 0.14，1600×1000，1x 静置 | 1755 | 11.976ms | 16.353ms | 24.799ms | 0 / 0 |

两种 0.14 窗口均实测约 15 秒，世界实际推进约 15 秒，2623 株植物，生态扩散 30 格。报告：`idle-after14.json`、`idle-large14.json`。

`weather-motion-after14.json` 为 1440 帧真实键盘、滚轮与持续施肥；四片晴云、三片降雨/酸雨云及三股龙卷风同时活动，1x/5x 各远中近三组，12 项操作检查通过。最高 38.107ms，18 帧超过 33ms，无超过 50ms；其中 1 帧在 1x、17 帧在 5x。0.13 同类记录最高 39.464ms、9 帧超过 33ms，故不宣称叠加灾难性能全面提升。1x 常规观察稳定，但 5x 多灾难与连续施肥仍存在长帧。

### 产物与检查

- [16.1 秒原生录像](../test-output/AeonVale-0.14-soil-earthquake.mp4)：1440×900、30fps、483 帧，2,753,542 字节；源帧 `film14/`，脚本 `earth_film.gd`。展示真实铺土、播种、地形下降/升起及近景雨滴，录像不作为性能证据。
- [Windows 0.14](../build/AeonVale-0.14.exe)：2026-09-06 22:59:31 导出，111,476,120 字节，版本 0.14.0.0；0.13 文件保留，未关闭用户正在运行的旧版游戏。导出日志 `export14.log`。
- 发布程序实际启动运行 900 帧后正常退出，退出码 0，标准错误文件 0 字节；`smoke14.log` 确认使用 OpenGL / RTX 3060。证据为 `smoke14-exit.txt`、`smoke14-error.log`。
- 编码检查通过；根 `check:agents-context` 12 个入口、`check:docs-links` 20 文档 / 37 本地链接通过。应用文档最终复核 15 篇、83 个本地链接，0 个未解析；历史 0.11/0.12 安装文件已不在当前目录，已将历史记录改为普通文本，保留原交付时间与字节数。
- 原仓库符号链接门禁问题沿用 0.13 的记录；本轮未改兼容入口，未自动提交。

## 0.13 验证

本轮新增主动酸雨，重做雨滴、水花、闪电、火焰、地震和龙卷风，加入海面波纹。保存升级 v10，保留 v1–v9 迁移；旧雨云仍为普通雨，旧档不新增历史地震痕迹，旧龙卷风保留剩余寿命。

### 行为与画面

- 实施前 `tempests.gd` 复现六个失败：酸雨不使树转枯、不留下云，地震不留外围枯树，龙卷风初始破坏过大、路径不转向、JSON 存读档后续行不同。
- 实施后原有十套逻辑检查 825 项通过，新 `tempests.gd` 42 项通过，覆盖四种酸雨笔刷、移动后的新损伤、自然天气不产生酸雨、地震裸土保存及撤销重做/重画清理、旧档备份、损坏字段拒绝、龙卷风大小与路径/续行等。
- 首轮真实 Godot 输入 `tempests_runtime.gd` 14 项通过：长按雨及合并云、暂停/恢复、闪电起火与雨立即灭火、地震裸土/枯树与后台地表一致、酸雨转枯、龙卷风成长消散和倍速、海浪时钟。
- 首轮截图审阅后，增加雨滴与浪花像素尺寸，修圆龙卷风顶口，改用像素火焰；把龙卷风与火焰合入共享图集，给远景短粒子设绘制上限。最终完整运行 175 项、年份/神力/笔刷原生回归 93 项、原降雨回归 16 项、风雨灾难录屏回归 15 项通过，共 299 项。证据为 `runtime-final13.log`、`seasons_runtime-final13.log`、`weather_runtime-final13.log`、`tempests-record13.log`。
- 原完整运行测试在访问地形按钮前遇到一次分类点击未完成，报出不存在的按钮键后等待。已停止该测试进程，将点击坐标读取放到布局实际绘制完成后，增加失败截图和明确退出；不改变产品分类逻辑。最终复测 175 项全通过。
- 录屏用真实沙漏控制从 1x 切至 5x，核对可见倍率，随后恢复 1x。中间一次录屏脚本遗漏展开倍率列表，造成三项检查失败；补上展开操作后 15 项全通过。最终 942 帧、30fps 录像见 [效果录像](../test-output/AeonVale-0.13-effects.mp4)，实际截图为 `170-rain13-cast.png` 至 `182-waves13.png`。

### 性能取样

所有性能场景独立执行，不与录屏并行。384×256、种子 781936、植被 .9，与 0.12 相同。普通 1x 静置 15 秒：1786 帧，P95 12.189ms，P99 16.101ms，最大 22.941ms，无超过 33/50ms 的帧；旧版同图最大 27.054ms，无超过 33/50ms。

初轮多天气压力测试仍有长帧，加入酸雨和三股龙卷风后有一次 123.919ms 长帧。收紧远景粒子绘制、共享动画图集和降雨粒子预算后，最终独立复测如下：

| 场景 | 样本 | 1x 各距离 P95 | 5x 各距离 P95 | 最大帧 | 超过 33/50ms |
| --- | --- | --- | --- | --- | --- |
| 0.12 三雨云、四浮云、连续施肥及移动缩放 | 1440 帧 | 18.074 / 25.484 / 16.988ms | 24.450 / 33.782 / 28.197ms | 56.357ms | 23 / 3 |
| 0.13 同场景，最终绘制优化后 | 1440 帧 | 13.704 / 16.979 / 11.976ms | 17.469 / 20.926 / 17.968ms | 30.641ms | 0 / 0 |
| 0.13 将一片雨云改为酸云、增加三股龙卷风 | 1440 帧 | 14.785 / 19.760 / 13.307ms | 23.954 / 29.072 / 28.532ms | 39.464ms | 9 / 0；9 帧均在 5x |

对应 `weather-motion-after12.json`、`weather-motion-after13-optimized.json` 与 `weather-motion-tempests13-optimized.json`；两套新压力测试各 12 项输入/世界推进检查通过。每种速度及距离取 240 帧，帧耗时影响实际经历的世界秒数，不等于完全相同的逐帧植物状态；该结果仅说明此硬件和取样场景下的表现，不代表任何地图或数量的灾难都稳定 60 帧。录屏不参与性能结论。

### 文档与仓库检查

中文编码检查通过。根目录三项文档/协作检查初次因系统 Bash 指向缺失的 WSL 虚拟盘而无法启动，改用已安装的 Git Bash 后：`check:agents-context` 通过（12 个入口），`check:docs-links` 通过（20 篇、37 个本地链接）。`check:harness` 仍报告原有根 `AGENTS.md` 和四个技能兼容入口是普通文件而非预期符号链接；已核对这些路径属性，未改动本轮以外的协作入口。此项未通过，不能宣称仓库所有门禁通过。

### 交付

Windows 0.13 已导出：`build/AeonVale-0.13.exe`，111,470,720 字节，文件版本 0.13.0.0，时间 2026-09-06 22:12:28。实际启动运行 900 帧后正常退出，退出码 0，标准错误 0 字节，实际使用 OpenGL 3.3 / RTX 3060。0.12 程序仍保留原文件。所有输入测试使用 `user://test-runs/`，不覆盖玩家存档。

最终录像 `test-output/AeonVale-0.13-effects.mp4`：1440×900、30fps、942 帧、31.4 秒、3,459,027 字节，已核对文件元数据及各效果实际画面。应用文档检查 15 篇、81 个本地链接全部解析；计划、使用说明、领域术语、素材记录和根项目指南已同步。未自动提交。

## 0.12 验证

本轮按用户确认完成土地质感、晴天浮云、换生态后的施肥与自然更新，以及固定缩略图建图流程。保留 84 秒一年、原有植物成熟与成年寿命、雨云灭火、暂停倍速和森系界面；存档升级为 v9。

### 实现与实际画面

- 草原、桦木林和热带草原使用共享角点的草土色块与低对比细纹，淡苔草、干土及少量草簇区分区域。首轮画面中较强的多层色带再次收敛，避免形成等高线状杂纹。修正主导树种列表被去重的问题，保留白桦等树种的原定出现比例；健康旧植物不因美术调整而删除。
- 晴天浮云采用四种原创分层像素纹理，云影错开，近景变淡。最多四片，单片 140–220 世界秒，默认首片约 12 世界秒后出现，之后每 38–70 世界秒尝试补充；关闭自然天气后不再自动生成，已有云按剩余寿命结束。移动与淡入淡出均依据世界时间，暂停冻结；晴天云不降雨、灭火或催熟。浮云绘制不使地表缓存失效，雨云仍使用既有降雨节奏和功能。
- 两类肥料先清理笔刷覆盖的适生区域内枯木与不适生植物，再计算新植物间距；保留健康适生树木、灌木和岩石。正常占位和树间距不报叉，水体、裸岩、岩石及着火地块仍有受限提示。催生结果立即写入世界，以约 1.25 秒动画呈现，暂停时也完成。
- 枯木消解按个体分散到约 41–63 世界秒，空出的适生位置进入自然补生，通常半到一年可见区域内新苗。低矮草花可以为自然大型植被让位，健康灌木保留；新苗继续按原物种速度成熟，不保证每个旧树坐标一对一补回同一株树。
- 八张固定模板 PNG 随游戏读取，切换模板和参数不启动地图生成。点击创建才取得新随机种子并启动单个生成任务；本轮实际创建样本约 8.188 秒，期间渲染 1,198 帧，加载保持响应。右侧细调替换模板页，返回保留参数，左侧与底部创建入口保留；植被滑条与标签中心对齐，没有纵向滚动容器。

画面证据均为实际 Godot 输出，无后期拼接。改前同种子截图保留为 `before12-140-landform-overview.png` 至 `before12-143-landform-ground.png`；改后为 `150-map12-overview.png` 至 `153-map12-ground.png`，使用同一 384×256、种子 781936 配置。`154-map12-meadow-birch-savanna.png` 和 `155-map12-three-soils.png` 为三类草土对照，后者隐藏植物；`156`–`158` 为云体/云影及近景减淡，`159`–`163` 为实际换生态、暂停施肥和不施肥的自然更新。`design12-new-world.png`、`design12-advanced.png`、`design12-small-advanced.png` 验证正常及 1100×720 小窗口、115% 偏好下的页面，另检查 90% 界面大小。矩形三生态场景专用于对照，不是随机地图轮廓。

### 规则、旧档和原生交互

`rules12-final.log` 为本轮完整十套无头检查：20 / 30 / 151 / 53 / 40 / 49 / 195 / 22 / 30 / 235，共 825 项，0 失败。新增换代检查涵盖十八类生态 × 两类肥料的实际产出、健康树与岩石保留、旧枯木清理、自然草花让位、真实无效土地、暂停、浮云存读档与关闭后的消散。

v9 保存浮云位置、寿命及下一次补充节奏，真实 JSON 往返和分段推进结果一致。兼容 v1–v8，原地图不重生成，原有年份、活植物进度和活动灾难秒数保留；旧枯木按消解进度比例映射到新时长。验证 v8 读取不会写回原文件，首次覆盖 `.v8.bak` 与原字节完全一致；v7 日历迁移和更早兼容检查继续通过。

| 原生检查 | 结果 |
| --- | --- |
| 完整运行 | 173 项通过，真实创建、地图操作、保存读取、完整植物生命过程及大地图 |
| 界面操作 | 220 项通过，分类、时间、设置、存读档、手记及尺寸适配 |
| 暂停倍速与笔刷 | 93 项通过，实际 1x 和 5x 时间推进、暂停及笔刷 |
| 植被专项 | 24 项通过，当前生态植物与主动肥料 |
| 建图与生态交融 | 34 项通过，固定缩略图、创建时随机、实际配置、WASD、二十年交融及地表局部更新 |
| 主动施法反馈 | 147 项通过，浆果、肥料、长按、暂停反馈、移动缩放与无效地块 |
| 天气与 Esc | 16 项通过，降雨灭火、暂停、设置与 Esc |
| 极近景 | 8 项通过，32 倍、滚轮锚点、WASD 与笔刷 |
| 界面设计 | 77 项通过，固定示意图、滑条对齐、独立细调、参数保留、无纵向滚动和小窗口 |
| 本轮换代与浮云画面 | 7 项通过，实际换生态后施肥、正常林地不重复冒叉、不施肥更新、云层暂停及倍速 |

上述十套有覆盖重叠，不累计为独立功能数量。通过记录分别位于 `native12-final.log` 的前五套、`feedback12-final.log`、`native12-remaining.log` 的后四套。所有测试使用隔离的 `user://test-runs/` 世界及偏好，不改玩家数据；没有同时运行两组 GPU 测试。

### 性能样本与限制

真实 1x 静置使用同一 384×256、种子 781936、植被丰度 0.9、正常 HUD、无主动施法，分别观察约 15 秒。原始记录为 `idle-before12.json`、`idle-after12.json`。

| 样本 | 帧数 | P95 | P99 | 最大 | >33.333 ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| 本轮改前 | 1,793 | 11.521 ms | 15.381 ms | 26.607 ms | 0 |
| 本轮改后 | 1,758 | 13.254 ms | 17.345 ms | 27.054 ms | 0 |

两次样本末尾均为 2,623 株植物、30 次生态替换。本轮增加土地与浮云效果后 P95 略高，未测得持续周期长帧，也不据此宣称整体性能提升。普通连续移动/缩放另以固定模拟步长运行 2,700 帧，包含暂停、1x、5x 与三个距离；`motion-after12.json` 中暂停最大 13.731 ms，1x 最大 28.632 ms，5x 最大 37.063 ms，总计一帧超过 33 ms、零帧超过 50 ms。该固定步长检查与真实时间静置分开解释。

组合压力测试同时加入三片雨云、四片晴天浮云、原地长按施肥、WASD 和滚轮；六个场景各 240 帧，1,440 帧共 23 帧超过 33 ms、3 帧超过 50 ms，最大 56.357 ms。全部 12 项交互条件通过；1x 三个距离的 P95 为 18.074 / 25.484 / 16.988 ms，5x 为 24.450 / 33.782 / 28.197 ms。原始记录为 `weather-motion-after12.json`、`weather-motion12-final.log`。高倍率、集中施肥和多云同时发生时仍有偶发长帧，不能把普通观察样本写成所有操作都流畅的保证。

### 检查过程中处理的问题

- `renewal12-pass1.log` / `renewal12-pass2.log` 记录早期自然换代与浮云往返失败。自然换代检查原先要求旧树的精确坐标仍有新苗，忽略了邻树间距与区域补生；改为检查旧枯木释放及区域新苗，并保留自然树苗让草、健康灌木保留的具体检查。浮云则确有十进制 JSON 浮点尾差，生产代码改为每世界秒以百分之一格更新位置、绘制时插值，随后真实 JSON 和分段推进一致。
- `renewal12-native1.log` 原先把岩石上的叉也当作错误；实际画面确认岩石被保留，检查改为只排除适生且无岩石/火焰的正常林地叉号。没有通过删除真正无效位置反馈来让检查通过，`renewal12-native2.log` 与最终原生检查均通过。
- 批量原生运行在主动反馈检查的首张截图前长时间等待，未输出脚本错误，终止了该测试进程。该检查另有“打开建图立即随机”的历史断言，按本轮明确要求更新为固定缩略图与无生成任务，增加 90 秒超时退出；单独重跑 147 项通过。等待的唯一成因尚未确定，不把重跑通过当成已经证明其根因，也不称批量日志整体退出成功。

### Windows 交付与文档

Windows 0.12（历史构建，当前目录未保留） 于 2026-09-06 20:58:42 本地时间导出，111,462,464 字节，文件/产品版本 0.12.0.0。保留 0.11 和更早程序，玩家旧世界可直接读取。独立发布程序使用默认入口实际初始化 OpenGL，运行 900 帧退出码为 0、标准错误为空，见 `export12-release.log`、`export12-smoke.stdout.log`、`export12-smoke.stderr.log` 和 `export12-smoke.exit.txt`。

产品计划、领域术语、使用说明、美术来源、参照记录、运行脚本与根项目指南同步为本轮规则；历史轮次保留为历史事实。定向编码检查通过，15 份应用文档的 78 个本地链接全部可解析；本轮启动的 Godot 检查进程均已退出。美术观感仍需玩家在新版中体验判断，本轮原生截图与测量提供可复查依据。

## 0.11 验证

本轮只调整界面与验证工具：沿用森系幻想绘本主题，重排建图、底栏、世界设置、造世手记和当前神力卡。地图计算、植物生命周期、84 秒日历、天气、主动施法与 v8 存档格式没有更改。

### 实际画面与交互

- 建图窗口改为大地图预览及配置两栏，八种模板保持缩略图选择；名称、植被、河流靠近预览，高级地形项展开后滚动到可见区域，创建按钮固定在底部。仍保留每次打开随机、隐藏种子字段和预览结果复用。
- 普通常驻及分类工具使用 64×64 方块，双排行距和列距为 10，分组间距为 24。底栏为滚动条预留空间，避免小窗口出现滚动条时挤低暂停与倍率。宽度不足时可滚动到最后的肥料组，实际点击能选择工具。
- 原生态开关勾选后悬停会回落到引擎默认主题，改前检查已复现。共享主题补齐 `hover_pressed`，生态扩散及自然天气的未勾选、勾选、悬停和焦点保持一致，开关仍修改实际世界规则。
- 造世手记使用两组真实行列布局。正常窗口与 1100×720、115% 界面下，全部快捷键均可见；保留滚动容器适配空间变化。
- 当前神力和沙漏采用同一张 64×104 卡，上方图案占 60 高、关闭区域占 44 高；笔刷和当前倍率各为独立 64×64 方块，关闭图形与窗口、观察浮层共用。实际检查关闭神力和恢复沙漏之前的神力。

共享主题去除厚阴影、按钮星芒与悬停位移，原插画、像素地图和叶片边饰保留。文字色加深：纸色底上的正文、次要文字和标题金色对比度分别约 7.12、5.04、4.93，主按钮文字约 5.59；这些数值只说明相关色对，不等同于完整无障碍认证。

原生截图使用 Godot Compatibility / RTX 3060 输出，逐张检查了建图、模板、高级项、主工具栏、自然工具栏、设置状态、手记、当前神力、沙漏展开及小窗口。`before11-*` 为改前画面，`design11-*` 为本轮画面；建图每次随机，因此建图对比用于观察布局，不能当成同一张地图的美术或性能对比。`design11-home.png`、`design11-nature.png` 与对应改前工具栏使用固定世界配置。截图是游戏实际绘制，没有后期拼接或从 WorldBox 提取素材。

### 本轮通过的检查

| 检查 | 结果 |
| --- | --- |
| 完整原生运行 | 175 项，创建、镜头、绘画、保存读取、植物生长枯萎及大世界运行 |
| 界面操作 | 222 项，分类、时间常驻、设置、全屏、界面大小、存读档和帮助 |
| 时间与笔刷 | 93 项，1x 实际 1.233 秒推进 1.217 世界秒；5x 实际 1.230 秒推进 6.076 世界秒 |
| 植被及地图预览/交融 | 24 / 35 项，预览复用、生成竞态、WASD、暂停、生态开关及二十年交融 |
| 主动施法反馈与天气 | 147 / 16 项，长按、暂停施法、肥料、降雨与 Esc |
| 极近景 | 8 项，32 倍滚轮、锚点、移动与笔刷 |
| 本轮设计专项 | 71 项，建图、高级选项、悬停、焦点、关闭、90%/100%/115% 与小窗口滚到末端实际选择工具 |

九套原生检查有覆盖重叠，不累计成独立功能数量。完整运行与界面专项分别见 `runtime-report.json`、`interface-report.json`；最终其余七套顺序运行的无错误日志为 `native11-final.log`。本轮未修改世界规则，九套无头规则检查沿用 0.10 的 590 项结果，没有把历史规则结果写成本轮重新执行。

所有输入验证使用独立的 `user://test-runs/` 目录，不覆盖玩家存档或设置。程序布局既核对原生尺寸，也逐张查看实际画面；这些检查不代替玩家对美术观感的判断。

### 性能观察的范围

完整运行的 5x 大地图样本 P95 为 16.488 ms、最大 203.388 ms，仍记录到一次长帧；本轮没有据此宣称已经解决所有高倍率卡顿，也没有通过反复取样删除这个结果。该检查从地图切换开始记录，并非纯稳定绘制基准。

随后顺序执行既有 `tests/idle_motion.gd -- ui11`，在 384×256、种子 781936、正常 HUD、无主动施法的 1x 场景观察 15.003 秒，共 1,663 帧；P95 14.305 ms，P99 19.286 ms，最大 33.038 ms，超过 33.333 ms 及 50 ms 均为 0。日志为 `idle11-release.log`，原始数据为 `idle-ui11.json`。这是有限正常运行样本，只用于核对本轮界面调整后未出现持续周期长帧，不作为新的整体性能提升结论。

### 检查中发现的问题

改前 `design11-before.log` 保存勾选悬停失败。`interface11-pass1.log` 报告小窗口下最右侧工具超过屏幕：新的间距使用横向滚动解决，几何检查改为识别可滚动容器，并加入实际滚到末端、完整可见及点击的验证；同时修正滚动条出现时的时间按钮行偏移。

`capture11-random-fixture.log` 记录旧检查假设随机地图必定同时有雪松与仙人掌的失败。完整渲染检查改为固定种子 781936，独立的新建世界检查继续验证每次随机；生产生成逻辑没有为测试改动。

组合回归曾发生一次分类点击未生效及后续时钟失败，单独输入诊断时 93 项通过；另一次出现笔刷落点比较失败。检查工具将瞬时点击、瞬时施法的按下/抬起放在同一派发周期，避免中途让出一帧；长按检查仍跨实际时间执行。该调整减少合成事件受桌面事件穿插的机会，不能单凭复查通过断言先前失败的唯一原因。未改游戏输入规则。早期原始日志为 `capture11-release.log`、`seasons11-input.log` 与 `interface11-release.log`。

### Windows 交付与文档

Windows 0.11（历史构建，当前目录未保留） 于 2026-09-06 19:27:19 本地时间导出，111,188,224 字节，文件/产品版本为 0.11.0.0。独立文件保留 0.10 及更早版本。实际发布程序运行默认入口，使用 OpenGL 初始化并运行 900 帧后退出，退出码 0、标准错误为空；日志为 `export11-release.log`、`export11-smoke.stdout.log`、`export11-smoke.stderr.log` 和 `export11-smoke.exit.txt`。

产品计划、使用说明、美术来源、参照说明及根项目运行指南已同步；定向编码与本地文档链接检查通过。`run.ps1 -Mode capture` 已纳入本轮原生设计专项。所有本轮启动的验证进程已退出，验证使用的临时世界不覆盖玩家存档。

## 0.10 验证

第十轮针对正常 1x 的周期卡顿、地表变化过快、山地与树群杂乱以及近景放大不足实施。日历参考官方 7 秒一个月及 12 个月的月份表，改为 84 秒一年；这个校正与性能问题分别处理。资料及丘陵/山峰的版本边界见[第十轮参照](./REFERENCE.md#第十轮山地尺度与时间参照)，实现取舍见 [ADR 0006](./adr/0006-calendar-cohorts-and-stable-boundaries.md)。

### 规则、原生操作与兼容

| 检查 | 结果 |
| --- | --- |
| 地图/存档、生态、物种/笔刷、年份/灾难、生态肥料、生成/扩散 | 20 / 30 / 151 / 53 / 40 / 49 项通过 |
| 主动肥料与天气 | 195 / 22 项通过，18 类生态、适生、占位、主动暂停施法和雨云时钟规则保留 |
| 日历、分步更新和休整期 | 30 项通过，旧 v7 六个生命阶段与阶段内进度、84 秒年界、中间八个处理位置保存后继续、跨帧分组一致性、三年边界变化、休整期保存与非法字段拒绝、旧档逐字节备份及最大年龄再次加载 |
| 完整原生运行 | 173 项通过；包含六个世界年生长、枯萎、保存/加载、连续绘画和 5x 大世界运行 |
| 界面、时间灾难、植被、地图交融 | 220 / 93 / 24 / 35 项通过 |
| 主动反馈、天气地表 | 147 / 16 项通过 |
| 极近景操作 | 8 项通过，32 倍上限、世界坐标锚点、实际滚轮/WASD、工具栏切换、笔刷及 Esc |

九套规则检查共 590 项，最终日志 `logic10-release.log`。八套原生检查存在重叠，不累计为独立覆盖数量：常规记录 `capture10-release.log`，最终专项为 `native10-final.log`、`landscape10-final.log`、`landform10-release.log`。后者解决了测试未释放滚轮事件导致工具栏点击失效的问题；没有为此改变游戏输入。规则检查也先复现了新增树间距阻挡母树繁殖的关联问题，将大型植被后代散落到四格外后，单株自然繁殖及间距检查通过，相关自然生态实机检查再次执行。

所有操作使用隔离测试存档和原生 Godot 输入、RTX 3060 绘制；没有在玩家存档上测试施法。完整运行最后样本：5x 大地图 2,735 株植物，P95 12.981 ms、最大 27.765 ms；世界计算最大 13.529 ms。另一小世界实际 5x 连续样本最大 13.250 ms。两者是有限样本，不代表所有地图和帧都达到固定帧率。

v8 将 v4–v7 的世界日历与植物年龄乘 84/60，保留所见年份、生命阶段及阶段内进度；v1–v3 沿用分段迁移到当前阈值。已活动的火、龙卷风和雨云保留剩余世界秒；只有下一次自然天气倒计时按年换算。生态整数时钟与小数进度不跳跃，边界三年休整期限随档保存。首次覆盖旧文件保留原字节备份，读取本身不修改原档。

### 正常 1x 卡顿的根因与分离实验

`tests/idle_motion.gd` 使用完整游戏、正常 HUD、无施法、384×256、种子 781936、丰度 0.9 的中景，按实际墙钟时间在 1x 运行 15 秒。120 fps 上限、关闭垂直同步；统计阈值为 33.333 ms。基准与其他 GPU/重计算检查顺序执行，不录屏编码。

| 条件 | 地表替换次数 | P99 | 最大帧 | 超过 33.333 ms |
| --- | ---: | ---: | ---: | ---: |
| 修改前 0.9 | 469 | 35.869 ms | 52.202 ms | 18 |
| 只关闭生态扩散 | 0 | 30.329 ms | 43.869 ms | 12 |
| 仅把植物更新分成八组 | 469 | 22.353 ms | 30.646 ms | 0 |
| 最终 0.10 | 30 | 16.172 ms | 21.463 ms | 0 |

原始文件为 `idle-before10.json`、`idle-nospread10.json`、`idle-cohorts10.json`。分项诊断 `probe10.log` 记录每秒植物处理约 7.7–12.3 ms、边界计算约 4.8–9.5 ms、原全图 HUD 统计约 3.45–6.95 ms，集中在同一帧造成峰值。仅分组的实验保留原有 469 次换土，说明改善并非全部来自减慢日历或减少植被。临时分项脚本已移除，保留日志作为诊断证据。

最终还将 HUD 统计改为生态位置集合，限制每帧地表纹理上传数量及时间，取消不使用的地表 mipmap；边界须有相邻支持、适合环境，并在改变后休整三年。不同地块的自然生命显示有不足一世界秒的相位差；自然更新不依赖镜头位置，实际世界变化和主动施法不会等待纹理发布。

最终记录 `idle-final10.json`：1,785 帧、现实 15.003098 秒、世界 15.000372 秒、P95 11.941 ms；没有超过 33.333 ms 或 50 ms 的帧。最终植物数 2,623，0.9 为 3,275；同一地形配置的新植被组合和演化速度已改变，因此最终整包比较不是完全相同的植物工作量，分组根因仍以同为 469 次换土的分离实验为依据。

### 连续移动及雨中施肥

普通镜头沿用 `tests/motion_benchmark.gd`：暂停/1 倍/5 倍固定更新负载 × 远中近景 × 平移/往返缩放，每组 150 帧，共 2,700 帧。每帧推进 `1/60 × 倍率` 世界秒、120 fps 上限、关闭垂直同步。这是固定步长压力样本，不等于实际墙钟倍率时间。

| 指标 | 0.9 | 最终 0.10 |
| --- | ---: | ---: |
| 最大帧 | 62.041 ms | 40.282 ms |
| 超过 33 ms | 93 / 2,700 | 1 / 2,700 |
| 超过 50 ms | 9 / 2,700 | 0 / 2,700 |
| 1 倍负载最大帧 | 46.770 ms | 30.811 ms |
| 5 倍负载最重组 P95 | 42.875 ms | 21.006 ms |

原始记录 `motion-release09.json`、`motion-final10.json`。剩余一次长帧出现在 5 倍负载；最终植物数 3,237 → 2,928，同一地形种子与测试步数，植被排列和自然演化规则有变化。暂停的全部组最大 11.865 ms。

更重的 `tests/weather_motion.gd` 使用完整游戏的实际 WASD、滚轮和长按施肥，世界按墙钟推进；1x/5x × 远中近景，每阶段 240 帧，共 1,440 帧，每阶段开始放三片云。12 项交互检查通过。新版本完成相同帧数所用现实时间更短，移动距离、施肥次数和世界推进量也随之不同，不作完全相同轨迹的速度比。

| 指标 | 0.9 | 0.10 首次最终样本 | 0.10 补查 |
| --- | ---: | ---: | ---: |
| 最大帧 | 72.796 ms | 99.296 ms | 54.923 ms |
| 超过 33 ms | 105 / 1,440 | 23 / 1,440 | 22 / 1,440 |
| 超过 50 ms | 30 / 1,440 | 2 / 1,440 | 3 / 1,440 |
| 1x 最大帧 | 45.657 ms | 35.993 ms | 38.091 ms |
| 5x 最重组 P95 | 52.696 ms | 31.817 ms | 31.391 ms |

分别保存为 `weather-motion-before10.json`、`weather-motion-first10.json`、`weather-motion-report.json`。首次出现一次约 99 ms 峰值，随后只给基准增加长帧状态记录并独立补查，生产代码没有改变；补查未再次出现同级峰值，不据此删除原结果或断言峰值原因已查明。长期停顿频次和 P95 改善，但 5x 加雨与密集主动施法仍会出现长帧，这是交付限制。日志为 `weather-motion10-final.log`、`weather-motion10-diagnostic.log`。

### 美术与观察尺度

逐图查看同一 384×256、种子 781936 新世界的 `140-landform-overview.png`、`141-landform-middle.png`、`142-landform-near.png` 和 `143-landform-ground.png`。连续低对比岩地、较大的不规则雪区取代重复三角山峰；同一生态以主导树种组成树群，保留少量其他适生种，草花缩小到陪衬尺度，叶冠减少零碎亮暗纹理。原有地图不重新生成或替换既有植物，新画法直接生效；后续自然补充使用新的组合与间距规则。

`144-landform-extreme.png` 展示整株白桦的极近景，`145-landform-extreme-brush.png` 展示同一尺度的笔刷。最大 zoom 从 7 提高到 32，仍为最近邻像素采样；真实输入检查了滚轮往返、暂停时 WASD 和 Esc 层级。山地沿用地形编号，丘陵仍允许适生植物和生态扩散，裸岩阻隔播撒生态及边界竞争。居民移动、建造、采矿未加入本轮，不能将参考研究当成已开放的文明功能。

新画面为原创程序绘图，没有从 WorldBox 截图提取资源。画面检查证明实际绘制、尺度和交互可用，不代替玩家对美术观感的最终判断。

20 世界年的双生态边界样地累计变化 125 次，实际地表纹理随之更新；局部概览与全图重新采样比较为 0 个不同字节。观察尺度截图由同一世界依次拍摄，标题栏与工具栏始终为原生游戏绘制。

### Windows 交付

`build/AeonVale-0.10.exe` 于 2026-09-06 18:25:49 本地时间导出，111,186,112 字节，文件/产品版本均为 0.10.0.0；使用独立版本文件，未覆盖 0.9。实际发布程序以 `--position -3000,-3000 --max-fps 60 --quit-after 900` 运行默认入口，OpenGL 正常初始化，退出码 0，标准错误日志为空。记录为 `export10-release.log`、`export10-smoke.stdout.log`、`export10-smoke.stderr.log` 和 `export10-smoke.exit.txt`。

产品计划、使用说明、参照研究、决策、美术来源和根项目指南已同步。最终定向编码检查通过，12 份应用说明/文档中的 67 个本地链接及标题锚点全部有效。源码规则及最后的相关专项日志无脚本错误；早期诊断失败记录保留，并在上文说明修正。所有本轮启动的检查进程已退出，玩家存档未被测试修改。

## 0.9 验证

第九轮 Q1–Q5 确认后，完成草花让位与大型植被催生、连续白色笔刷及撒落反馈、自然降雨和持续雨云、山脊岩壁与干湿地表、Esc 直接进入设置。组合测试发现的连续施肥重叠计算已修复。日期：2026-09-06。

| 检查 | 结果 |
| --- | --- |
| 地图/存档、生态、物种/笔刷、年份/灾难、生态肥料、生成/扩散 | 20 / 30 / 151 / 53 / 38 / 49 项通过 |
| 主动肥料 `tests/feedback.gd` | 195 项通过；18 类生态两种肥料均有实际产物，草花让位、灌木/树木/岩石保留、奇数位置、巨菇、预览逐格一致性、折线覆盖及间距 |
| 天气 `tests/weather.gd` | 22 项通过；立即与持续灭火、无生长加成、世界时间、开关、周期、到期、保存后确定性、旧档及非法字段 |
| 完整渲染/界面/时间灾难/植被/地图交融 | 173 / 220 / 93 / 24 / 35 项通过 |
| 主动反馈原生操作 | 147 项通过，最终合并路径实现后重跑 |
| 天气与地表原生操作 | 16 项通过，包含实际自然降雨、暂停/继续、肥料、Esc 和山体后台局部更新与全图像素一致性 |
| 雨中长按施肥与镜头 | 12 项通过，实际鼠标长按、WASD 与滚轮输入，1x/5x × 远中近景，共 1,440 帧 |
| 演示录屏 | 原生 1440×900、30 fps、435 帧、14.5 秒，H.264，2,121,889 字节 |
| Windows Release | `build/AeonVale-0.9.exe`，文件/产品版本 0.9.0.0，111,185,200 字节，2026-09-06 17:00:51 本地时间生成 |
| 发布程序默认入口 | 实际 OpenGL 启动，`--position -3000,-3000 --max-fps 60 --quit-after 900`，退出码 0，标准错误日志为空 |

八套规则检查合计 558 项，最终日志为 `test-output/logic-09-release.log`。七套原生检查存在重叠，不相加声称独立覆盖数量；完整日志 `capture-09-final.log`，最后的施肥路径合并后重跑 `feedback-runtime-09-final.log`、`weather-record-09.log` 与 `weather-motion-09-final.log`。检查使用隔离存档与真实 Godot 输入、RTX 3060 OpenGL 绘制，没有修改玩家存档。558 项中的部分物种检查按实际出现个体计数，不能只凭数量与旧版本比较覆盖程度。

### 连续移动与组合操作

普通镜头基准延用 `tests/motion_benchmark.gd`：384×256、种子 781936、丰度 0.9，暂停/1 倍/5 倍更新负载 × 远中近景 × 平移/往返缩放，每组 150 帧，共 2,700 帧。每渲染帧固定推进 `1/60 × 倍率` 世界秒，120 fps 上限、关闭垂直同步；是固定模拟工作量压力样本，不能视为日常实际倍率时间。每个 GPU 检查独立执行，录屏与编码不参与基准。

| 指标 | 本轮改前 0.8 | 0.9 普通镜头 | 0.9 加入雨云 |
| --- | ---: | ---: | ---: |
| 最大帧 | 57.351 ms | 62.041 ms | 67.951 ms |
| 超过 33 ms | 90 / 2,700 | 93 / 2,700 | 95 / 2,700 |
| 超过 50 ms | 7 / 2,700 | 9 / 2,700 | 7 / 2,700 |
| 暂停镜头最大帧 | 12.828 ms | 14.846 ms | 14.607 ms |

原始记录为 `motion-before09.json`、`motion-release09.json`、`motion-rain-release09.json`。雨云样本每组开始放置三片云，随后按正常寿命移动和消散。初始配置一致，因荒原植物选择变化，最终植物数 3,238 → 3,237。此次没有把普通镜头的最大帧报告为改善；5 倍负载仍有少量长帧，本机样本不能保证所有机器和地图都无卡顿。

更重的完整游戏组合测试发现：原连续路径逐格盖整个肥料刷，同时预览对每格扫描邻域，会反复计算重叠区域；快速镜头移动又会放大路径长度。修正为现实时间约 0.13 秒的施肥节奏、保留转折并合并路径、按距离重叠盖印、单次计算占位遮罩，以及预计算排序。首次按下立即响应，松手补完末段；预览和实际放置保留相同的适生与间距规则。

| 组合操作指标 | 合并处理前 | 最终 |
| --- | ---: | ---: |
| 最大帧 | 444.806 ms | 72.796 ms |
| 超过 33 ms | 620 / 1,440 | 105 / 1,440 |
| 超过 50 ms | 434 / 1,440 | 30 / 1,440 |
| 1x 三个距离最大帧 | 444.806 ms | 45.657 ms |

记录为 `weather-motion-before-fix09.json` 和 `weather-motion-before10.json`（0.10 开始前保留的 0.9 最终报告）。该测试使用真实时间的 WASD、施法及世界推进，每阶段固定渲染 240 帧，帧率变化会改变阶段现实时长、移动距离、施法次数和推进年份；所以它证明组合操作的大停顿明显减少，不能当成完全相同轨迹的逐帧速度比。最终 1x 三个距离没有超过 50 ms 的帧，余下 30 个超过 50 ms 的帧均在 5x 组合负载，最重的中景 P95 为 52.696 ms。

### 画面、录屏和兼容

逐图查看同一生成世界的 `120-ridges-overview`、`121-ridges-middle`、`122-ridges-close`、`123-ridges-ground`，以及 `125-rain-coast`、`132-natural-rain`。山体使用错落岩体、受光面、岩层与局部积雪，替换柔软噪声色环；检查了跨缓存块衔接和旧地图直接换绘法。沙岸与荒原保留暖沙色和细纹，沼泽使用湿润地色及局部水色。受控样地 `126`–`131` 展示连续白色笔刷、树木与植物肥料、巨菇及干湿植被组合；其笔直分区是测试布置，不是随机地图生成结果。

[演示视频](../test-output/AeonVale-0.9-rain-fertilizer.mp4)来自实际游戏连续帧：0–4 秒自然雨与雨神力，4–6.7 秒白色预览和树肥，6.7–8.8 秒植物肥料，8.8–10.9 秒巨菇，10.9–14.5 秒暂停施雨后继续移动。以固定 30 fps 运行导出帧，再编码为 MP4，抽查了落下、出现与受阻阶段；它是动画/操作证据，不是性能录像。自然云的实机验证把待降雨倒计时置零以触发正常生成分支，1–3 年间隔由规则检查覆盖，没有声称等待了完整自然年份。参照视频只用于体验观察，没有提取原作图像或音频。

v7 保存天气开关、倒计时、随机序列与活动雨云，读写保留浮点精度；v1–v6 继续读取，旧档从无活动雨云开始、不补算历史降雨。v6 原扩散开关、年份、植物、环境与持续灾难保留，首次覆盖旧文件先逐字节备份。巨菇纳入大型植被肥料用途，不改变物种 ID 或原生命周期。具体时钟取舍见 [ADR 0005](./adr/0005-weather-clock-save.md)。

Windows 0.9 使用独立文件名，保留 0.8；运行离线，不增加第三方依赖。源码、说明、领域术语、美术来源、计划、决策与根项目指南已同步。最终定向编码检查通过，13 份应用文档中的本地链接无缺失；规则、原生操作、录屏、镜头、导出与发布启动日志无脚本错误。本轮启动的检查程序均已退出。

## 0.8 验证

第八轮 Q1–Q5 确认后，完成镜头绘制复用、局部地表上传、分帧植物与林冠更新、近景自然动画、共用神力卡、主动施法、随机新建，以及树木和海岸重绘。浆果丛直接放置；肥料在适生空位催生对应植物，并催熟已有幼苗。新规则覆盖下方历史版本的“空地无效”和等待世界时间催熟记录。

| 检查 | 最终结果 |
| --- | --- |
| 地图/存档 `tests/run.gd` | 20 项通过 |
| 生态/旧档 `tests/ecology.gd` | 30 项通过 |
| 物种/笔刷 `tests/polish.gd` | 151 项通过 |
| 年份/种子/灾难 `tests/seasons.gd` | 53 项通过 |
| 肥料/生态 `tests/canopy.gd` | 38 项通过，断言按新规则更新 |
| 配置/扩散 `tests/landscape.gd` | 49 项通过 |
| 主动催生 `tests/feedback.gd` | 147 项通过；覆盖 18 类生态下两种肥料的对应类型、适生、疏密、占位、丘陵与保存结果 |
| 完整原生操作 `tests/runtime.gd` | 173 项通过 |
| 界面 `tests/interface.gd` | 219 项通过 |
| 时间/灾难/笔刷 `tests/seasons_runtime.gd` | 93 项通过 |
| 图像/施肥 `tests/canopy_runtime.gd` | 24 项通过 |
| 配置/交融/镜头 `tests/landscape_runtime.gd` | 35 项通过，最终专项复跑通过 |
| 主动反馈 `tests/feedback_runtime.gd` | 最终 174 项通过，含持笔施法时 WASD 移动、滚轮缩放、持续施法和释放输入 |
| Windows Release | `build/AeonVale-0.8.exe`，文件及产品版本 0.8.0.0，111,167,848 字节，2026-09-06 15:25:18 本地时间生成 |
| 发布程序默认入口 | 实际 OpenGL 启动，`--position -3000,-3000 --max-fps 60 --quit-after 900`，退出码 0；标准输出及错误日志无错误 |

七套逻辑检查合计 488 项，见 `test-output/logic-08-release.log`。六套原生运行检查有重叠，不累计为独立覆盖数量，完整日志为 `capture-08-final.log`。最后的局部生态优化、绘制层次和海面颜色调整后，再执行全部逻辑、镜头基准与相关的 `feedback-runtime-08-release.log`、`landscape-runtime-08-release.log` 专项检查。测试使用隔离存档目录及实际 Godot 输入和 GPU 绘制；导出程序另做默认入口启动，未在玩家存档上执行测试施法。

主动施法结果立即写入世界；肥料出现动画约 1.25 秒，暂停时也会结束。自然生长仍按世界时间推进，中远景不播放自然长大动画。原地持笔按固定节奏持续撒落，世界年龄继续推进；无效落点有短暂红色 ×。实际输入检查了浆果丛、空地树木催生、既有幼苗、暂停施法、神力卡、倍率收起和恢复、沙漏展开时的镜头操作，以及重新打开新建窗口产生不同图案且不存在种子字段。预览与创建一致性的旧测试改为内部配置比较。

世界时钟独立实测：约 1.245 秒现实时间推进 1.222 世界秒（1x），约 1.242 秒推进 6.098 世界秒（5x）。旧版 `canopy_runtime` 中约 4.41 秒是整段操作和等待时间，在 0.8 中不能解释为催熟所需时间；即时状态及暂停动画结束由新专项直接检查。

### 连续镜头性能

`tests/motion_benchmark.gd` 在同一台机器、384×256、种子 781936、初始植被丰度 0.9 的地图上运行。暂停、1 倍和 5 倍更新负载分别覆盖远中近景的平移和往返缩放，每组 150 帧，共 2,700 帧。引擎限制 120 帧且关闭垂直同步；每渲染帧固定推进 `1/60 × 倍率` 世界秒，以保持改前改后的模拟工作量一致。这是固定步长压力样本，不能把其中的倍率或帧率当成日常运行时的实际时间流速。

| 指标 | 修改前 | 0.8 最终 |
| --- | ---: | ---: |
| 最大帧时间 | 229.890 ms | 65.935 ms |
| 超过 33 ms 的帧 | 797 / 2,700 | 83 / 2,700 |
| 超过 50 ms 的帧 | 350 / 2,700 | 6 / 2,700 |
| 暂停、中景平移 P50 | 34.468 ms | 8.328 ms |
| 暂停、中景平移 P95 | 40.269 ms | 8.638 ms |
| 暂停、中景平移 P99 | 43.455 ms | 9.182 ms |

原始记录为 `motion-before.json`、`motion-release.json`。最终暂停样本最大 12.626 ms，1 倍更新负载最大 48.371 ms，剩余 6 个超过 50 ms 的帧出现在 5 倍更新负载。生态竞争拆分到每世界秒的一部分地块后，后续演化顺序与 0.7 有差异，结束时植物数分别为 3,251 和 3,238；初始配置和测试步数相同。各次基准没有并行执行其他自动 GPU 检查，但这是本机样本，不能保证所有机器或所有地图都无卡顿。

完整游戏另有实际 5x 大世界样本：3,069 株植物，P95 为 13.123 ms、最大 47.235 ms；连续 12 帧地形绘画约 0.075 秒。生成 288×192 地图的两次运行约 7.04 与 9.35 秒，显示真实进度。本轮改善的是连续操作与更新峰值，不宣称地图生成加速或所有长帧消失。

### 画面、兼容与交付

逐图检查了 `110-berry-cast`、`111-tree-cast`、`112-birch-grove`、`113-shared-hourglass`、`114-new-overview`、`115-new-middle`、`116-new-close`、`117-new-ground` 和 `118-coast-native`。114–118 来自同一 288×192、种子 68324 的生成世界；110–113 是适生规则与主动反馈样地。检查了共享卡片、树干分叉和叶冠、去除中景透明混合后的清晰度、林地纹理、沙岸水深层次、远景海面接缝及植物不遮住工具栏。视频观察支持功能与反馈方向，不等于逐帧复刻或用户已认可最终美术。

物种和生态编号、三种轮廓变化及生命阶段保持兼容，继续使用 v6 存档；旧档原有肥效不会被新施法规则抹去，原始地图与年龄不重生成。生态候选局部刷新仍维持排序，保存后在同版本继续推进可复现。分期竞争导致未来演化与 0.7 不完全相同，见 [ADR 0004](./adr/0004-biome-spread-world-rule.md)。新放置和催熟结果立即可保存，短反馈动画不作为待完成的世界事件存档。

发布文件保留独立的 0.8 名称；0.7 文件未覆盖。使用说明、产品计划、术语、参照、美术来源、决策记录及根项目指南随本轮同步。最终定向编码检查通过，12 份文档中的 56 个本地链接无缺失，最终规则、原生操作、镜头、导出和发布启动日志无脚本或运行错误。

## 0.7 验证

第七轮 Q1–Q7 确认后，完成地图模板与完整配置预览、海岸层次及生态省区、环境约束下的 18 类生态竞争、折叠倍率、按钮对齐、WASD 与 v6 存档。绘制更新使用后台快照，地表和缩略图同步发布；世界数据与施法判断仍立即生效。

| 检查 | 结果 |
| --- | --- |
| 地图/存档 `tests/run.gd` | 20 项通过 |
| 生态/旧档 `tests/ecology.gd` | 30 项通过 |
| 物种/笔刷 `tests/polish.gd` | 151 项通过 |
| 年份/种子/灾难 `tests/seasons.gd` | 53 项通过 |
| 肥料/18 类生态 `tests/canopy.gd` | 38 项通过 |
| 地图配置/扩散 `tests/landscape.gd` | 49 项通过，包含重复生成、参数变化、自然生态占比、初始适生植被、八模板、三年边界变化、阻隔、丘陵保留、保存后继续演化及 v5 字节备份 |
| 完整渲染/操作 `tests/runtime.gd` | 173 项通过，广阔地图覆盖五次扩散周期 |
| 界面 `tests/interface.gd` | 219 项通过；固定预览布局完成后再次通过 |
| 时间/灾难/笔刷 `tests/seasons_runtime.gd` | 93 项通过 |
| 画面/施肥 `tests/canopy_runtime.gd` | 24 项通过，1x 实际施肥至成熟约 4.41 秒 |
| 本轮实际操作 `tests/landscape_runtime.gd` | 35 项通过，报告无失败 |
| Windows Release | `build/AeonVale-0.7.exe`，版本 0.7.0.0，111,156,888 字节，2026-09-06 13:59:54 本地时间生成 |
| 发布程序默认入口 | `--headless --max-fps 60 --quit-after 360`，退出码 0，`package-smoke-07.log` 无错误 |

六套逻辑检查共 341 项。五套运行检查存在重叠，不累计为独立覆盖数量。最终规则日志为 `logic-07-final.log`；完整运行的前四套结果见 `capture-07-final.log`，布局与局部采样修正后的最终专项日志为 `interface-07-final.log`、`landscape-07-final.log`。对应五份 JSON 报告均无失败。所有输入和存档测试使用隔离目录，没有操作玩家存档；发布包做默认入口启动检查，源码使用实际 GPU 渲染与 Godot 输入事件。

预览验证既比较真实世界对象、地形、生态与植物，也比较预览像素和游戏远景合成结果。快速更换设置时旧任务不会覆盖新结果；预览尚未完成时创建会接管同配置任务；关闭预览、输入非法种子、改名后创建均已检查。高级设置滚动时顶部地图预览保持完整可见。

交互验证覆盖沙漏先只显示当前倍率、点击展开、选速后恢复神力与暂停、倍率打开期间滚轮及中键拖动可用且不施法；倍率、笔刷、当前神力和关闭入口尺寸一致，普通工具与常驻时间按钮同行对齐。WASD 在暂停和时间面板内可移动；设置、输入文字与 Ctrl+S 不误移动镜头。

交融测试使用受控的桦木林/温带林边界，推进二十个模拟年后发生 1,215 次地块转换，植物继续自然更替；这是加速模拟，不是现实等待二十分钟。变更后的局部缩略图与重新缩小全图逐字节一致，差异为 0；测试还覆盖绘制未完成时切换世界、再次返回以及原修改保留。新世界扩散开启，v1–v5 首次加载关闭，开关不停止植物生命过程；v6 保存环境场，保存后后续演化可复现。

实测 288×192 生成约 6.63–7.58 秒，期间真实进度和窗口持续更新；12 帧连续笔刷约 0.37 秒。384×256、3,085 株植物、5x 的长样本中，帧耗时第 95 百分位约 6.71 毫秒，最大约 144.50 毫秒。此前同步重画地表并缩小全图曾出现约 1,049 毫秒停顿，现由后台局部绘制消除该集中阻塞；个别生态处理及图像发布仍可能产生短帧波动，不代表所有硬件性能。

逐图检查了 `90-generation-preview`、`91-generation-options`、`92-landscape-overview`、`93-landscape-middle`、`94-landscape-close`、`95-landscape-surface`、`96-landscape-toolbar`、`97-speed-collapsed`、`98-speed-expanded`、`99-ecology-before` 和 `100-ecology-twenty-years`。远中近景及地表来自同一生成世界；最后两张是专门的生态交融样地。海岸、水深、林冠和山体已调整，保留像素阶梯与原创绘制；这不等于复刻原作资源或用户已认可最终美术。

定向中文编码检查、根文档链接（20 文件 / 37 本地链接）、应用文档链接（13 文件 / 52 本地链接）通过。产品计划、使用说明、领域术语、研究与决策、美术来源及根项目指南已同步。

## 0.6 验证

第六轮完成肥料快速显效、远景降噪、整像素植物重绘、海岸与地表调整、68 种植物、18 类生态及 v5 存档。以下记录为最终代码的验证结果，历史版本记录保留。

| 检查 | 结果 |
| --- | --- |
| 地图/存档 `tests/run.gd` | 20 项通过 |
| 生态/旧档 `tests/ecology.gd` | 30 项通过 |
| 物种/预览 `tests/polish.gd` | 151 项通过，包括 68 种活植物的三种不同占用轮廓及生态归属 |
| 日历/种子/灾难 `tests/seasons.gd` | 53 项通过 |
| 本轮规则 `tests/canopy.gd` | 38 项通过，包括 4 秒催熟、自然生长对照、成年衰老、两类肥料边界、v4 JSON 与精确字节备份、v5 存读、边缘笔刷、后部物种自然萌发 |
| 完整渲染/输入 `tests/runtime.gd` | 171 项通过 |
| 界面 `tests/interface.gd` | 216 项通过 |
| 时间/灾难/笔刷 `tests/seasons_runtime.gd` | 93 项通过 |
| 画面与施肥 `tests/canopy_runtime.gd` | 24 项通过，18 类种子经实际按钮点击与地图施法生效 |
| Windows Release | `build/AeonVale-0.6.exe`，文件版本 0.6.0.0，111,142,960 字节；2026-09-06 12:29:56 本地时间生成 |
| 发布程序默认入口 | `--headless --max-fps 60 --quit-after 360`，等待程序退出，退出码 0，`package-smoke-06.log` 无错误 |

五套逻辑检查共 292 项。四套运行检查有重叠路径，不累计为独立覆盖数量；最终日志为 `logic-06-final.log`、`capture-06-final.log`，均无脚本或运行错误。输入通过真实 Godot 事件分发，窗口放在屏幕外，使用隔离的 `user://test-runs/`；未在玩家正在运行的 0.5 EXE 内操作或更改其存档。

施肥测试先在暂停状态通过鼠标给 20 株白桦幼苗施肥，确认肥效存在但植物不成长，然后点击继续，在 1x 下真实等待并拍摄成长过程。约 4.41 秒的操作与等待后，20 株全部成熟；未施肥对照仍按 3–5 年自然节奏生长。成熟后残留肥效不加快衰老，空地、成年树与错误植物类型不会受该肥料影响。小地图放大后完整树形不再受远景图层稀释，施肥不再重新烘焙未改变的地表。

自然萌发原先将概率抽样值除以 1000 后用于选种，后部目录无法覆盖；现使用独立的完整取值范围，实测温带林可自然出现目录尾部的萤光菇与杜鹃。新增生态与物种采用追加编号；v4 的年份、年龄、阶段不重算，首次 v5 覆盖保留 `.v4.bak` 原字节，v1–v3 的旧迁移继续通过。修复了 JSON 浮点版本号校验与画笔移出地图产生负尺寸范围的问题。

逐图检查了最终版本的远景、近景、海洋深浅交界、湿沙、隐藏植被后的地表、三页植物图鉴，以及实际施肥前后。`70-canopy-overview`、`71-canopy-middle`、`72-canopy-close`、`73-canopy-ground`、`74-canopy-coast` 来自同一 288×192 可编辑生成世界。`75-eighteen-habitats` 和 `76-habitat-*` 为规则测试种植园，18 片土壤经种子施法后推进六个模拟年，不代表随机地图或现实等待六分钟。`77`–`80` 为真实等待的施肥场景；`81-catalog-page-*` 为三种轮廓的资源检查页。

性能样本：完整运行中 288×192 地图生成约 5.35 秒，专项复测约 4.85 秒；12 帧连续绘画约 0.63 秒。384×256 世界有 4,086 株植物，5x 第 95 百分位帧耗时约 6.99 毫秒，最大约 69.01 毫秒。精细地表的生成和连续绘画比 0.5 更耗时，生成有真实进度，个别重绘仍可能短暂波动；不能据此保证其他硬件表现。

定向编码、diff 空白、根文档链接（20 文件 / 37 本地链接）与应用文档链接（6 文件 / 28 本地链接）检查通过。根软链接检出状态未改变；本轮未修改 Harness 规则或宣称解决此前的五项软链接问题。计划、产品说明、领域术语、参考来源、美术来源与项目运行指南已同步。玩家的 0.5 进程保持运行，0.6 以独立文件交付；发布程序做默认入口无头检查，源码做 GPU 渲染与交互检查，最终美术偏好仍待用户实玩评价。

## 0.5 验证

第五轮 Q1–Q7 确认后完成生态种子、年份与生长、五种灾难、四组笔刷及左侧工具位。以下为本轮最终证据，0.4 及更早记录保留为历史。

| 检查 | 结果 |
| --- | --- |
| 地图/存档 `tests/run.gd` | 20 项通过 |
| 生命周期/旧档 `tests/ecology.gd` | 30 项通过，按新成熟及寿命目标更新观察时间 |
| 物种/预览 `tests/polish.gd` | 119 项通过，52 种实际植物各有三种轮廓，均可由自然更新产生 |
| 本轮规则 `tests/seasons.gd` | 49 项通过，包括 14 生态、肥料、四种范围、灾难、v3 年份/生命阶段迁移及备份 |
| 完整实际渲染/输入 `tests/runtime.gd` | 最终 163 项通过，`runtime-report.json` 无失败 |
| 界面专项 `tests/interface.gd` | 最终 204 项通过，`interface-report.json` 无失败 |
| 本轮玩法专项 `tests/seasons_runtime.gd` | 最终 93 项通过，`seasons-runtime-report.json` 无失败 |
| Windows Release | `build/AeonVale-0.5.exe`，文件版本 0.5.0.0，111,136,304 字节，2026-09-06 11:26:33 本地时间生成 |
| 发布程序默认入口 | `--headless --max-fps 60 --quit-after 300` 正常退出，`package-smoke-05.log` 无错误 |

四套无头检查共 218 项；三套运行检查覆盖相同路径，不汇总成互不重复的测试数量。逐棵树种页面取消后，完整运行中原有分页/筛选断言改成 14 类生态种子与无个体树种入口；自然幼苗作为施肥对象，完整生命周期仍检查衰老、死亡和自然补充。

实际输入覆盖左侧倍率在上/沙漏与 × 在下、恢复原神力、笔刷按钮在神力卡上方、四组共 25 档图案、Alt＋滚轮不改变镜头缩放、四种预览与真实受影响地块逐格一致、暂停施法及持续灾难停走、火焰存读、雨灭火、龙卷风移动、地震形成地裂，以及年份在 60 秒处进入下一年。相同时长下分别测得 1x 约 1.23 世界秒、5x 约 6.13 世界秒；这验证实时推进比率，未把快速推进六年生态的测试描述成现实等待六分钟。

修正了将上一帧预览遮挡状态用于阻止当前输入导致的快速点击丢失，以及打开设置时沙漏/神力的锁定恢复边界。桌面窗口检查曾出现间歇性的按钮输入失败；最终补全移动、按下、释放的输入顺序，并把 GPU 测试窗口放在桌面可视区域外后通过。`run.ps1 -Mode capture` 使用该独立窗口方式，依次运行三套检查，截图仍来自真实 Godot 渲染，不是浏览器复刻或静态 UI 拼图。

逐图查看了左侧沙漏、四组笔刷、三类工具栏、无边框范围、14 生态和新增植物，以及火焰、雨、龙卷风、地震与闪电的实际画面。`54-fourteen-ecologies` 和 `55-new-vegetation-close` 是用于检查的规则种植园：14 个方形土壤区域经实际种子输入后自然生长，用于对比生态，不代表随机地图生成的地貌。其他远中近景继续使用可编辑生成地图。新增植物为青竹、龟背竹、芭蕉和伞冠菇；新增图标、地裂与天象动画均为原创绘制代码。

最终完整样本：生成约 2.53 秒，12 帧连续绘画约 0.244 秒；384×256 世界有 3,981 株活植物，5x 第 95 百分位帧耗时约 12.12 毫秒、最大约 59.81 毫秒。最大帧仍可能有短暂波动，该样本不代表其他硬件的性能保证。

所有测试只读写 `user://test-runs/`。检测到玩家旧版 `build/AeonVale.exe` 正在运行，因此发布到独立的 `AeonVale-0.5.exe`，没有关闭该进程或改写其运行文件；新版可读取同一玩家目录内的旧档，首次保存前留备份。发布程序完成了默认入口无头启动检查，源码完成 GPU 渲染和输入验证，尚未在玩家当前 EXE 会话中做人工桌面验收。

定向编码、根 AGENTS 上下文、根及应用本地文档链接、diff 空白检查通过。默认 Bash 命令命中了不可用的 WSL，改用本机 Git Bash 执行检查；Harness 仍报告此前已有的五项软链接检出问题：根 `AGENTS.md` 及 `.claude/.codex/.codebase/.trae` 下的 `skills`，本轮未改动这些文件。完整计划与存档迁移理由已同步到 [PLAN](./PLAN.md#第五轮反馈与决策树) 和 [ADR 0003](./adr/0003-calendar-life-migration.md)。

## 0.4 验证

用户确认第四轮 Q1–Q5 后完成工具栏结构与森系幻想绘本界面。以下为本轮执行证据，历史检查保留在后文。

| 检查 | 结果 |
| --- | --- |
| 地图/存档、生态/迁移、物种/神力规则 | 20 + 30 + 111 项全部通过 |
| 完整实际渲染与输入 | 231 项通过，`test-output/runtime-report.json` 无失败 |
| 最终界面专项 | 199 项通过，`test-output/interface-report.json` 无失败 |
| Windows Release 导出 | `build/AeonVale.exe`，文件版本 0.4.0.0，111,117,104 字节 |
| 发布程序默认入口启动 | `--headless --max-fps 60 --quit-after 240` 正常退出，`package-smoke-04.log` 无错误 |
| 文档检查 | 应用 10 份 Markdown 的 31 个本地链接、定向编码、diff 空白检查通过；根 AGENTS 上下文与根文档链接检查通过 |

界面专项经真实 Godot 输入分发点击按钮，覆盖新建世界进入主工具栏、两个分类页签、返回保留当前神力、暂停与沙漏跨工具栏固定位置、神力面板内笔刷改变真实半径、选择 3x 保留暂停与神力、× 取消神力、观察距离与植物显隐、全屏/窗口切换、90%–115% 范围内的界面设置、设置保存，以及界面变大时保持当前观察位置。最终复核通过实际卡片点击保存/加载，已有存档进入覆盖确认，取消不会覆盖。

完整运行回归继续覆盖生成响应、连续地形笔刷、暂停/五档速度、播种/施肥、生长与枯萎、物种分页/筛选、远中近景、旧存档兼容与无效落点预览。原 Ctrl+Z/Y 行为检查改为确认已绘画地形不被这些快捷键改变；数据层历史接口的旧检查保留，游戏已无撤销/重做用户入口。检查数量包含实际可见控件边界，随工具栏重组变化，不与 0.3 数量直接比较覆盖深度；两份运行报告存在共同路径，不合并成互不重复的测试总数。

本轮完整样本：地图生成至揭示约 1.98 秒，12 帧连续绘画约 0.20 秒；384×256 地图含 3,904 株活植物，5 倍速第 95 百分位帧耗时约 5.20 毫秒、最大约 34.61 毫秒。这是本机样本，不保证其他硬件表现，也不把测试快速推进生态解释为玩家等待时间。

已逐页查看实际渲染截图，包括：`31-storybook-title`、`32-storybook-new-world`、`33-storybook-loading`、`34-storybook-home`、两类 `35-storybook-category-*`、`36-storybook-hourglass`、`37-storybook-observation`、`38-storybook-settings`、小窗口与 115% 界面的 `40/41/45-*`、`42-storybook-save`、`43-storybook-load`、`44-storybook-help` 和 `46-storybook-confirm`。专项截图等待短入场动画完成；生成截图仍取得加载中的实际进度。修正了初次布局在页签后方留下空带的问题，地图现延伸到工具栏上沿；页签及浮层区域会阻止预览和穿透施法。

`run.ps1 -Mode capture` 顺序运行完整与界面专项；单独界面复核使用引擎参数 `--path apps/aeon-vale --script tests/interface.gd`。所有测试存档及设置位于独立 `user://test-runs/`，未覆盖玩家数据。最后的存档卡片、枯树图标及界面标识调整后，进一步把保存反馈收为居中小提示、让观察浮层避开分类页签；对应检查通过后重新导出，并重新执行发布程序启动检查。

发布程序冒烟证明打包资源与入口可运行，源码窗口中的输入与渲染证明界面行为；本轮未再次通过 Computer Use 接管桌面，未将二者描述为发布 EXE 的桌面交互验收。用户最终画风满意度仍待实玩反馈。根 Harness 的历史软链接检出问题未改动，本轮未重复执行 Harness；Node 版本警告不影响 Godot 构建。

## 0.3 验证

用户确认第三轮 Q1–Q4 后实施。以下为本轮实际执行记录，保留后面的 0.2/0.1 历史证据。

| 检查 | 结果 |
| --- | --- |
| 地图、存档、植物生态与早期迁移 | 原有 20 + 30 项检查通过 |
| 作用预览、植物目录与版本 2 迁移 | 111 项检查通过；首次建立规则检查时按预期失败，实现后通过 |
| 实际 Godot 渲染与输入运行 | 329 项检查通过，报告 `test-output/runtime-report.json` 无失败 |
| Windows Release 导出 | 0.3 成功导出，运行资源嵌入 `build/AeonVale.exe` |
| 导出程序启动冒烟检查 | 使用发布程序默认入口、无头运行 240 帧后正常退出，日志 `package-smoke-03.log` 无错误 |
| 文档与协作入口 | 定向编码、应用链接、根文档链接与 AGENTS 上下文检查通过；根 Harness 仍为既有五项软链接检出问题 |

新验证包括：三档地图规模的俯瞰均采用概览表达，距离标签与固定工具栏一致；当前神力图标和 × 取消；浏览分类与调整时间保留神力；沙漏提供 1x–5x 五个档位，关闭不变速、暂停中选档不恢复；鼠标进入面板隐藏地图预览；预览判断与实际单格修改一致；全部物种可分页选取，生态筛选结果匹配物种目录。

四十八种植物逐种验证三个变体的非透明轮廓不同，二十四种树木不包含岩石和仙人掌；所有物种均可通过对应生态的自然生成规则产生。一个新增物种曾因两个取模选择相互约束而永远无法自然出现，已改用不同的采样位并验证全部物种可达。版本 3 保存新增编号，版本 1/2 原编号继续兼容；读取不改原文件，首次覆盖前保留对应 `.v1.bak` 或 `.v2.bak`。

运行时输入使用视口本地坐标送入真实 Godot 输入分发，经过控件与地图事件处理，不直接调用按钮回调代替点击。生态筛选另检查选择回调和重建后的控件状态。早期缩小窗口的检查暴露了测试输入坐标换算问题；改用明确的视口坐标后通过，未将它误报为游戏地形编辑缺陷。该输入接口语义见 [Godot Viewport.push_input](https://docs.godotengine.org/en/stable/classes/class_viewport.html#class-viewport-method-push-input)。

地表从每格 4×4 提升为 12×12 美术像素，逻辑坐标保持不变。第一版细节绘制与全图重采样令建图约 12.23 秒、12 帧绘画约 3.25 秒；复用相同外观纹理、只更新受影响的概览区域后，最终完整报告为建图约 2.42 秒、期间渲染 280 帧，12 帧连续绘画约 0.28 秒。384×256 地图有 3,904 株活植物，5 倍速样本第 95 百分位帧耗时 12.124 毫秒、最大 41.415 毫秒。数值包含本机运行条件和过渡，不保证其他硬件相同。

新增截图包括 `20-hourglass`、`21-power-preview`、四页 `23-plants-page-*`、`23-arid-plants`、三种规模的 `24-overview-*`、`25-ground-only`、`26-detailed-forest` 和 `27-plant-catalog`。最后的枝叶、草花和针叶树造型微调后，111 项定向植物检查再次通过，补充渲染检查 182 项通过，报告为 `test-output/polish-report.json`。运行脚本支持附加用户参数 `--polish-only` 重建本轮画面；完整报告与补充报告分开保留，不把重叠检查相加宣传。

这些画面是游戏实际渲染，已检查山脊雪线、海岸、地表单独显示、植物轮廓、选中面板及预览。最终是否达到用户期望的 WorldBox 贴近程度仍由用户实玩判断。无头发布程序冒烟只证明打包资源与入口可运行，不等同于导出 EXE 的桌面交互或美术验收；本轮没有再次接管用户桌面。测试使用独立存档目录，没有覆盖玩家存档。

## 0.2 验证与本轮边界

本节保留 0.2 已执行的验证，不代表本轮重跑。用户随后提出的远景缩略、神力面板、落点预览、1x–5x 选择和更精致美术属于上面的 0.3 记录，不倒算为 0.2 已通过。

| 检查 | 结果 |
| --- | --- |
| 地图与存档逻辑 | 20 项通过 |
| 植物生态与存档迁移逻辑 | 30 项通过 |
| 源码在实际 Godot 渲染窗口中的输入和运行检查 | 148 项通过，报告 `test-output/runtime-report.json` 的 `failures` 为空 |
| Windows x64 Release 导出 | 已导出 0.2，资源嵌入 `build/AeonVale.exe`，111,423,200 字节 |
| 导出程序的桌面交互复核 | 已启动并识别到游戏窗口；用户按物理 Esc 停止 Computer Use，未完成建图和缩放的桌面交互复核 |

新增逻辑验证覆盖：地形与生态独立；适生空地在 120 个世界秒内出现混合生长阶段；长期自然更新、死亡与密度上限；改地时适生个体保留、不适生树转枯、草本消失和水体清除；枯树消解；两类肥料仅加速对应已有幼年植物；完整状态撤销重做；带小数时间的存档继续模拟；旧存档内存转换、岩石归类和首次覆盖前原文件备份；实际 JSON 磁盘往返和损坏数据拒绝。

运行验证增加了三个观察尺度、固定工具栏、小窗口内实际分类点击与图标文字边界、同一空地在 12/45/120 个世界秒的渲染、转枯动画与枯树消解、实际播种和施肥输入、连续 5 倍速与镜头无关的生命过程。测试写入独立的 `user://test-runs/runtime-*` 和 `user://test-runs/ecology-*`，不覆盖玩家的三个存档位置。

0.2 最后一次源码运行报告记录：默认地图生成到揭示约 1.144 秒，期间渲染 68 帧；12 帧连续笔刷输入约 0.115 秒；384×256 大地图有 3,913 棵/株活植物，5 倍速样本帧耗时第 95 百分位为 12.198 毫秒、最大 36.985 毫秒。地图生成耗时包含完成态停留与过渡；加速执行的 120 步生态场景用于检验世界时间，不把约 0.878 秒的测试耗时解释为正常玩家生长速度。这些是本机样本，不是跨硬件性能保证。

本阶段修正了 JSON 数字解析后与整数列表的严格成员比较导致有效存档被拒绝的问题；另修正了玩家在非自然萌发候选位置播种后，个体未加入生态缓存的问题。生态候选位置和植物寿命缓存降低更新开销，植物统一图集减少分散纹理绘制开销。对应行为有回归检查。

截图保存在应用 `test-output/`，包括远中近景、七类生态的自然生长、转枯前后、肥料效果和大地图。该目录与构建文件均为忽略的本地产物，可通过根项目指南的命令重建。自动检查证明状态、输入和布局，用户已明确反馈远景及地表、植物精致度仍不足，不能据此声称视觉已验收。

先前通过 AGENTS 上下文和文档链接检查。根 Harness 的既有符号链接检出问题与 Node 版本警告见下面历史记录；没有修改相关路径或检出配置。

## 0.1 历史验证

以下保留首版记录，其 83 项检查和性能样本不代替上面的 0.2 记录。

### 首版已执行

| 检查 | 结果 |
| --- | --- |
| Godot 无头编辑器导入 | 通过，无脚本解析错误 |
| 地图与存档行为 | 20 项通过 |
| 实际 Godot 渲染与输入事件 | 83 项通过 |
| Windows x64 Release 导出 | 通过，资源嵌入 `build/AeonVale.exe` |
| Windows 交付文件启动 | 已启动并识别到“纪元谷 · Aeon Vale”窗口 |
| 文档编码、diff 空白检查 | 通过 |
| AGENTS 上下文与文档链接 | 通过 |
| 根 Harness | 未通过：既有五个兼容符号链接在本地检出为普通文件 |

地图检查覆盖固定种子重现、种子/模板差异、尺寸、海岸与河流、植被兼容性、边缘笔刷、整笔撤销重做、海岸邻接重绘、存档往返、损坏/过大/未知版本拒绝、环礁中心水体及关闭河流。

运行时检查在真实 Godot 渲染窗口中注入鼠标和键盘事件，覆盖启动准备、八种模板选择、加载期间持续渲染、全图与近景、细长植被的非零尺寸、锚点缩放、连续绘画、Ctrl+Z/Y/S、暂停环境动画、倍速、实际磁盘写入与覆盖、读档失败保留当前地图、取消新建以及窗口内控件边界。

测试把存档写入独立的 `user://test-runs/runtime-*`，没有覆盖玩家的存档。运行时截图为 1440×900，以及把窗口缩到 1100×720 后得到的缩放画面。截图和机器报告在应用 `test-output/`，可按根项目指南的 `capture` 命令重建；该目录属于忽略的本地产物。

Harness 失败项为根 `AGENTS.md` 与 `.claude/skills`、`.codex/skills`、`.codebase/skills`、`.trae/skills`。Git 索引记录它们为 `120000` 符号链接，本地却是普通文件；这些路径没有本轮改动，未擅自改变仓库检出配置。此外当前 Node 26.4.0 超出根仓库要求的 22.x，文档检查有环境警告；Godot 工程运行不依赖 Node。

### 首版实测数据

- 默认 288×192 地图从生成到揭示约 1.07 秒，期间继续渲染 35 帧；这包括完成态停留与淡出，不是纯算法耗时。
- 12 帧连续笔刷输入约 0.19 秒；只更新受影响的植被行组后，同一段测试从约 0.34 秒降至该数值。
- 上述数值是本机单次验收样本，不作为跨硬件性能保证。

### 首版已修正的实际问题

- 简单坐标哈希导致植被斜向重复与形态分配偏斜，已改为混合充分的离散采样。
- 细长植被的宽高比整数除法产生零宽绘制，已修复并加入实际渲染边界回归。
- 笔刷需要在本次数据写入完成后重绘邻接海岸；现在编辑后的图像与从存档数据重新绘制一致。
- 损坏 JSON 与 Base64 通过显式错误分支处理，保留当前世界，不在正常错误处理过程中产生引擎报错。
- 运行时测试按实际容器布局完成后的几何位置点击，并区分逻辑画布和缩放后窗口尺寸。

### 首版边界

用户最终视觉验收尚未完成。Computer Use 启动 Windows 交付文件后，用户按物理 Esc 停止电脑控制；因此只确认交付程序成功启动，未完成该可执行文件的桌面交互复核。83 项运行时检查来自源码在实际 Godot 渲染窗口中的受控输入事件，不冒充用户当前桌面会话验收。

本版使用原创插画、素材和工具图标，并没有证明与 WorldBox 的每一帧或每个图标一致。文明、战争、气候生态演化和音频不属于这次已确认首版交付。Windows 以外的平台未导出验收。
