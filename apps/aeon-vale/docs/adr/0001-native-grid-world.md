# 使用独立的 Godot 地图数据与渲染

纪元谷采用用户指定的原生 Godot 引擎，重新建立可编辑方格地图，不恢复仓库中已删除的 Web 版本。地形和植被编号独立于图像资源保存：同一份数据驱动生成、笔刷、撤销和版本化存档，避免“画面改了但世界没有变化”，也让后续更换美术不必迁移玩家地图。

地图生成与纹理烘焙在后台线程完成，场景与图形资源由主线程接管；这一取舍增加了进度协调，但保证生成过程中加载动画能够响应。首版不把文明、房屋和战争加入地图数据，因此它们没有在界面上伪装成已完成能力。
