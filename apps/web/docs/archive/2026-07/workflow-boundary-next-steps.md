# 工作流不开放节点后续步骤（已归档）

> [!ARCHIVED]
> 2026-07-22 归档。该备忘创建时 `condition` 节点尚未开放；当前工作流已支持其配置、图校验与运行时测试，因此不再作为现行待办。

## 原始记录

1. 在工作流执行入口（服务端或统一运行时）加硬性校验，禁止 `code` / `condition` / `loop` 节点在执行路径中运行。
2. 增加执行前静态校验报错的统一文案并回传前端，确保历史工作流不报 500。
3. 补一条手工验收：打开含有不开放节点的 workflow 提交运行，前端与后端均能给出“当前未开放”提示。

## 后续边界

`code` 和 `loop` 是否继续保持未开放，应在需要开放相应节点时，以当前工作流运行时与产品范围重新确认；不沿用本备忘的 `condition` 限制。
