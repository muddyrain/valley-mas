# 从原始配套包按范围读取预览文件

状态：已接受，待实施。

文章配套包只在私有对象存储中保留一份原始 ZIP，上传校验时记录受信任的文件清单、压缩方式与字节位置。访客选择可预览文件后，服务端按清单从 TOS 读取对应字节范围，并在明确的单文件上限内解压和返回；不为包内文件逐个创建对象，也不在每次预览时重新下载整个 ZIP。该方案增加了范围读取和受限解压逻辑，但避免最多 2048 个派生对象的创建、授权与清理成本。
