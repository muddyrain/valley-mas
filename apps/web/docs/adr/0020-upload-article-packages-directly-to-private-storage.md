# 文章配套包直传私有对象存储

状态：已接受，待实施。

浏览器使用服务端签发的短时凭证，把最大 64MB 的 ZIP 直接上传到归属站主的 TOS 临时私有对象；服务端随后读取该对象、校验归档、生成文件清单并确认配套包，不转发上传主体。这个协议需要为指定 Web 来源配置受限的 TOS CORS，并要求临时对象具有到期回收机制，但能避开当前 API 60 秒超时、应用服务器双倍内存占用和重复带宽成本。未经服务端确认的对象不能绑定文章或进入公共预览与下载。
