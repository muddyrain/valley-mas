# 保留旧专栏关系并分离首页策展

日常发布只暴露一个可选的“专栏”，并原样沿用现有 `PostGroup` 及文章绑定，不将旧数据迁移到 Category 或 Tag。Category 暂作兼容字段并从创作体验中隐藏；Tag 保留为 AI 可建议的可选检索元数据，不新增“系列”概念。专题精选将作为独立策展对象引用已有文章和主题合集，不修改内容原有的专栏或标签。这一取舍优先保护旧文章绑定并减少站主维护字段，而不追求理论上完整的多层分类。
