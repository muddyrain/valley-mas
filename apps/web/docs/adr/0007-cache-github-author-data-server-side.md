# 在服务端缓存 GitHub 作者资料

关于页需要展示 `muddyrain` 的公开头像、简介、代表仓库和贡献记录。公开资料与仓库可以通过 GitHub REST API 获取，贡献记录需要使用经过认证的 GraphQL API，因此公共站不在浏览器中直接访问多组 GitHub 接口，也不向浏览器暴露 Token。服务端提供统一的只读聚合数据并设置缓存，代表仓库由站主显式配置；各数据块允许部分成功，GitHub 请求失败时优先返回最近一次成功数据，贡献记录不可用时只隐藏该区域。这样可以保留相对新鲜的作者信息，同时避免访客流量直接消耗匿名限额或让第三方故障破坏关于页。
