# Life Trace 产品计划

最近整理：2026-06-26

本文件是 Life Trace 的唯一产品计划入口，用于维护当前状态、近期优先级、路线图、模块边界和验收规则。历史执行明细不再在正文里堆叠展开；已经完成的内容沉淀为能力状态，未完成内容集中放在“当前待办”和“后续能力池”中。

## 维护规则

- 只在本文件维护 Life Trace 当前计划，不新增并行的 MVP、Roadmap、Task 或 Pantry 计划文档。
- 顶部“当前状态”和“当前待办”必须保持可信。已完成内容放入“已完成能力”，不要继续伪装成待办。
- 计划变化、页面新增、接口新增、数据模型新增、依赖策略调整、验收标准变化时，先更新本文件，再进入实现。
- 只做临时调试、格式化、拼写或不改变产品方向的局部修复时，可以不更新本文件，但收尾必须说明原因。
- Life Trace 业务数据、接口、表名和前端生命周期必须继续保持 `life_trace_` 命名空间，不复用 Web 内容站的业务模型。

## 状态标记

- `已完成`：代码已落地，基础验证通过，计划中不再作为待办跟踪。
- `已完成首轮`：核心链路可用，但仍允许后续体验增强。
- `待验收`：代码已落地，但还需要真机、真实数据或线上环境验证。
- `后置`：不属于当前冲刺，保留方向，后续单独立项。
- `未开始`：只有计划描述，尚无明确代码入口。

## 产品定位

Life Trace 是一个 AI 主导的个人生活计划、踪迹记录和家庭库存管理 PWA。它把天气、提醒、日程、图片记录、家庭库存和 AI 建议组合成一个生活闭环：用户先计划生活，再完成计划，并沉淀为可回看的生活踪迹。

核心闭环：

```text
今日天气 -> 创建生活计划 -> 完成计划 -> 自动生成生活踪迹 -> 在踪迹流中回看
```

家庭库存闭环：

```text
拍照/手动记录商品 -> AI 识别和补全信息 -> 用户编辑确认 -> 入库 -> 到期提醒/使用记录 -> 生成生活踪迹
```

## 当前状态

当前 Life Trace 已从静态 MVP 进入“登录门禁 + 云端生活数据 + 服务端 AI + 家庭库存 + AI 拍照入库 + 个人生活扩展页”的早期产品化阶段。

### 已稳定接入

- 登录门禁、云端计划、云端踪迹、偏好设置。
- 今日天气、未来小时天气、计划提醒、本地系统通知、服务端 Web Push 扫描。
- 计划日历互操作：计划可生成标准 `.ics` 日历事件，交给 iOS/Android 原生日历或已安装日历 App 添加。
- 移动端路由栈整理：底部 Tab 只在主 Tab 根页显示，计划详情、踪迹详情等子页面隐藏底部 Tab。
- 移动端图片预览公共组件：Pantry、踪迹、计划、上传预览和反馈截图等图片可点击放大查看。
- Today 首页加载体验：天气、最近成就、库存和计划等异步卡片已统一使用稳定骨架屏。
- 用户端主 Tab 视觉首轮改造：Today、计划、踪迹、Life AI、我的已对齐浅色柔和日记风，统一纸感背景、绿色中央 AI Tab、生活摘要卡片和移动端卡片层级。
- 服务端今日建议 AI、每周回顾 AI、图片生活分析 AI、智能体对话。
- AI 拍照分析商品、条码识别、保质期线索识别、多商品识别、识别历史和云端草稿。
- Pantry 库存列表、详情、创建、编辑、状态更新、删除、筛选、排序、分页、家庭空间切换、提醒和操作时间线。
- 家庭空间模型、家庭 API、个人空间回填、共享家庭加入/退出/转移/解散。
- 库存优先智能菜谱：从 AI 页进入独立菜谱管理页，基于 Pantry 食品库存生成临期优先菜谱；生成结果会进入历史菜谱，支持独立菜谱详情页继续查看，并可加入吃饭计划；视频生成入口不再返回占位视频，等待 HyperFrames 渲染链路接入。
- 历史周报保存、展开、删除，以及从“下周行动”生成计划。
- 生活成就系统：成就池扩展到 72 枚，AI 纪念语、PNG 分享卡和家庭共同成就首轮已接入。
- 地点库 v2：自动沉淀、手动创建、想去/去过状态、城市/区县/地址/经纬度、列表/详情、收藏、改名、归档、JSON 导出。
- 书影音日记 v1：独立二级页、五类记录、AI 辅助补全、评分/短评/摘录/封面、自动生成书影音踪迹。
- 捕捉 Inbox v1.1：文本、链接、单图、手动 AI 整理、手动转计划/踪迹；当前已降级为次级能力，不再作为 Today 和 Profile 的核心入口。
- 轻账本 v1：手动记账、月度摘要、Inbox/Pantry 草稿入口、AI chat 记账。
- 衣橱 / 穿搭 v1.4：个人衣橱、共享衣物池、衣物拍照识别草稿、结合天气和今日计划的穿搭建议、建议卡保存/今天就穿、穿搭保存、穿着后生成踪迹、衣物详情穿过次数和最近穿着日期、闲置中筛选、久未穿标记、整理提醒、洗护方式、建议洗护周期、待洗护筛选、刚洗过动作，以及衣物穿搭偏好分档和推荐加权。
- Life Trace AI 架构基座 v1：服务端 AI 配置、文本 JSON、视觉 JSON、prompt contract 和 Assistant action registry 已开始统一；图片生活分析、Inbox 整理和书影音补全已迁入提示词契约层，前端 Assistant SSE 调用已抽出 feature hook，后续新增 AI 能力优先接入该基座。
- 问题反馈入口、多图提交，以及 Admin 后台查看反馈、图片、反馈人、反馈时间和标记解决。
- Admin 后台 Life Trace 管理分组：只读查询总览、用户概览、计划、踪迹、库存、周报、AI 对话和推送记录。
- 服务端一次性 schema sync 命令：用于历史迁移遗漏字段时补齐当前 GORM model 对应的缺失表和字段。
- PWA manifest、应用图标、基础离线缓存、SPA rewrite、版本更新提示和系统分享入口。

### 当前待办

1. `已交付` P0 App 式访问体验治理
   用户端已按移动 App 访问体验治理导航与加载：底部 Tab 保留各自滚动现场，子页面返回优先走浏览历史栈，列表页返回时先恢复旧数据与滚动位置再后台刷新；计划、Pantry、踪迹、灵感、轻账本、采购清单、订阅、地点、书影音、衣橱和成就等页面已补齐稳定骨架屏、非挤压刷新态和滚动锚点，减少数据加载后的页面跳动。

2. `待验收` P0.1 统一 App 布局系统 / 页面间距治理
   用户端正在把主 Tab、子页面、列表、筛选条和卡片密度收敛到统一布局原语：主 Tab 使用一致页面内边距和顶部距离，子页面默认拥有统一内容节奏，普通卡片默认舒适紧凑，横向筛选条和列表间距复用统一组件；AI 聊天、拍照识别和菜谱播放器保留沉浸结构，但统一安全区、按钮和状态卡密度。验收重点是 Today、计划、AI、踪迹、我的五个主 Tab 起始边距一致，Pantry、轻账本、采购清单、订阅、地点、书影音和衣橱等子页不再出现明显二次内边距或松散卡片密度。

3. `已交付` P1 计划周期任务
   计划模块新增 recurrence 字段，支持 daily / weekly / monthly / yearly + 整数倍 interval；完成时由服务端派生下一条计划，覆盖周期家务、保养、证件续费等真实需求，不引入新模块。

4. `已交付` P2 Pantry 采购清单
   新增 `/shopping` 页面和 `life_trace_shopping_list_items`，承接 Pantry 缺失的“该买什么”一半；第一版只做手动 + Pantry 用完/丢弃触发，不做 AI 自动建议。

5. `已交付` P3 Today 快速记一笔浮层
   Today 快速记录条「记一笔」按钮接入 BottomSheet，三字段（金额/分类/备注）+ localStorage 记忆上次分类，复用现有轻账本接口，3 秒完成一笔。

6. `已交付 A 块` P4 Inbox 去核心化
   保留 `/inbox` 路由和服务端能力，避免已有 Inbox 数据、转计划/踪迹/账目/地点/书影音草稿链路失效；Today 首页和 Profile 个人工具不再展示 Inbox/灵感作为主入口，应用启动也不再预加载 Inbox 列表。Inbox 仅作为可直达的低频次级能力保留，核心首页心智回到计划、库存、采购、账本和穿搭。

7. `后置` P8 扩展页增强池
   Inbox 语音、书影音从计划/Inbox 生成草稿、地点地图视图、账本票据 AI 草稿、订阅/续费提醒等能力保留为后续单独立项。

8. `后置` 成就体系增强
   积分体系、排行榜、社交排行和更精细的库存整理、季节回顾、家庭协作成就继续后置。

## 下一步优先级

### P1：计划周期任务

状态：已交付（2026-06-18）。

实现要点：

- `life_trace_plans` 新增 `recurrence_frequency`（`none|daily|weekly|monthly|yearly`，默认 `none`）、`recurrence_interval`（正整数，默认 1）、`recurrence_end_at`（可选结束日期）、`recurrence_parent_id`（派生链路根节点 ID）四个字段；保留 `recurrence_weekdays` 待后续扩展，本轮未启用。
- 服务端 `UpdatePlanStatus` 完成分支会按 frequency + interval 计算下一次 `scheduledDate`，并通过 `maybeDerivePlanRecurrence` 复制原计划字段创建下一条；`recurrence_end_at` 命中时停止派生，取消完成不回滚已派生计划。
- 接口在派生时返回 `{ plan, derivedPlan }`，前端 store `completePlan` 兼容旧返回，把派生计划即时拼到列表前面，并写一条 AI 行动记录。
- 创建/编辑 Drawer 新增“重复”表单区：频率按钮 + interval 数字 + 可选结束日期；列表卡片显示“循环”徽标；筛选增加“仅看周期”。

验收（已通过）：

- 服务端：`go build ./...` 和 `go test ./internal/lifetrace/...` 全绿。
- 前端：`pnpm --filter @valley/life-trace exec tsc --noEmit` 通过。
- 待真机抽测的金标用例：每周日洗床单、每年 8 月续保险、每 2 天浇花、关闭循环后不再派生、`recurrence_end_at` 截止后不再派生。提醒口径继续走计划提醒，不新增通道。

### P2：Pantry 采购清单

状态：已交付（2026-06-18）。

实现要点：

- 新增 `life_trace_shopping_list_items`（GORM AutoMigrate）：name、quantity、unit、category、source（`manual` / `pantry_used_up` / `pantry_low` / `pantry_discard` / `recipe`）、`source_pantry_item_id`、`note`、`checked_at` 软勾选时间，按 household 隔离。
- 服务端 `/api/v1/life-trace/shopping` 五个端点：列表（按 `created_at desc`，可过滤 `status=open|checked|all`）、新增、编辑、勾选、删除；返回结构包含 `householdId / householdName / list / summary.openCount`。
- 前端 `/shopping` 二级页（SubPageShell）：顶部新增按钮，列表分组「待买」「已买」，点击圆形 toggle 勾选/恢复，编辑/删除走 BottomSheet 表单和 ConfirmDialog。
- Pantry 触发：`PantryPage` 在「使用」「丢弃」动作完成且物品状态变为 `used-up` / `discarded` 时弹出 ConfirmDialog 询问"是否加入采购清单"，确认后以 `pantry_used_up` / `pantry_discard` 来源写入。
- 入口：`/profile` 个人工具组新增「采购清单」入口卡片；继续与 Pantry 共享当前家庭空间（`preferredPantryHouseholdId`）。
- Today 摘要：当存在待买项时，Today 在 Pantry 卡之后渲染一条「采购清单 · 还有 N 项待买」摘要卡，点击跳 `/shopping`。
- 勾选已买顺手记账：在 `/shopping` 勾选某项为已买后弹出顺手记一笔 BottomSheet，金额必填、分类默认按采购分类映射（食品→吃饭、日用品/宠物→家用、药品→医疗，其他读 localStorage 上次值）、备注预填名称；可跳过；提交走 store `addLedgerEntry`。
- 勾选记账开关：`/shopping` 顶部提供「勾选已买时弹出记一笔」开关，本地 localStorage 持久化（`life-trace.shopping-list.ledger-prompt-enabled`），关闭后勾选只标记已买不再弹账本。
- 已买分组：`/shopping` 的「已买」区按勾选当地日期分组（今天/昨天/MM-DD 周几），日期组按时间倒序排，组内按 `checked_at` 倒序，避免历史已买项堆成长列表。
- 第一版不做 AI 自动建议、不做服务端自动写入、不做按分类分组、不做一键清空已买。

验收（已通过）：

- 服务端：`go build ./...` 通过。
- 前端：`pnpm --filter @valley/life-trace exec tsc --noEmit` 通过。
- 待真机抽测的金标用例：手动新增→出现在「待买」、勾选→进入「已买」、Pantry 全部用完后弹出加入清单→在 `/shopping` 看到一条 `pantry_used_up` 来源条目、家庭空间切换后清单跟着切。

### P3：Today 快速记一笔浮层

状态：已交付（2026-06-18）。

实现要点：

- Today 快速记录条右下角钱包图标「记一笔」按钮触发 `QuickLedgerSheet`。
- 三字段：金额（必填）、分类（默认上次使用，localStorage 持久化）、备注（选填）；固定 direction='支出'、currency='CNY'、occurredAt=now。
- 复用 store `addLedgerEntry`（即 `POST /api/v1/life-trace/ledger`），提交后 toast 提示并自动关闭。
- 浮层不展开支付方式 / 商家 / 图片，那些字段保留在 `/ledger` 里。

验收（已通过）：

- 从打开 Today 到完成一笔记录全程不超过 3 秒。
- 提交后 Today 的本月支出摘要立即更新（依赖原有 ledger summary 通道）。
- 浮层取消和提交都不会跳转其他页面。

### P4：Inbox 去核心化

状态：A 块已交付（2026-06-18）；去核心化已交付（2026-06-26），B 块后置。

实现要点（A 块）：

- `/inbox` 路由保留，服务端表名、接口、模型不动；前端不再把 Inbox 作为核心首页入口。
- `InboxPage` 顶部 eyebrow 改为 `Inspiration`、标题改为「灵感」；移除右上「写下灵感」按钮，避免与下方速记 Card 形成两套新建入口；新建灵感统一走页面顶部 Card 内的 Textarea + 「记下」按钮，BottomSheet 仅承担编辑灵感 / 补全字段。
- 列表加载态改为「正在同步灵感」；空状态 icon 换为 `Lightbulb`，eyebrow 「灵感」、标题「还没有灵感记下」、描述「写下你刚才想到的事，先记下来再说。」。
- 卡片操作区拆为主行（编辑 + 更多/收起）和展开行（AI 整理 / 应用建议 / 转计划 / 转踪迹 / 转账目 / 归档 / 恢复 / 删除），通过 `expandedActionId` 内联展开，避免主页面被次级动作淹没。
- BottomSheet 仅作为编辑入口（编辑灵感）；图片字段 label 改为「灵感图片」；提交按钮文案「保存修改」。
- TodayPage 快速记录条回到计划主线，主动作改为添加今日计划；Today 不再展示 Inbox/灵感 EntryCard。
- ProfilePage 个人工具移除 Inbox/灵感入口，保留轻账本、订阅、采购、地点等次级工具；应用启动不再预加载 Inbox 列表。

B 块（后置）：

- 服务端解禁 AI 自动整理：保存灵感后异步触发摘要/标签生成，含去重和 rate limit；失败静默不阻断保存。
- UserSettings 增加「灵感自动整理」开关，默认开启；关闭后保存灵感不触发服务端 AI 调用。
- AI 摘要 / 标签作为副字段弱化展示，主页面继续展示原文。
- 底部 Tab 是否要把「灵感」加为一级 Tab，留作 B 块再决定。

验收（A 块已通过）：

- 前端 `pnpm --filter @valley/life-trace exec tsc --noEmit` 通过。
- 待真机抽测：Today 顶部主动作进入计划，不再进入 `/inbox`；Today 工具卡不再出现灵感入口；ProfilePage 个人工具不再展示灵感；直接打开 `/inbox` 仍能管理旧数据。

### P5：个人生活扩展页增强

状态：进行中。

#### P5.1 灵感沉淀路径（已交付）

- 服务端 `validInboxConvertedTypes` 扩 `media | place`，覆盖到 PATCH /lifetrace/inbox-items/:id/convert，附 `inbox_handler_test.go` 子测试。
- 前端 `InboxConvertedType` 同步扩 union；`lib/inbox.ts` 新增 `buildInboxMediaDraft / buildInboxPlaceDraft`，含基于域名的最浅 `mediaType` 推断（豆瓣/IMDB/Bilibili/网易云等）。
- InboxPage 展开行新增「转书影音」「转地点」按钮，复用既有 `convertInbox` 落库；按钮仅在 `status === 'inbox'` 显示。
- MediaDiaryPage / PlacesPage 接 `?new=1&inboxItemId=…` query 预填新建表单，提交成功后自动写入 `convertedType` 和 `convertedId`，并把原灵感置为已转化（与现有 ledger 入口一致）。
- 验证：`pnpm --filter @valley/life-trace exec tsc --noEmit`、`go test ./internal/lifetrace/...`、`encoding-guard` 全部通过；本次未做 AI 推荐 chip（aiSuggestedType 仅支持 plan/trace，避免改 prompt）。

#### 后续子卡

- P5.2a 订阅与续费（已交付）：服务端 `LifeTraceRecurringPayment` 模型与 5 个端点（list/create/update/delete/advance）+ `LedgerEntry.recurring_payment_id` 外键、reminder push 投递；前端 types/api/store/RecurringPaymentsPage 独立页面与 ProfilePage 入口、LedgerPage `?recurringPaymentId=` query 接入与保存后调 advance；服务端单测 `recurring_payment_handler_test.go` 覆盖 nextRecurringDueDate 6 频率 / resolveInitialNextDue / buildRecurringPaymentSummary 折算 / Create 校验 / Advance 跨 EndAt 归档与正常前进。续期策略：advance 仅在 LedgerPage 保存成功后触发，提醒只推不自动记一笔。
- P5.2b 票据图片 AI 草稿（待启动）：复用 `image_ai_handler` + `LifeTracePhotoItemDraft` 思路，把票据图片转换为 LedgerEntry 草稿。
- P5.3 地点 v3 / 书影音外部检索：地图 pin 视图、外部地图搜索、地点分类、地点照片、地点标签；书影音外部平台检索；本月书影音月报。
- 灵感（原 Inbox）：语音输入、AI 摘要质量增强、AI 页生活动作入口。
- 衣橱 / 穿搭：穿着次数统计、最近穿着时间。

### P6：成就体系增强

状态：后置。

- 积分体系、排行榜和社交排行。
- 更细的库存整理、季节性回顾和家庭协作成就。
- 成就图标可从前端 SVG 替换为 AI 生成静态 PNG/WebP，但不做运行时生图。

## 已完成路线图

### P0：云端生活数据闭环

状态：已完成。

- 计划、踪迹、偏好已接入服务端。
- 计划完成后可生成踪迹。
- 结构化计划时间已落地。

### P1：服务端 AI 能力

状态：已完成首轮。

- 今日建议 AI 已接入，保留在 AI 页快捷操作中；首页不再主动加载今日建议和 AI 今日简报。
- 每周回顾 AI 已接入并支持历史保存。
- 图片生活分析 AI 已接入。
- AI 不可用时保留本地兜底。
- AI 架构基座已启动首轮治理：统一读取 ARK/OpenAI-compatible 配置，集中承接文本 JSON、视觉 JSON、prompt contract 和 Assistant action registry；图片生活分析、Inbox 整理和书影音补全已作为首批 prompt contract 迁出 handler。后续新增 Life Trace AI 功能应先定义 prompt contract 与 action spec，再接 handler 或前端入口。

### P2：家庭空间和 Pantry

状态：已完成并验收收口。

- Household model、API、personal 回填、共享家庭、Pantry household 查询已落地。
- Pantry 详情页、单条库存详情接口和操作时间线接口已落地。
- 个人库存到共享家庭的单条/批量移动与复制已落地，支持冲突预览、合并和保留两条。
- 家庭成员管理、归档态和操作踪迹已完成本轮验收收口。

### P3：AI 拍照分析入库 MVP

状态：已完成并验收收口。

- AI 页“拍照分析商品”入口和独立拍照分析页已落地。
- 商品图片识别接口已接入服务端模型代理，Pantry 拍照分析优先使用 Gemini 视觉模型，未配置时回退 ARK。
- 用户确认后才写 Pantry，不自动入库。
- 拍照、相册上传、重拍、编辑、入库、失败重试和共享家庭入库已完成手动验收。

### P3.1 / P3.2：AI 拍照分析 Post-MVP

状态：已完成并验收收口。

- 条码识别首版已使用浏览器原生 `BarcodeDetector`，不可用时提供手动编码兜底；不接外部商品库。
- Pantry item 已增加可选 `barcodeValue`、`barcodeFormat` 字段，用户确认后才持久化。
- 条码连接商品识别记忆库已落地：按当前家庭空间复用历史确认商品资料，命中后只预填稳定字段。
- 重复库存合并已增加批次保护，到期日不同的同码/同商品不推荐合并。
- Pantry 日常消耗已支持"用 1"、指定数量使用/丢弃、全部用完和全部丢弃；标记用完动作（列表卡数量为 1、详情页"用完"、库存处理面板"全部用完"）已统一弹出二次确认，避免误操作。
- 多商品识别、保质期线索识别、识别历史、草稿恢复、低置信度复核、识别质量反馈、家庭空间/位置/单位智能建议、提醒规则智能建议、重复库存合并和批量连续扫描已落地。
- 最近商品识别已拆成 AI 页摘要和独立历史页，完整云端历史可在 `/ai/photo-item-history` 查看，旧本地记录会在登录后同步上云。
- 库存列表筛选已支持"未设过期"快捷项；状态、分类、排序选择会随用户切换写回 `LifeTraceSettings`，再次进入页面或换设备时按云端记忆恢复，URL 参数仍优先于云端记忆。

### P4：提醒链路真机验收

状态：已完成。

- 本地系统通知桥接已实现。
- 提醒设置页已补“通知诊断”，集中展示权限、Service Worker、Push 绑定和最近一次测试结果。
- 服务端 Web Push 已进入外部 Cron 扫描方案，通过 `WEB_PUSH_CRON_SECRET` 保护扫描接口。
- Vercel Serverless 环境应配置 `WEB_PUSH_WORKER_ENABLED=false`，只保留外部 Cron 扫描。
- 提醒扫描和推送失败会写入 `operation_logs`，便于线上诊断。
- iPhone 和 Android 真机已确认权限、到达率、点击回跳、延迟窗口和 PWA 主屏幕模式表现。

### P5：智能体入口增强

状态：已完成首轮，持续增强。

- AI 页已完成第一轮重排：今日上下文摘要、对话入口、生活动作、Pantry 智能四块同页呈现。
- 支持读取今日天气、计划、踪迹和 Pantry 上下文。
- 支持更细的历史对话分组、搜索和动作追踪。

### P6：库存优先智能菜谱

状态：已完成首轮并验收收口。

- AI 页“智能菜谱”快捷入口已跳转至 `/ai/recipes` 管理页。
- 服务端 `POST /api/v1/life-trace/ai/recipes` 已落地。
- 已支持基于当前 Pantry 家庭空间生成临期优先菜谱。
- 已支持历史菜谱管理和独立菜谱详情页。
- 已排除已过期食材和非食品库存。
- 菜谱可加入“吃饭”计划，不自动扣减 Pantry 库存。
- 真实模型输出质量、移动端卡片布局、加入计划后的用户手动库存消耗确认体验已完成验收。

### P7：生活成就系统

状态：已完成首轮。

- 成就接口：`GET /api/v1/life-trace/achievements`。
- 成就记录：`life_trace_achievements`。
- 成就定义由服务端静态维护，写操作成功后静默评估，不阻断原业务流程。
- 成就池从 20 枚扩展到 72 枚，覆盖计划、踪迹、Pantry、AI、家庭共同成就、季节和连续习惯。
- 成就首次解锁后异步生成 AI 纪念语，AI 配置缺失或失败不阻塞业务。
- 前端已接入 `/achievements`、我的页入口、Today 最近成就卡、解锁提示、PNG 分享卡和专属徽章图标。
- 家庭共同成就首轮已接入，但积分、排行榜和社交排行继续后置。

## 页面与模块范围

### 今日

- Today 首页采用“柔和生活日记 / 今日节奏”首屏：问候与天气、四格今日节奏、快速记录条、时间线式今日计划、生活卡片和今日小结优先呈现。
- 今日天气主卡。
- 未来小时天气。
- 今日穿搭建议卡。
- 家庭库存临期提醒。
- 今日计划模块；存在可提醒计划时提示下一次提醒。
- Inbox、轻账本等快捷入口。

### 计划

- 计划筛选：全部、今天、周末、已提醒。
- 内置计划类型：电影、吃饭、运动、阅读、聚会、普通事项。
- 计划卡片展示提醒状态。
- 计划卡片和详情页支持加入系统日历。
- 创建计划 Drawer。
- 创建和编辑计划时支持选择地点库中的常用地点，并保留地点文本字段兼容旧数据。
- 计划详情使用 `/plans/:planId` 二级页面承载，进入详情时隐藏底部 Tab。
- 计划列表、创建、完成和删除通过服务端同步。
- 完成计划后生成踪迹，并通过服务端同步。

### AI

- Life AI 指挥中心。
- 智能体对话、历史消息、历史分组、搜索和动作追踪。
- 快捷操作：生成今日建议、创建计划、智能菜谱、分析图片、生成踪迹、每周回顾。
- 图片生活分析：用于电影海报、美食照片、生活照片，结果可生成计划或踪迹。
- 商品拍照分析：从 AI 页跳转到 `/ai/photo-item-analysis`，走 AI 拍照识别，最终入 Pantry 库存。
- 最近商品识别：AI 页展示草稿优先摘要，完整记录进入 `/ai/photo-item-history`。
- 今日穿搭和拍照识别衣物：从 AI 页进入衣橱相关能力。
- 智能菜谱：从 AI 页进入 `/ai/recipes`，集中管理生成、历史菜谱、加入计划和菜谱详情。

### Pantry

- 库存列表、筛选、排序、分页和摘要；状态筛选支持"在库 / 临期 / 已过期 / 未设过期 / 已用完 / 已丢弃"，状态、分类、排序选择会写回云端用户设置并跨端同步。
- 库存详情页：真实图、透明封面、基础字段、所属家庭空间、到期状态、提醒规则、条码、备注和操作时间线。
- 创建、编辑、删除库存物品。
- 标记已用完、已丢弃、部分使用、部分丢弃。
- 库存到期、临期、正常状态派生。
- 库存图片、分类、标签、数量、单位、保质期、购买日期、存放位置、备注。
- 家庭空间切换：个人空间和共享家庭空间。
- AI 拍照分析入库入口、识别历史和草稿恢复。

### 家庭空间

- `Household` 是统一空间抽象：`personal` 和 `shared`。
- 每个用户始终拥有一个个人空间，个人空间不可解散。
- 用户加入共享家庭后，个人空间仍然存在。
- 库存永远属于某个 household，不直接属于某个 user。
- 用户加入家庭时，不自动迁移个人库存。
- 家庭解散后，家庭库存保留在原 household 上，进入只读归档态。
- 普通成员可以退出家庭。
- owner 如果要退出且仍有其他成员，需要先转移 owner。
- owner 是最后一位成员时，应走解散家庭流程。

已落地能力：

- 通用 household model。
- personal household 自动创建和回填支持。
- household API：列表、详情、创建、成员、邀请、加入、退出、转移 owner、解散。
- Pantry 按 household 查询和权限校验。
- Pantry 家庭空间切换和家庭管理入口。
- 邀请码复制、重新生成、撤销。
- 共享家庭详情、成员列表和归档家庭只读展示。
- 个人库存移动或复制到共享家庭库存，支持重复预览、合并数量或保留两条。

### 衣橱 / 穿搭

- 使用 `/closet`、`/closet/items/:itemId`、`/closet/outfits/:outfitId` 和 `/ai/photo-clothing-analysis`。
- 入口：Today 今日穿搭卡、AI 页今日穿搭和拍照识别衣物、我的页“衣橱与穿搭”。
- 个人衣橱默认仅本人可见。
- 共享家庭空间采用主动共享衣物池，成员只看到主动共享的衣物和穿搭。
- 支持手动创建、编辑、删除衣物。
- 支持照片生成 AI 衣物草稿，用户确认字段和所属空间后才入库。
- 支持规则优先、AI 增强的穿搭建议。
- 支持保存穿搭、查看穿搭详情和标记已穿。
- 标记已穿后生成 `source=穿搭` 的生活踪迹。
- 共享穿搭只能引用共享衣物，不允许把未共享的个人衣物暴露到家庭空间。

### 踪迹

- 完成后的生活记录流。
- 图片、标签、心情、AI 回忆文案。
- 来源可以是计划、库存状态、书影音、穿搭或手动记录。
- 踪迹详情使用 `/traces/:traceId` 二级页面承载，进入详情时隐藏底部 Tab。
- 当前通过服务端同步，localStorage 只保留非核心兜底状态。
- 创建和编辑踪迹时支持选择地点库中的常用地点。
- 带地点的计划和踪迹会自动沉淀到地点库。

### 地点库

- 使用 `/places` 和 `/places/:placeId`，不接入地图 SDK。
- 地点从计划和踪迹的地点字段自动归集，按用户隔离，同名地点按规范化名称复用。
- 地点列表展示收藏地点、想去地点、常去地点和最近出现地点。
- 地点详情展示记录次数、首次/最近出现时间、备注，以及关联计划和踪迹。
- 地点支持手动创建、收藏、改名、归档和 JSON 导出。
- Pantry 的冷藏、厨房、储物柜等库存存放位置不进入地点库。

### Inbox

- 使用 `/inbox`。
- 用于快速收集暂时不知道归到哪里的生活碎片。
- 支持文本、链接、单图和标签。
- 支持按状态、类型和关键词筛选。
- 条目状态：`inbox`、`converted`、`archived`。
- 支持手动触发 AI 整理，生成标题、摘要、标签和建议去向。
- 支持手动转为计划草稿或踪迹草稿，用户保存后原 Inbox 条目标记为已转化。
- AI 建议不会自动创建计划或踪迹。

### 书影音日记

- 使用 `/media-diary` 和 `/media-diary/:id`。
- 类型：书籍、电影、剧集、动漫、音乐。
- 字段：标题、原名、作者/导演/艺人、年份、封面图、开始日期、完成日期、状态、评分、短评、摘录、标签。
- 支持手动创建、编辑、删除。
- 支持 AI 辅助补全作者/导演/艺人、年份、标签和短评草稿。
- 保存记录时自动生成 `source=书影音` 的踪迹。
- 编辑记录时同步更新关联踪迹，删除记录时保留历史踪迹。

### 轻账本

- 使用 `/ledger`。
- 做生活型轻记录，不做完整财务系统。
- 字段：金额、币种、分类、支付时间、商家/地点、备注、图片。
- 方向：支出、收入、退款、转账备注。
- 分类：吃饭、交通、购物、书影音、订阅、家用、礼物、医疗、其他。
- 支持手动记一笔、编辑、删除和分页列表。
- 支持本月支出、收入、退款、结余和支出分类统计。
- 支持从 Inbox 文本和 Pantry 商品生成账目草稿。
- 支持 AI chat 记账：用户明确说“记账/记一笔/花了/收入/退款”等并提供金额时直接入账；缺金额时只追问金额。
- 所有草稿必须用户确认后才入账。

### 我的

- 个人资料、天气城市、工作时间、每日简报和通勤方式。
- 我的首页为总览和入口，提醒细项不堆叠在首页。
- 生活成就入口。
- 提醒设置页：天气风险、每日简报预览、计划提醒、计划提前提醒、工作日规则、勿扰时间、系统通知、服务端推送绑定和 Pantry 默认提醒。
- AI 个性化保留在我的首页，不和提醒规则混放。
- 库存与家庭：默认家庭空间、家庭管理入口。
- 衣橱与穿搭入口。
- 应用安装、更新检查、分享入口和版本信息。

### Admin 后台

- Admin 菜单按业务域分组，Life Trace 作为独立后台管理域。
- Life Trace 管理页提供数据概览、用户概览和统一记录查询。
- 统一记录查询支持计划、踪迹、库存、周报、AI 对话、推送订阅和推送投递记录。
- 第一阶段只读查询，不在后台直接编辑或删除用户生活数据。

## AI 与图片能力边界

### 通用 AI 规则

- AI 能力只做整理、摘要、建议和草稿生成。
- 所有写入计划、踪迹、账目、衣橱、地点、库存的动作必须由用户确认。
- AI 未配置或失败时返回可解释错误，不阻断手动流程。
- 前端不保存真实 AI 密钥，不直连模型。
- 药品、保健品、婴幼儿食品等敏感品类只做库存记录建议，不输出医疗或安全结论。

### 商品拍照分析

接口：

```text
POST /api/v1/life-trace/ai/pantry-photo-analysis
```

请求只走 AI 商品识别，不再提供独立文字识别拍照模式；AI 返回商品草稿时可包含生产日期、到期日、保质期天数和日期线索。只有识别到到期日，或同时识别到生产日期和保质期时才自动回填到期相关字段；仅识别到“保质期 9 个月”这类无日期锚点线索时只提示用户确认，不写入表单日期字段；没有日期线索时仍可作为普通商品拍照录入。

AI 返回草稿字段：

- `name`：商品名称。
- `category`：分类，例如 食材、饮料、零食、药品、日用品、护肤、清洁、其他。
- `brand`：品牌，可为空。
- `spec`：规格，例如 `500ml`、`2kg`、`12片`。
- `quantity`：数量，默认 `1`。
- `unit`：单位，例如 件、瓶、盒、袋、个、包。
- `storageLocation`：建议存放位置。
- `expiresAt`：保质期日期，可为空。
- `purchaseDate`：购买日期，默认今天或空。
- `tags`：标签。
- `confidence`：整体识别置信度。
- `warnings`：识别不确定、保质期不可见、需要用户确认等提示。
- `cropBox`：建议裁剪框，保留服务端响应兼容。
- `source` / `model` / `modelTag`：本次识别使用的 AI provider、模型名和展示标签，例如 `Gemini · gemini-2.5-flash` 或 `ARK · ep-...`。

用户可编辑字段：

- 商品名称。
- 分类。
- 数量和单位。
- 购买日期。
- 保质期。
- 存放位置。
- 所属家庭空间。
- 默认提醒规则。
- 备注。
- AI 缩略图。
- 商品标签：主分类保持食品、日用品、药品、宠物、其他等稳定大类；标签用于记录冷冻、零食、调料等可自定义细分类。

### 透明封面策略

- 入库时保存原图 URL 到真实图片字段。
- AI 缩略图字段默认留空。
- 列表卡片优先展示真实图片。
- 透明封面只在用户手动点击“生成透明封面”时触发。
- 主路走服务端 remove.bg，API key 只放在服务端环境变量 `REMOVE_BG_API_KEY` 中。
- remove.bg 配置缺失、额度耗尽或请求失败时，自动使用浏览器本地 IMG.LY 模型兜底。
- 透明封面结果作为 `thumbnailUrl` 候选，不覆盖真实图片。
- 如果未来希望完全离线且不依赖 IMG.LY 默认模型 CDN，再单独评估自托管模型文件、`rembg` / RMBG sidecar 或其他 provider。

## 技术基线

- 前端：React + Vite + TypeScript。
- UI：Tailwind CSS + shadcn/ui dark-first 风格。
- 图标：lucide-react。
- 状态：Zustand + localStorage 兜底持久化；核心数据通过 Go API 同步。
- PWA：manifest + service worker。
- 后端：复用 Valley MAS Go 服务，Life Trace 能力集中在 `server/internal/lifetrace`。
- AI：服务端统一代理模型调用，不在前端直连模型；Pantry 拍照分析优先使用 Gemini 视觉模型，其他 Life Trace AI 默认复用现有 ARK / Life Trace 文本配置。
- 图片：复用现有上传接口和服务端图片分析能力；用户手动上传/拍照选择的生活图片在前端先压缩，再上传到云端。

依赖策略：

- 默认不新增第三方依赖。
- 新增依赖前必须说明要解决的问题、主要取舍和替代方案，并询问用户是否采用。
- AI 拍照分析复用浏览器原生 `MediaDevices.getUserMedia`、`canvas`、现有上传接口；Pantry 商品识别优先走 Gemini 视觉模型，未配置时回退现有 ARK 视觉/文本模型。
- 条码识别首版使用浏览器原生 `BarcodeDetector`；不可用、识别失败或格式不支持时，提供手动输入编码入口。
- IMG.LY 兜底使用较小的 `isnet_quint8` 模型，优先保证能返回透明 PNG，不追求最高质量。

## UI 设计规则

- 跟随系统外观：系统浅色时使用柔和生活日记浅色主题，系统深色时使用原深色主题。
- 浅色背景：`#fbf7ef`。
- 浅色卡片：`#fffdf8`。
- 浅色抬升面：`#f1e9dd`。
- 浅色边框：`#e2d7c8`。
- 浅色主文本：`#2d2923`。
- 浅色次文本：`#7b7166`。
- 深色背景：`#09090b`。
- 深色卡片：`#18181b`。
- 深色抬升面：`#27272a`。
- 深色边框：`#27272a`。
- 深色主文本：`#fafafa`。
- 深色次文本：`#a1a1aa`。
- 功能色：
  - 天气：`#3b82f6`。
  - AI：`#06b6d4`。
  - 计划：`#8b5cf6`。
  - 踪迹：`#10b981`。
  - 健康：`#f59e0b`。
  - 风险提醒：`#f97316`。

移动端约束：

- 优先适配 390px iPhone 宽度。
- 页面不允许横向滚动。
- 底部 Tab 只在今日、计划、AI、踪迹、我的五个主 Tab 根页显示；子页面隐藏底部 Tab。
- 子页面使用顶部返回栏和从右向左进入的导航栈动效，内容区不再预留底部 Tab 高度。
- 主 Tab 内容区预留底部安全距离。
- 拍照页的拍照按钮、重拍按钮、入库按钮要固定在可触达区域，避免被浏览器底栏遮挡。

## PWA 能力

- 已提供 `manifest.webmanifest`。
- 已生成 192px / 512px 应用图标。
- 已注册 service worker，并缓存应用壳、manifest 和图标。
- manifest 与图标采用网络优先刷新，降低应用名称或图标变更后继续使用旧缓存的概率。
- service worker 导航请求遇到托管平台 404 时会回退到应用壳，避免用户在子路由点击“立即更新”后看到 `404: NOT_FOUND`。
- 服务端保留 `/api/v1/life-trace/checkins` 旧接口兼容层，只给下线每日打卡前的 PWA 缓存包返回空列表 / no-op 结果，避免旧首页请求触发 404 toast；不恢复每日打卡功能和数据模型。
- Vercel 部署配置已补 SPA rewrite，直接打开或分享子路由时统一返回 `index.html`。
- 全局应用壳提供安装和新版本提示。
- 我的页提供 PWA 控制台，展示安装状态、离线缓存状态、更新状态、分享能力，并提供安装、检查更新、立即更新和分享应用入口。
- 我的页底部展示当前应用版本，格式为 `v产品版本+构建标识`，例如 `v0.1.0+abc1234`。
- 生产构建会把当前 commit 短 SHA 注入 service worker，让每次提交后的部署都能被浏览器识别为可更新版本。
- 应用分享优先使用系统分享面板，失败时回退为复制应用链接。
- 本地系统通知桥接已实现；服务端 Web Push 采用外部 Cron 扫描兜底。

## 验收与校验

功能或代码变更时按范围运行：

- Life Trace 前端类型检查：`pnpm --filter @valley/life-trace exec tsc --noEmit`。
- Life Trace 前端测试：`pnpm --filter @valley/life-trace exec vitest run`。
- Go 服务改动：`cd server && go test ./...` 或更小范围的 `cd server && go test ./internal/lifetrace`。
- 中文文案、Markdown、协作规则或 skill 改动：`python3 .agents/skills/encoding-guard/scripts/check_mojibake.py <相关文件>`。

前端、游戏或可视交互改动不使用 Playwright 自动验收。涉及移动端布局、拍照、PWA 通知、真实模型输出或真机权限时，最终回复必须给出清晰的手动验收路径。
