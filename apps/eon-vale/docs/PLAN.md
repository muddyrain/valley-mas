# Eon Vale 产品与交付状态

更新时间：2026-08-13。玩法语义、默认值、阈值、例外和待决问题以 [GAME_RULES.md](./GAME_RULES.md) 为唯一真源。

## 产品定位

Eon Vale（纪元谷）是一个浏览器单机 2D 像素神明沙盒。核心体验不是随机移动单位演示，而是可持续演化、可被玩家干预且可存取的活世界：居民出生、成长、工作、建设、获得装备与身份；聚落发展经济并建立王国；王国可能结盟、开战或灭亡；动物参与觅食、逃跑、捕食和繁殖；神力会改变真实模拟状态。

## 已交付状态

- **阶段 0 性能骨架**：Web Worker 固定 20 Hz 模拟，主线程快照插值，实体状态使用 Packed TypedArray；个体寻路使用带预算 A*，战争群体移动使用 Flow Field，局部避让使用空间哈希。
- **PixiJS 8 真 2D 渲染**：不再使用 Three.js、透视相机或斜视角。地图固定正北，只允许有边界的平移与 `0.5/0.75/1/1.5/2/3/4` 阶梯缩放，缩放锚定指针，不支持旋转和倾斜。
- **三级地图 LOD**：世界层使用每格 1 像素的宏观纹理，只保留连续海岸、大片生物群系、聚落名称和资源叠层；聚落层使用 4 像素区划纹理并显示抽样居民、抽样资源、道路与建筑；居民层显示完整居民、建筑与资源节点。地形 Chunk 原位更新，地图只同步脏单元，资源只同步脏节点，不在周期快照中传输或重建整张地图，避免整屏闪烁。
- **有限世界**：支持 128、256、384 三种地图，默认 256；支持确定性群岛、主大陆和空白海洋，边界始终为深海，同一种子可重放。空白海洋不预置居民或动物，需玩家先创造陆地和生命；自然回归只作用于该世界曾经存在过的物种，不会把从未引入的生命凭空加入空白世界。
- **地图编辑**：升高、降低、陆地、水域、森林、食物、石料与清理；森林、石料和清理直接创建或移除独立节点，编辑仅使受影响导航 Chunk 失效，并同步脏单元与脏节点。
- **个体人口系统**：所有居民都是持续存在的真实个体，拥有年龄、性别、家庭、伴侣、父母、健康、饥饿、精力、职业、等级、经验、贡献、身份、装备、特质、携带资源、资源目标和个人经历。初始人口采用平衡年龄结构；出生来自同村伴侣并继承家庭关系，新生代性别会优先补齐本王国未成年人口中较少的一侧，不再受历史槽位奇偶影响；居民会老去，并按寿终、饥荒、疾病、袭击或灾害记录死亡原因。实体数组达到 1,200 个历史槽位后，会安全复用未被存活家庭、王国领袖或先遣队引用的死亡槽位，避免长局永久停止出生和投放。
- **动态承载力与迁徙**：村庄承载力由住房、农田产能、储粮趋势和安全共同决定，空置遗址不再虚增当前人口承载目标。人口压力按“抑制出生 → 扩建真实住房 → 家庭迁居 → 消耗储备 → 长期饥荒”响应；满载聚落的年轻伴侣会带未成年子女沿真实路径迁入同王国可达的低占用聚落，低人口聚落的未婚成年人也可迁往存在适龄配偶的村庄，均不使用瞬移或生成替身。
- **像素居民与王国识别**：居民由肤色、头发、衣服、王国色、职业工具、身份和装备分层生成；无王国居民使用中性色。建筑、居民和旗帜共享王国色，同一套像素实体在世界、聚落和居民层连续缩放，不替换为战略圆点。
- **动物生态**：鸡、羊、牛、鹿、狼、熊、鱼七种独立侧面像素轮廓。食草动物觅食并躲避捕食者；狼和熊优先捕猎野生动物，只有极度饥饿且远离聚落时才会攻击居民。动物会因寿命、长期饥饿、捕食、疾病或灾害死亡，繁殖要求成年、饱食、异性配对且低于真实栖息容量；生态面板按物种显示累计出生、死亡与死因。自然回归仍只作用于曾经存在的物种。尸体、狩猎、屠宰、搬运和捕鱼物流仍属于第四批。
- **世界法则目录**：建立类型安全 `WorldLawCatalog`，统一法则 ID、分类、默认值、决策状态和交付状态。当前 UI 只开放“动物自然回归”和默认关闭的显式沙盒法则“文明重启”；后者仅在人类灭绝满 20 年后投放八名成年奠基者，不参与默认人口平衡。饥饿、老死、繁衍、捕食、扩张、外交战争与灾害已经登记为计划中法则，在关闭行为真正接通前不显示为可用开关。
- **独立资源节点**：树木、露天石料和金属矿脉使用可扩容 TypedArray 存储和 `8×8` 分块索引，384 地图稳定生成约 2.95 万个节点。树木按树桩、幼苗、幼树、成熟阶段通过到期事件再生，石料和矿脉保持有限；节点只参与局部软避让，不使全局 A*/Flow Field 失效。512/768 地图已覆盖生成、索引、V6 保存和恢复。
- **需求驱动经济与有机布局**：食物、木材、石料、金属、黄金、工具与装备构成资源闭环；伐木工和矿工只在库存低于人口目标时出工，预留节点后采集并真实携带回仓库，完成入库前不增加村庄库存。金属开采要求先在有限矿脉附近建成矿场，不再由矿场或伐木营地定时凭空产出资源。住房围绕中心生长，农田位于外圈，生产与公共建筑形成小型功能区，道路连续连接中心和建筑。
- **施工物流**：建筑选址会先锁定一次成本，清理范围内的真实资源节点并回收材料，再由搬运者分批送达工地；全部材料到位后建筑工才推进施工阶段。清障、运输、施工、受损和摧毁状态均可被地图反馈观察。
- **王国战争**：村庄形成王国并维护和平、联盟和战争关系。守卫沿共享 Flow Field 集结，接敌后优先攻击敌方守卫；防守力量崩溃后才累计聚落占领进度，不主动追杀平民。占领会转移聚落、居民和王国归属，保留至少 65% 聚落与建筑健康，使经济可以继续运行。战争记录初始军力、进展、得分与疲劳；持续 5 年后会因军力低于开战时 40%、失地或长期无进展而议和，20 年强制结束，并进入 15 年休战。
- **战斗表现**：守卫沿用可见武器、护甲等级与队长旗帜，并增加两帧突刺、受击红闪、仅战斗中显示的细血条和死亡烟尘；装备等级继续影响像素外观和实际伤害/减伤。
- **十六种神力**：降雨、雷击、火焰、龙卷风、陨石、瘟疫、祝福、治愈、狂暴、和平/战争、诅咒、生长、冰霜、地震、净化、繁盛；均作用于真实环境、资源节点、实体、建筑或外交状态。生长会创建或催熟树木，火焰会烧毁树木，陨石与地震会移除命中节点。
- **观察与操作**：全屏地图、紧凑顶部状态、底部工具与时间控制、按需展开的世界局势和检查器。顶部人口入口展示趋势，并可展开出生、死亡、迁徙、承载力、年龄结构、住房、储粮和死因诊断；顶部动物入口展开逐物种数量、栖息地容量和稳定、濒危、灭绝、等待栖息地、回归冷却等状态。居民检查器包含概况、成长、装备、经历四页，并支持收藏和跟随。居民视图的 hover 使用恒定 1 屏幕像素、对齐脚下可见占地的薄椭圆，选择使用更强金色轮廓；聚落层的建筑和资源按占地轮廓反馈，世界层只保留聚落范围与名称。
- **V6 存档**：Zod 校验的版本化 JSON；开发期按当前约定不迁移旧版本。V6 严格校验完整世界法则状态，并使用新的 IndexedDB 命名空间隔离旧开发档案；继续保存地图、独立节点、居民、家庭、施工物流、生态诊断、战争和休战状态，提供三个手动档、三个滚动自动档和导入导出。单槽写入使用临时记录后替换，失败时保留上一份有效档案。
- **音频**：Web Audio 程序化环境底噪和创建、神力、危险、选择提示音，可静音，不依赖外部素材。

## 阶段 0 浏览器实测

环境：自动化 Google Chrome 151、macOS、1440×900、设备像素比 1。三档均加载 128×128 确定性群岛作为代表性地形，预热后进入 4× 居民近景并读取实时遥测。Draw Calls 是按 PixiJS 可见纹理批次计算的透明估算值，不冒充 GPU 驱动计数。

| 居民数 | FPS | 平均 Tick | P95 帧时间 | 批次估算 Draw Calls | 三角形 | 近景可见居民 | 寻路队列 | Long Tasks |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 100 | 119.5 | 0.22 ms | 9.7 ms | 3 | 80 | 37 | 1 | 0 |
| 500 | 119.5 | 0.67 ms | 10.1 ms | 3 | 414 | 197 | 3 | 0 |
| 1,000 | 119.5 | 0.93 ms | 9.9 ms | 3 | 788 | 378 | 113 | 0 |

永久 E2E 门槛为 FPS 大于 20、P95 小于 50 ms、批次估算 Draw Calls 不超过 20、三角形少于 30,000，并要求 100/500/1000 三档近景都发生视口裁剪。当前三档均通过；1,000 居民仍存在 113 个待处理路径请求，但平均 Tick 保持在 1 ms 内。

长寿世界基线另有完整世界门禁：384×384 主大陆、1,000 初始居民、约 3 万个独立资源节点，锁定玩家选择的 8× 连续运行 30 分钟。Windows Headless Google Chrome 151 的最终代码 30 个逐分钟采样中，最低 `118.9 FPS`、最差 `P95 16.8 ms`、最高累计平均 Tick `2.41 ms`，Tick 从 `9,635` 推进到 `241,493`，全程满足 `FPS ≥ 60`、`P95 ≤ 25 ms`、`平均 Tick ≤ 4 ms`，且速度未被观察行为修改。

## 架构边界

- `src/simulation` 是无 DOM 的确定性模拟核心；地图和实体使用 TypedArray，村庄、王国、建筑和事件使用结构化对象。
- `src/worker` 负责时间倍率、世界法则命令、可转移实体快照、地图脏单元、资源脏节点、人口与生态诊断、检查器数据和 V6 序列化；高频模拟状态不进入 React。
- `src/simulation/rules` 提供类型安全世界法则目录和人口、生态、劳动、领土、运行时领域规则接口；具体行为仍由各领域模块实现，不建设万能规则引擎。
- `src/render` 负责 PixiJS 8 WebGL 2D 场景、24×24 渲染 Chunk、三级地形 LOD、像素纹理、插值、分层拾取、覆盖层和即时反馈。地图和资源变化只立即重绘当前可见 LOD，其他 LOD 记录脏标记并在玩家切换层级时补绘。
- `src/App.tsx` 只协调低频 UI、设置、选择、IndexedDB 档案和音频。
- 导航与资源索引保持 8×8 Chunk；地形编辑只递增受影响导航 Chunk 版本，使旧路径自动失效。

## 自动化验收

- Vitest 覆盖世界法则目录与默认值、领域规则契约、确定性随机数、A* 工作区复用、Flow Field、空间哈希、快照插值、阶梯缩放、三级 LOD 决策、悬浮/选择几何、地图编辑、动物寿命/饥饿/繁殖/捕食诊断、自然回归、实体槽位复用、家庭人口与完整家庭迁居、动态承载力、居民成长、有机聚落规划、资源节点、真实携带、施工物流、百年森林、经济、战争、神力和 V6 存档。长局门禁包括固定种子与 3 个种子的完整 250 年逐 Tick 模拟，以及 10 种子 2,000 年分层门禁；后者前 100 年完整逐 Tick，随后按季度检查窗口验证槽位复用、持久诊断和文明存活。
- Playwright 覆盖新建世界、地图编辑、投放居民、暂停/加速、神力、三级视角、人口诊断、聚落名称、悬停、选择、居民成长/装备/经历/收藏、聚落检查、IndexedDB 保存与刷新载入、100/500/1000 居民代表场景，以及 384 地图、1,000 居民、约 3 万节点的完整世界门禁。
- 固定种子 30 年人口测试保持不少于初始 72 人、承载力覆盖当前人口、存在真实父母关系且饥荒死亡为 0；固定种子百年资源测试中成熟树木占初始树木比例持续高于 82%。

## 剩余风险与下一阶段

当前阶段定为“活世界可读性”，详细行为、视觉、人口、生态、性能和非目标边界见 `docs/GAME_RULES.md`；实现不得把计划中规则误报为已交付。

交付顺序已经接受为六个完整闭环，第一批已完成，其余五批仍为计划中：

1. **长寿世界基线（已完成）**：人口家庭、动物生命周期、快捷键、8× 性能和长局门禁。
2. **居民十秒可读垂直切片**：真实任务、步行与工作动画、目标解释，以及村庄/森林/矿区/海岸切片。
3. **聚落与王国可读性**：逐格领土、四级聚落、王国图层、工作热点与历史定位。
4. **完整野生生态与食物闭环**：捕食、尸体、狩猎、真实物流和岸边捕鱼。
5. **全地图视觉推广**：正式像素资产、三级 LOD 和完整世界压力复验。
6. **海洋交通扩展**：码头、船只、沉船、应急游泳与跨岛行为。

第一批的 250 年代际、多种子长局和 30 分钟 8× 浏览器门禁均已通过。下一批只进入“居民十秒可读垂直切片”：目标、原因、任务阶段、步态和工作动作仍未交付，不因长寿世界基线完成而提前标记。

具名政治、倒地救援、专业治疗、村庄军械库、低端集显、设备像素比 2、Firefox 与 Safari 实测仍是后续风险，不得插入六批核心闭环前制造范围漂移。

历史回放、多人和后端同步不在当前阶段范围。
