# 计划索引

`docs/plans/` 用于存放仍在执行、需要跨模块协作的临时实施计划；长期产品状态以各子项目的 `docs/PLAN.md` 为准。

## 当前活跃计划

- 暂无。

## 已归档

- 暂无。

## 维护规则

- 新增活跃计划时，将 Markdown 文件置于本目录，并在“当前活跃计划”中添加相对链接。
- 完成后，将计划移动至 `archive/YYYY-MM/`，在正文标注完成状态，并将链接移至对应“已归档”章节。
- 计划集合发生变化后运行 `pnpm check:plans-index`。
