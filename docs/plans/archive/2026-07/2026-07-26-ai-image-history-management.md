# AI 图片创作历史管理

> [!DONE] 已于 2026-07-26 手动验收完成并归档

## 已交付

- 使用 URL query 保留历史筛选与分页：状态、模型、仅看收藏。
- 历史详情抽屉展示图片、状态、尺寸、模型、服务、模板、参考图与版本来源等元数据；不展示提示词。
- `ai_image_generations` 增加 owner-private 收藏字段和受鉴权更新接口；不复用资源库收藏。
- 历史列表一次读取最近 50 条，供本地筛选和 12 条分页使用。

## 已验收

1. 筛选与分页写入 URL，刷新、前进后退后结果一致。
2. 收藏状态在刷新后仍保留，且用户不能修改他人的记录。
3. 详情抽屉不含提示词，并能展示可用图片和完整生成元数据。
4. 现有下载、预览、保存、再次创作、来源查看、删除流程不回归。
