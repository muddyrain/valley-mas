# 工作流循环节点实施计划

> [!DONE] 已完成并归档
> 日期：2026-07-23

## 已交付

1. 完成 `loop`、循环内部控制节点、循环配置、数组值类型及执行预算元数据。
2. 完成循环体校验、作用域引用、内部拓扑执行、数组/次数/无限三种模式、控制信号、聚合输出与迭代 trace。
3. 覆盖嵌套循环、控制节点、错误传播、深度与轮次限制的回归场景。
4. Web 已支持循环节点选择、属性表单、循环输出、主画布容器投影、内部节点与连线，以及拖入/拖出循环体。
5. 右侧运行详情支持按轮次查看循环 trace、深度与循环体节点标识。

## 验收记录

- Server 与 Web 静态校验已完成。
- 用户已完成循环节点、运行详情、嵌套与控制节点的浏览器交互验收。

## 边界

- 第一版严格串行执行，不承诺并行迭代。
- 无限循环继续由服务端轮次与截止时间限制；工具和模型调用仍受预算、权限与取消策略约束。
