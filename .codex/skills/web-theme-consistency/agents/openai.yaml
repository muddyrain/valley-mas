interface:
  display_name: "Web 主题一致性"
  short_description: "约束整个 Web 端跟随当前主题 token，避免页面和组件各写各的配色"
  default_prompt: "当任务涉及 Valley MAS Web 端的页面或共享组件时，先检查背景、Banner、空状态、按钮、卡片、表单、导航入口和头像 fallback 是否真正跟随当前主题 token；优先复用 theme-* 类、CSS 变量与共享组件，不要让单页继续写死紫蓝橙粉等独立色系。"
