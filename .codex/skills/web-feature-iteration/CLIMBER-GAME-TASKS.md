# Climber Game 任务清单（线程内持续维护）

> 说明：本清单用于 `packages/climber-game` 的迭代跟踪，避免一次性做完导致质量失控。  
> 范围：尽量把复杂逻辑收敛在独立包，不污染 `apps/web` 入口。

## P0 基础架构（已完成）

- [x] 新建独立包承载 3D 原型与关卡逻辑（`packages/climber-game`）。
- [x] `apps/web` 仅作为挂载入口，核心逻辑不下沉到业务页。
- [x] 三人称跟随相机 + 鼠标视角 + 键盘移动/跳跃。
- [x] 进入游戏遮罩（Pointer Lock）与 Esc 暂停菜单。

## P1 核心可玩性（已完成）

- [x] 平台实体碰撞修复（平台与边界墙为实心）。
- [x] 基础地面可站立，不会穿透。
- [x] 终点判定与登顶特效（环形脉冲 + 爆发特效）。
- [x] HUD 内展示高度/进度/按键说明（不依赖页面外部展示）。
- [x] 菜单内全屏开关 + 键位优化（`P` 打开菜单，`Esc` 作为系统退出键）。

## P2 角色与动画（进行中）

- [x] 角色切换入口内置到游戏菜单（不放页面外控制）。
- [x] 支持 `idle / run / jump` 三态驱动。
- [x] Peach 与 Daisy 模型接入（GLB）。
- [x] Daisy 跳跃质感进一步统一（同一骨架下细调过渡参数）。
- [x] 动画切换 Debug 面板（开发态可开关，生产态隐藏）。
- [x] 模型高度/脚底自动校准工具（减少“漂浮/陷地”手工调参）。

## P3 大地图关卡（进行中）

- [x] 收敛为单张大地图（不再多关卡切换）。
- [x] 主路线平台数量收敛，并加入长短不一 setpiece 模型路线（管道/木板/货柜/斜坡/横杆）。
- [x] 移除坠落次数概念（仅保留高度/进度/时间）。
- [x] “重新开始”后直接进入操作态（自动重新锁定）。
- [x] 相机鼠标输入防突变（限制单帧鼠标增量 + 角度归一），减少突发闪转。
- [x] 关键跳点净空检测（按主路线逐段检查，识别“模型遮挡导致跳不上去”风险）。
- [x] 关键跳点 debug 可视化（主路线连线 + 阻塞模型高亮 + 风险统计）。
- [x] 关卡设计规则入参（最小可玩尺寸 / 小件聚集半径 / 头顶净空）并接入检测汇总。
- [x] 第一批新增模型接入并重排（大号货柜 / 短阶踏步石 / 桶塔），落在中段可达区域。
- [x] 新增模型流程标准化：按 `climber-game-design-guard` 执行“每批 1~3 个 + 局部重排 + 净空门槛验证”。
- [x] 模型展览升级为“全模型目录 + 当前地图实例 + 每种模型出现次数”，新增模型/实例自动同步显示。
- [x] 清理关卡僵尸点位：移除所有已禁用模型对应的 `sp` 配置，避免“源码里有但游戏里不出现”。
- [x] 精简 setpiece 资产：`SETPIECE_CATALOG/types/assets` 收敛到关卡真实使用模型，剔除未使用模型与遗留 glb。
- [x] 底层地面场景第一批：补大树 / 花草 / 岩石（底层区域启用碰撞，避开主路线关键跳点）。
- [x] 底层地表分层：土地区 + 水泥区（颜色/材质明确分层，不再是单一平面观感）。
- [x] 底层铺装密度校准：可视上“填满”，但留足角色移动和视角空间，不做拥堵堆叠。
- [ ] 地面场景碰撞回归：确认不会挡住出生区与前 3 段跳跃起步节奏。
- [ ] 增加主题区块（集装箱区、木板桥区、管道区、农场区）以提升辨识度。
- [ ] 加入移动障碍/摆锤/旋转杆（高难挑战段）。
- [ ] 主路线做一次“实跳可达性”回归（每段最大跳距/高度留余量）。

## P4 美术资源与建模（进行中）

- [x] 建立资源目录约定：`packages/climber-game/assets/models/<theme>/...`
- [x] 首批自建模块：斜坡、圆管、长短货柜、桥板、警示横杆、圆台。
- [x] 建立 Blender CLI 生成脚本：`packages/climber-game/scripts/generate_setpieces.py`
- [x] 模型运行时材质增强：按预设生成贴图风格（wood/hazard/container/metal/concrete）。
- [ ] 统一低多边形风格规范（面数、贴图尺寸、材质命名）。
- [ ] 地面美术可辨识度基线：树像树、草像草、花像花（优先轮廓 + 主色块，其次细节贴图）。
- [ ] 大树比例基线：默认高度接近现实大树观感（建议 8m+），树冠和树干比例统一。
- [ ] 草花尺寸基线：避免“微缩草点”，草簇与花簇在第三人称镜头下一眼可见。
- [ ] 纹理策略升级：从纯色材质升级到“程序贴图/贴花/顶点色混合”最小方案，先解决“无纹理感”。
- [ ] Blender 导出流程文档（坐标、缩放、骨架、动画命名、GLB 压缩）。

## P5 体验打磨（待开始）

- [ ] 新手引导（首次进入 8 秒内的最小引导）。
- [ ] 中继点/复位点（可配置是否启用）。
- [ ] 结算面板（本次用时、最高高度、最佳记录）。
- [ ] 音频分层（环境/动作/特效）与音量设置。

## 每轮提交前检查（本轮已执行）

- [x] `pnpm --filter @valley/climber-game exec tsc --noEmit`
- [x] `pnpm --filter web exec tsc --noEmit`
- [x] `pnpm --filter @valley/climber-game build`
- [x] `python .codex/skills/encoding-guard/scripts/check_mojibake.py`

## 2026-04-12（收口总结）

- [x] 斜坡碰撞系统升级为可旋转凸体（支持 `shape: ramp`），并接入 setpiece 运行时。
- [x] 斜坡碰撞基元改为与 Blender 资产同轴（默认高边在 `-Z`），不再依赖临时方向猜测。
- [x] `ramp_wedge` 改为使用资产标定碰撞参数（size/offset/local rotation），避免 localBounds 漂移导致偏差。
- [x] 碰撞体调试渲染从强红线改为低干扰显示（半透明填充 + 低强度边线 + 深度测试）。
- [x] 边缘站立抖动问题已修复（落地粘附滞回 + 落地音效冷却）。
- [x] 小高差贴边过渡已优化（step assist 自动上台阶）。
- [x] `ramp_wedge` 物理从“纯三角楔形”改为“梯形+顶部小平面”凸体（与玩家建议一致）。
- [x] `ramp_wedge` 可视模型同步改为“梯形+顶部小平面”，并完成 Blender 脚本重导出。
- [x] 为避免静态资源缓存，斜坡模型切换为 `ramp_wedge_v2.glb` 并在 catalog 强制引用新资源。
- [x] `ramp_wedge` 改为运行时程序化视觉网格（与碰撞同参数），保证视觉层与物理层 1:1 对齐。
- [x] 程序化斜坡视觉从“中心对齐”改为“底面对齐（y=0 起）”，与碰撞高度一致。
- [ ] 待下轮实机复测：`ramp_wedge` 在你标注点位是否已“完全契合”模型表面，如仍有偏差则做单实例精调。

## 下轮优先任务（接着做）

- [ ] 地面场景冲刺包（分 3 批）：先树石，再草花，最后土地/水泥地分层与填充。
- [ ] 每批地面模型补齐碰撞并跑一次出生区 + 主路线前段回归，避免新增阻挡。
- [ ] 先做“可辨识度”再做“精细度”：用 80/20 原则快速把观感从“空”提升到“像一个世界”。
- [ ] 为 `ramp_wedge` 增加开发态“单实例碰撞微调参数”（offset/rotation 快速校准）。
- [ ] 补充“碰撞可达性 + 贴合度”调试报告（打印斜坡顶面高度与角色脚底差）。
- [ ] 完成一轮路线全段复测并固化参数，确保关键跳点稳定可达。
- [ ] 以“净空检测高风险=0”为门槛，分批补充新模型（每批不超过 3 个）并回归验证。
- [ ] 清理“小件密集区”告警段，避免连续小板块挤在一起影响操作体验。
